---
title: Mitigating Package Hallucinations in Large Language Models via Model Editing
title_zh: 通过模型编辑缓解大语言模型中的包幻觉
authors: "Shuhan Liu, Yukai Zhao, Xing Hu, Kui Liu, Xiaohu Yang, Xin Xia"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02052v1"
tags: ["query:lpt"]
score: 9.0
evidence: 直接针对LLM中的包幻觉抑制，采用模型编辑方法
tldr: "大型语言模型在软件工程中可能出现包幻觉，生成不存在的包名，可被用于供应链攻击。本文提出BOUND框架，通过将缓解包幻觉转化为包有效性边界编辑问题，采用风险感知定位和边界感知目标编辑模型。实验表明，BOUND在包推荐任务中降低包级幻觉率79.9%，在代码生成和pip install任务中也有效，提高了LLM在包相关输出中的可靠性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02052v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 424, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02052v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1875, \"height\": 726, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02052v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 945, \"height\": 197, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02052v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1868, \"height\": 939, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02052v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1767, \"height\": 830, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02052v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 911, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02052v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1860, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02052v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 908, \"height\": 237, \"label\": \"Table\"}]"
motivation: LLM在软件工程中生成不存在的包名，可被用于供应链攻击，需缓解包幻觉。
method: BOUND将包幻觉缓解建模为包有效性边界编辑，通过风险感知定位相关模块并使用边界感知LoRA适配器进行编辑。
result: "包推荐任务中，BOUND降低包级幻觉率79.9%（编辑提示）和65.4%（未见提示），在代码生成和pip install推荐中分别降低12.8%和34.0%。"
conclusion: BOUND通过模型编辑精炼LLM的包有效性边界，有效降低包幻觉，提高包相关输出的可靠性。
---

## 摘要
大语言模型（LLMs）在软件工程任务中展现出强大能力，例如代码生成、库推荐和依赖配置。然而，近期研究表明，LLMs可能遭受包幻觉问题，即生成不存在或无效的包名。攻击者可能利用这些幻觉注册恶意包，从而发起软件供应链攻击。因此，缓解包幻觉对于提高LLM辅助软件开发的可靠性和安全性至关重要。本文提出BOUND，一种轻量级局部模型编辑框架，用于缓解LLMs中的包幻觉。BOUND将包幻觉缓解形式化为包有效性边界编辑问题，其中边界指模型在给定任务上下文中区分有效包与幻觉包名的能力。它首先通过风险感知定位策略定位与包幻觉相关的模块，然后使用边界感知目标（强化有效包、抑制幻觉包并保持局部行为）的轻量级LoRA适配器编辑这些模块。实验结果表明，BOUND在保留有效包推荐的同时有效减少了包幻觉。在包推荐任务中，BOUND在编辑提示上将包级幻觉率（Package-HR）降低了79.9%，在未见提示上降低了65.4%。所学习的包有效性边界进一步泛化到其他包相关任务，在代码生成中将Package-HR降低12.8%，在pip install推荐中降低34.0%。这些结果表明，BOUND优化了LLMs的包有效性边界，提高了包相关输出的可靠性。

## Abstract
Large language models (LLMs) have demonstrated strong capabilities in software engineering tasks, such as code generation, library recommendation, and dependency configuration. However, recent studies show that LLMs may suffer from package hallucination, where they generate non-existent or invalid package names. These hallucinations can be exploited in software supply chain attacks, as attackers may register malicious packages under hallucinated names. Therefore, mitigating package hallucination is important for improving the reliability and security of LLM-assisted software development. In this paper, we introduce BOUND, a lightweight localized model editing framework for mitigating package hallucinations in LLMs. BOUND formulates package hallucination mitigation as a package-validity boundary editing problem, where the boundary refers to the model's ability to distinguish valid packages from hallucinated package names under a given task context. It first locates modules related to package hallucination through a risk-aware localization strategy, and then edits these modules with lightweight LoRA adapters using a boundary-aware objective that reinforces valid packages, suppresses hallucinated packages, and preserves locality behavior. Experimental results show that BOUND effectively reduces package hallucinations while preserving valid package recommendations. In the package recommendation task, BOUND reduces package-level hallucination rate (Package-HR) by 79.9% on edit prompts and by 65.4% on unseen prompts. The learned package-validity boundary further generalizes to other package-related tasks, reducing Package-HR by 12.8% in code generation and by 34.0% in pip install recommendation. These results show that BOUND refines the package-validity boundary of LLMs and improves the reliability of package-related outputs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLMs）在软件工程任务（如代码生成、库推荐、依赖配置）中表现优异，但容易产生“包幻觉”（package hallucination）——即生成不存在或无效的软件包名。攻击者可利用这些幻觉名称注册恶意包，发动软件供应链攻击（如包混淆攻击）。
- **问题**：现有缓解方法分为外部缓解（如检索增强、自我修正）和内部模型适应（如微调），但前者增加推理开销或流水线复杂度，后者更新大量参数可能导致过拟合或损害通用能力。
- **目标**：提出一种轻量级、不需全模型重训练的局部模型编辑方法，直接修正模型内部的包有效性边界，降低包幻觉的同时保留有效包推荐。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将包幻觉缓解视为**包有效性边界编辑问题**——模型在给定任务上下文下区分有效包与幻觉包的能力。BOUND 通过定位与包边界最敏感的模块，并注入 LoRA 适配器进行局部编辑。
- **关键技术细节**：
  - **风险感知定位**：对每个高风险的包推荐提示，计算下一 token 的 logits，构造边界风险分数 \( R_i = \text{LSE}(z_i[H^{tok}_i]) - \lambda_a \text{LSE}(z_i[G^{tok}_i]) \)，反向传播该分数以获得每个候选模块（注意力/MLP 投影矩阵）的显著性得分，选择 top-k 模块作为编辑目标。
  - **边界感知模型编辑**：仅在被定位的模块中注入 LoRA 适配器（低秩矩阵 \(A_m, B_m\)），保持原始权重冻结。优化三部分损失：
    - 有效包负对数似然（Valid NLL）：鼓励保留有效包。
    - 幻觉包不应似然（Hallucinated Unlikelihood）：降低幻觉包概率。
    - 局部性 KL 散度（Locality KL）：保持原始模型在相似提示上的行为。
  - 总损失：\( L_{\text{BOUND}} = \lambda_v L_{\text{valid}} + \lambda_h L_{\text{hall}} + \lambda_l L_{\text{loc}} \)。

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据集**：基于 Spracklen 等[12] 发布的 LLM 生成提示数据集，选取 Python 子集。为每个模型（DeepSeekCoder、Qwen3、Llama-3.1）构建独立的**高风险提示集**（若该提示在 5 次生成中样本级幻觉率 > 0.5 则保留）。统计：DeepSeekCoder 741 条，Qwen3 609 条，Llama-3.1 1134 条。分割为 4 个编辑子集（每子集 50 条）和 1 个未见评估子集。
- **场景（任务）**：
  - 主要任务：包推荐（显式要求推荐包）。
  - 泛化任务：代码生成、pip install 命令推荐。
- **基准方法**：Base（未编辑模型）、Full-FT（全参数微调）、Self-Refinement（自我修正）、ROME（单事实编辑）、MEMIT（批量事实编辑）、DINM（行为编辑）。
- **评价指标**：
  - Sample-HR（样本级幻觉率）：包含至少一个幻觉包的样本比例。
  - Package-HR（包级幻觉率）：所有生成包中幻觉包的比例。
  - Valid-Rate（有效包率）：至少包含一个有效包的样本比例。

### 4. 资源与算力

- 文中明确提及：**硬件**为 Linux 服务器，Ubuntu 22.04 LTS，双路 Intel Xeon Platinum 8358P CPU（64核）、1TB RAM、八块 NVIDIA A100-SXM4 GPU（80GB 显存）。**软件**：PyTorch + HuggingFace Transformers，权重以 bfloat16 加载。
- **训练时长**：定位阶段平均 23.81 秒，编辑阶段平均 83.09 秒（每编辑集 50 条提示）。相比 ROME（384.45 秒）和 Full-FT（124.16 秒）更高效。
- **存储开销**：BOUND 仅需平均 731 KB 的 LoRA 参数，约是全微调检查点的 1/20000。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：在 3 个模型 × 2 种评估集（编辑+未见）上对比 6 种方法，报告 4 折平均结果。
  - 泛化实验：2 个下游任务（代码生成、pip install），在 100 条未见提示上评估 4 折平均值。
  - 消融实验：5 种变体（w/o 定位、w/o Valid-NLL、w/o Hall-UL、w/o Locality-KL）在 100 条未见提示上评估。
  - 额外分析：HumanEval 上的代码准确率（pass@1、pass@10）检测编辑对通用能力的影响；成本分析。
- **充分性评价**：
  - **充分**：覆盖多个模型家族、多种基线、编辑/未见数据集、多个任务、消融、泛化、成本分析、通用能力测试。
  - **客观公平**：所有训练基线使用相同的编辑案例；生成参数固定（temp=0.7, top_p=0.9, 5 次采样）；指标定义清晰；控制变量如包有效性检查使用 PyPI 元数据并考虑知识截止时间。
  - **潜在偏差**：仅针对 Python PyPI 生态系统；提示来源单一（基于已有数据集）；提取包名依赖正则表达式（虽用 one-shot 约束格式）。

### 6. 论文的主要结论与发现

- **BOUND 显著降低包幻觉**：在包推荐任务编辑提示上，Package-HR 平均降低 79.9%（从 0.4463 到 0.0897），Sample-HR 降低 75.9%。
- **泛化能力强**：编辑边界泛化到未见提示（Package-HR 降低 65.4%）和下游任务（代码生成降低 12.8%，pip install 降低 34.0%）。
- **优于现有方法**：相比 ROME、DINM 等，BOUND 在减少幻觉的同时保持高 Valid-Rate（不通过抑制生成包来降低幻觉）；相比 Full-FT，BOUND 更轻量且泛化更稳定。
- **消融证实组件必要性**：去除定位、有效包 NLL、幻觉 unlikelihood 或局部性 KL 均导致性能显著下降。
- **对通用能力影响极小**：HumanEval 上 pass@1 变化在 ±1.4% 以内。

### 7. 优点：方法或实验设计上的亮点

- **新视角**：将包幻觉重新定义为包有效性边界问题，而不是简单事实纠错，更贴合实际场景。
- **轻量高效**：编辑仅需 83 秒，存储仅 731KB，可即插即用，无需外部检索或全模型微调。
- **强泛化**：单个任务的编辑可迁移到其他包相关任务，且保留有效包生成。
- **风险感知定位**：通过边界风险分数反向传播，准确定位影响包边界的模块（包括注意力和 MLP），比随机选择更有效。
- **三目标损失设计**：平衡了强化有效、抑制幻觉和保持局部性，避免极端行为（如完全停止生成包）。
- **实验严谨**：多模型、多基线、多折交叉、多指标、泛化验证、消融分析、成本分析、通用能力测试，覆盖全面。

### 8. 不足与局限

- **生态系统局限**：仅针对 Python PyPI，未验证对其他语言（如 Java、JavaScript）或包管理器的适用性。
- **数据来源单一**：高风险提示集基于已有公开数据集，可能未涵盖真实开发中的复杂依赖管理场景（如版本约束、环境配置）。
- **包提取不确定性**：依赖正则表达式从自由文本中提取包名，可能因输出格式变化导致误提取（虽用格式约束减轻，但仍有风险）。
- **未讨论故障模式**：若定位模块选择受噪声干扰（如梯度噪声、编辑案例质量），可能影响编辑效果；聚合多案例可缓解但未彻底消除。
- **编辑规模限制**：当前仅编辑 50 个案例（每折），更大规模编辑可能带来遗忘或性能下降（文中未测试）。
- **缺少在线评估**：未在真实开发者使用场景（如与 IDE 集成）中测试安全性和用户体验。

（完）
