---
title: "SkillRise: Agentic Reinforcement Learning for Cross-Task Skill Evolution"
title_zh: SkillRise：面向跨任务技能演化的智能体强化学习
authors: "Zhiyuan Yao, Yuxin Chen, Zhengxi Lu, Zishan Xu, Yueqing Sun, Yifu Guo, Yuquan Lu, Zhengzhou Cai, Kangning Zhang, Zhuowen Han, Zi-Han Wang, Ziang Ye, Qi Gu, Xunliang Cai, Weiwen Liu, Yongliang Shen"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26784v1"
tags: ["query:lpt"]
score: 8.0
evidence: 跨任务技能演化的统一RL框架，解耦信用分配
tldr: "为了LLM智能体在关联但不同任务间共享可复用模式，现有方法或独立处理任务，或采用多阶段流水线。本文提出SkillRise，一种统一的强化学习框架，通过将相关实例组织成渐进挑战序列，并利用单一策略交替执行任务求解和策展演进的技能文档，实现跨任务技能学习。在ALFWorld等基准上，SkillRise相比最强基线Pass@1提升2.3-8.5个百分点，且学习到的策展策略在相同任务重试中仍然有效。测试时性能随相关任务序列增长而提升，表明跨任务可迁移技能的重用，同时显著降低多阶段流水线的运行时开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26784v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 719, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1382, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1389, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1381, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1413, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1387, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26784v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1390, \"height\": 378, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26784v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26784v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26784v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 289, \"label\": \"Table\"}]"
motivation: 现有方法将任务独立处理或多阶段流水线，无法高效提取和复用跨任务的共享技能。
method: 构建渐进任务序列，单策略交替进行任务求解与技能文档策展，并通过解耦信用分配联合优化。
result: "在三个基准上Pass@1提升2.3-8.5%，且策展策略可泛化至同任务重试，测试时性能随任务序列长度提升。"
conclusion: SkillRise提供了一种简单高效的训练范式，使LLM智能体能够跨任务提取、精炼和复用可迁移技能。
---

## 摘要
大型语言模型智能体经常遇到相关但不同的任务，这些任务共享可重用的解决方案模式。然而，标准的智能体强化学习将任务视为独立的片段，而现有的技能学习方法要么专注于重复尝试单一任务，要么使用包含多个阶段的流水线，这些阶段将提取、检索和执行纠缠在一起。我们引入SkillRise，一个统一的强化学习框架，用于跨任务学习技能。SkillRise将相关实例组织成渐进挑战的序列，并使用单一策略交替进行任务求解和策划一个不断演化的技能文档，该文档直接传递给下一个任务。解耦的跨任务信用分配通过当前任务结果监督求解，并通过折扣的下游结果监督策划。在ALFWorld、WebShop和ScienceWorld上的实验表明，SkillRise在比较方法中实现了最强的Pass@1性能，相较于最强基线提高了2.3到8.5个百分点。尽管在不同任务上训练，其学到的策划策略在相同任务的重复尝试中仍然有效。进一步的分析揭示了测试时跨任务的扩展：即使每个任务只尝试一次，随着相关任务序列的延长，性能也会提升。这一趋势表明SkillRise跨任务重用可迁移技能，而非从相同任务的重复采样中受益。SkillRise在显著降低多阶段技能学习管道的运行时开销的同时，仍保持强劲性能。总之，这些结果为LLM智能体提供了一种简单高效的训练范式，用于跨任务提取、提炼和重用可迁移技能。

## Abstract
Large language model agents often encounter related yet distinct tasks that share reusable solution patterns. Yet standard agentic reinforcement learning treats tasks as independent episodes, while existing approaches to skill learning either focus on repeated attempts of one task or use pipelines with multiple stages that entangle extraction, retrieval, and execution. We introduce SkillRise, a unified reinforcement learning framework for learning skills across tasks. SkillRise organizes related instances into progressively challenging sequences and uses a single policy to alternate between task solving and curating an evolving skill document passed directly to the next task. Decoupled credit assignment across tasks supervises solving with the current task outcome and curation with discounted downstream outcomes. Experiments on ALFWorld, WebShop, and ScienceWorld show that SkillRise achieves the strongest Pass@1 performance among the compared methods, with gains over the strongest baseline ranging from 2.3 to 8.5 percentage points. Although trained across distinct tasks, its learned curation policy remains effective for repeated attempts on the same task. Further analysis reveals scaling at test time across tasks: performance improves with longer sequences of related tasks even when each task is attempted only once. This trend suggests that SkillRise reuses transferable skills across tasks rather than benefiting from repeated sampling of the same task. SkillRise further retains strong performance while substantially reducing the runtime overhead of skill learning pipelines with multiple stages. Together, these results provide a simple and efficient training paradigm for LLM agents to extract, refine, and reuse transferable skills across tasks.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义
- **研究动机**：LLM Agent在实际部署中常遇到一系列相关但不同的任务（如ALFWorld中不同物品的拾取、放置），这些任务共享可重用的交互模式。然而，标准智能体强化学习（Agentic RL）将每个任务视为独立片段，丢弃历史经验，导致重复探索；现有技能学习方法要么局限于对同一任务的重复尝试（如Reflexion、LaMer），技能难以跨任务迁移，要么采用多阶段流水线（如RetroAgent、SkillRL），将技能提取、存储、检索和执行耦合在一起，使得下游性能归因困难且计算开销大。
- **整体含义**：本文目标是设计一个统一、端到端的RL框架，使Agent能在处理不同但相关的任务序列时，自动从先前交互中提取可迁移技能，并在后续任务中重用和精炼，从而整体提升跨任务性能，同时避免复杂的外部管理管道。

#### 2. 论文提出的方法论
- **核心思想**：SkillRise将同一任务族中的不同实例组织成**渐进困难序列**（由易到难），使用**单一策略**（shared policy）在序列内交替执行两个角色：**任务求解**（task solving）和**技能策展**（skill curation）。任务求解时，Agent根据当前技能文档（初始为空）生成轨迹并获得当前任务奖励；任务完成后（除最后一个任务），同一策略根据该任务的轨迹和奖励**重写技能文档**，并将新文档直接传递给下一个任务，作为唯一的跨任务信息通道。
- **关键技术细节**：
  1. **跨任务序列构建**：依据环境提供的任务族元数据，从同一族中选择多样化的实例，并按其属性复杂度（如WebShop中按所需属性和选项数量）排序。序列长度为K（实验中K=3），“逐步挑战”设计使前面任务的经验能支撑后面任务。
  2. **解耦信用分配**（Decoupled Credit Assignment）：将任务求解和技能策展两个角色的学习信号分离。求解角色只获得当前任务奖励；策展角色获得后续所有任务的**折扣累积奖励**（γ=0.6），即策展的效果由下游任务表现衡量，且赋予更近的任务更高权重。
  3. **角色感知组相对优化**（Role-Aware Group-Relative Optimization）：对同一任务位置和同一角色（求解/策展）的N个独立试验（trials）计算组内相对优势（advantage = 个体返回 - 组平均返回），然后应用PPO风格的clipped objective进行优化。这确保了不同角色和不同序列位置的返回不会相互混淆。
- **公式与算法流程**（文字描述）：
  - 每个训练batch包含若干任务序列；每个序列生成N个独立rollout（从空文档开始）。
  - 对于序列中第i个任务和第n个trial：
    - 求解阶段：使用文档 $S_{i-1}$ 生成轨迹 $\tau_i$，获得奖励 $r_i$；
    - 策展阶段（i<K）：基于 $S_{i-1}$、$\tau_i$、$r_i$ 生成新文档 $S_i$。
  - 返回：求解返回 = $r_i$；策展返回 = $\sum_{j=i+1}^{K} \gamma^{j-i} r_j$。
  - 优势：针对每个(i, role)组内N个试验的返回减去均值。
  - 优化：所有策略生成的token共享该角色的advantage，采用clipped PPO目标联合优化。

#### 3. 实验设计
- **数据集/场景**：三个广泛使用的交互式文本环境：
  - **ALFWorld**（TextWorld版）：6个任务族（Pick, Look, Clean, Heat, Cool, Pick2），要求导航和操作物体。
  - **WebShop**：1000个产品版，要求搜索、配置并购买符合自然语言描述的物品。
  - **ScienceWorld**：128个保留实例，评估科学推理和实验。
- **Benchmark与评估指标**：
  - 主要指标：Pass@1（一次尝试的成功率），以及Pass@2和Pass@3（允许重试，并携带技能文档，用于测试同任务泛化）。
- **对比方法**：
  - **Prompt基线**：Zero-shot、ReAct、Reflexion。
  - **任务独立RL**：PPO、RLOO、GRPO、GiGPO（每种方法使用相同的24个rollouts每任务，以保证公平）。
  - **跨任务/跨尝试方法**：LaMer（Meta-RL，在同一任务上重复尝试并总结反思）。
- **训练设置**：所有方法使用Qwen3-1.7B或4B backbone。SkillRise训练：batch size=16序列，K=3，N=8独立试验；每update共处理384个任务plays（与其他RL方法一致）。学习率1e-6，max response length 1024，训练最多150 updates。评估时temperature=0.7。

#### 4. 资源与算力
- **文中明确**：所有实验在**8张 NVIDIA H800 GPU**上完成，使用FSDP和vLLM进行训练和推理部署。
- **未明确**：未说明具体训练时长（如小时数），但对多阶段管道（RetroAgent、SkillRL）的相对运行时间进行了比较：在相同GPU配置下，RetroAgent耗时是SkillRise的6.0倍，SkillRL是4.3倍，说明SkillRise在效率上具有显著优势。

#### 5. 实验数量与充分性
- **主要实验组数**：
  1. **三个benchmark的Pass@1结果**（Table 1）：各方法在ALFWorld（6个任务族平均）、WebShop、ScienceWorld上的表现。
  2. **同任务重试泛化**（Table 2）：Pass@1/2/3。
  3. **跨任务测试时缩放**（Figure 2）：序列长度K=2,4,6时的Pass@1（仅一次尝试），与多种方法对比。
  4. **模型规模消融**（Table 3）：Qwen3-1.7B vs 4B。
  5. **折扣因子敏感性**（Figure 3左）：γ=0.3,0.4,0.6,0.7的收敛曲线。
  6. **技能策展消融**（Figure 3右）：对比完整SkillRise、无策育变体（w/o Curation）和GRPO。
  7. **管道复杂度与效率对比**（Figure 4）：与RetroAgent、SkillRL的性能和相对运行时间。
  8. **附录案例研究**（Appendix B）：两个训练rollout的具体案例。
- **充分性与公平性**：
  - 控制计算预算：所有可训练方法每update处理相同数量任务plays（384）。
  - 消融实验覆盖关键设计：技能策育、折扣因子、模型规模。
  - 测试时缩放实验表明趋势而非偶然，且对比方法未表现出类似增长，说明SkillRise学到的是可迁移的跨任务自改进机制。
  - 局限性：仅三个文本环境，且模型最大4B参数；未在多模态或真实世界环境中验证；要求任务族元数据。

#### 6. 论文的主要结论与发现
- **SkillRise在所有三个基准上实现最强Pass@1**：ALFWorld（85.9%）、WebShop（84.4%）、ScienceWorld（54.6%），分别超过最强基线GiGPO 2.3、7.1、8.5个百分点。
- **跨任务训练的策育策略可泛化到同任务重试**：在Pass@3上，SkillRise在所有基准上均优于LaMer（LaMer专门在重复尝试训练），说明学到了通用的经验提取策略。
- **测试时跨任务缩放**：随着测试时相关任务序列长度K增加，SkillRise性能单调提升（K=2:83.6% → K=6:87.5%），而对比方法无此趋势。这表明技能文档在长序列中逐步精炼，带来更强的跨任务迁移。
- **更大模型下优势更明显**：从1.7B到4B，SkillRise提升7.8个百分点，而GRPO提升4.7、LaMer提升3.3，说明额外参数容量更有利于联合执行与跨任务抽象。
- **高效性**：相比RetroAgent（6.0×）和SkillRL（4.3×），SkillRise保持相近或更好性能，且运行时间大幅降低。
- **端到端设计优于多阶段管道**：无需外部教师模型、独立记忆和检索模块，直接通过后续任务监督策育，学习信号更清晰。

#### 7. 优点
- **方法创新**：将跨任务技能学习建模为单一策略交替求解与策育的端到端过程，避免了复杂外部组件；解耦信用分配自然区分了“即时执行”与“未来影响”，使学习信号与角色时间角色对齐。
- **实验严谨**：计算预算控制公平；消融实验系统验证了关键设计；测试时缩放分析不仅展示结果，还揭示了机制（技能文档精炼）而非重复采样效应。
- **实用价值**：训练后的策育策略可直接用于同任务重试（无需额外训练），且测试时随序列增长自动提升，无需微调，有良好适应性。
- **效率突出**：与最先进的技能学习管道相比，性能持平或更优，但运行时间降低一个量级，更适合实际部署。

#### 8. 不足与局限
- **依赖任务族元数据**：需要预先知道任务族划分来构建序列，无法自动发现任务间的相关性。论文提及这是未来方向。
- **模型规模限制**：仅验证到4B参数，更大模型（如7B/13B）上的行为未知。由于资源约束，未进行更大规模实验。
- **环境单一**：三个环境均为纯文本交互，未涉及多模态（如视觉）或真实世界复杂环境（如网页、桌面操作）。技能文档的表征形式（文本）可能不适用于需要结构化知识或更抽象技能的场景。
- **奖励可验证性假设**：目前依赖可验证的结果奖励（如任务成功/失败），对于需要主观评价或部分反馈的任务，框架需要调整。
- **未深入分析**：技能文档的内容质量、更新过程的可解释性、过长序列下文档退化风险等未被探讨。
- **实验对比局限**：对比方法中LaMer也是针对重复尝试设计，但SkillRise在跨任务训练后仍能同任务泛化，说明其策略确实更通用；但缺乏与其他端到端跨任务学习变体（如Evolving-RL、SkillOS）的直接比较（文中提到但未复现对比）。

（完）
