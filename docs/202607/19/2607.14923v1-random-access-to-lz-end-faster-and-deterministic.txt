Title: Random Access to LZ-End: Faster and Deterministic

URL Source: https://arxiv.org/pdf/2607.14923v1

Published Time: Fri, 17 Jul 2026 01:37:41 GMT

Number of Pages: 54

Markdown Content:
# Random Access to LZ-End: Faster and Deterministic 

Itai Boneh 1 and Paweł Gawrychowski 11University of Wrocław 

Abstract 

The LZ-End parsing of a length-n string is a variation of Lempel-Ziv compression introduced by Kreft and Navarro [DCC 2010], motivated by the lack of a linear-size structure with O(log n)

access time for the classical variant. While the original paper was only able to provide efficient extraction from the phrase boundaries, recently Kempa and Saha [SODA 2022] established that, for a string S whose LZ-End parsing consists of z phrases, there exists a random access data structure that uses O(z) space and guarantees O(log 4 n · log log n) query time. However, their proof does not yield an efficient construction algorithm, and their data structure is inherently randomized. We resolve both limitations by providing a deterministic, O(z)-space data structure that sup-ports random access queries in polylogarithmic time and can be constructed in O(z log 2(n/z )) 

time directly from the LZ-End parsing. In addition to eliminating randomness and provid-ing an efficient construction algorithm, the query time of our data structure is O(log 2(n/z )) ,significantly improving upon the query time of Kempa and Saha. We also show that our techniques can be used to support the more general substring-extraction. Namely, we present a data structure with the same space and the same construction time that given two indices i and j, outputs S[i..j ] in O(j − i + log 2 nz ) time. arXiv:2607.14923v1 [cs.DS] 16 Jul 2026 1 Introduction 

The natural approach to compressing data is to identify its repeating fragments. Then, for every subsequent occurrence of such a fragment, instead of spelling it out explicitly again, we can write down a pointer to the previous occurrence. This is the underlying high-level idea of the well-known Lempel-Ziv compression scheme [ZL77] used in popular compressors such as ZIP , gzip , RAR , PKZIP ,and PNG .While being able to compress the input to obtain its hopefully significantly smaller compressed representation that can be still decompressed to obtain the original input is already interesting, in most applications we would actually prefer to avoid the decompression and instead directly operate on the compressed representation. Ideally, we would like to augment the compressed representation with a data structure of roughly the same size that allows querying the original input. We refer to Navarro’s survey [Nav22] for an overview of such results for different compression methods. While the ultimate goal is designing a compressed index , which is a data structure that allows for efficient pattern matching queries [BGG +15, BEGV18, CEK +21, KN13, NII +20, GGK +14, TTS15, CNP21], the first step (and in fact an important building block in many of the compressed indexes) is a compressed random access structure. A compressed random access structure for a string S[1 ..n ] should use space roughly proportional to the size of the compressed representation of the input and allow for extracting any character S[i]

efficiently. Ideally, we would like such a query to take logarithmic time in the length of the string. Whether this is known to be possible depends on the chosen compression method. A Lempel-Ziv (LZ77) representation of a string S[1 ..n ] is a factorization of the string into z blocks called phrases: S[1 ..n ] = P1P2 . . . P z . Each phrase Pi is either a single character, that is, |Pi| = 1 ,or has an earlier occurrence in S. This allows us to encode each phrase is constant space by either specifying its only character, or the position and the length of the previous occurrence. It is known [LZ76] that greedily choosing each phrase to be as long as possible results in the smallest number of phrases. A related (provably weaker, but easier to operate on) compression method is a straight-line program (SLP) representation, which is simply a context-free grammar with exactly one production for each non-terminal that derives exactly one string S. It can be seen that an SLP consisting of g productions can be converted into a LZ77 parse of size g. In the other direction, Charikar et al. [CLL +05] and Rytter [Ryt03] independently established that a LZ77 parse of size z can be converted into a SLP consisting of O(z log( n/z )) productions. In fact, the obtained SLP is balanced ,meaning that the depth of the derivation tree is O(log n). This has the advantage of automatically providing a compressed random access structure with query time O(log n) and size O(z log( n/z )) ,which simply operates by descending down in the grammar. Verbin and Yu [VY13] proved that, roughly speaking, this is very close to optimal. Namely, any structure of size O(z polylog n) must have Ω(log n/ log log n) query time, for the case where n = z1+ ϵ. This however does not exclude the possibility of providing a compressed random access structure with size roughly proportional to the size of the compressed representation and, say, logarithmic query time. This goal has been in fact achieved for grammar compression. Bille et al. [BLR +15] showed that, assuming the Word RAM model, given a SLP representation of size g representing a string S[1 ..n ],we can build a data structure of size O(g) that supports random access queries in O(log n) time. Later, Ganardi, Jeż, and Lohrey [GJL21] showed a stronger version of this statement: any SLP of size g representing a string S[1 ..n ] can be converted into a balanced SLP of size O(g) representing the same string. This can be seen as the ultimate random access structure for SLPs, although the lower bound of Verbin and Yu [VY13] still leaves the possibility of designing a structure of size 

O(g) and allowing random access in optimal O(log n/ log log n) time. Somewhat surprisingly, the best known bounds for a LZ77 compressed random access structure follow by converting the LZ77 2parse to a grammar, and achieve O(z log( n/z )) space with O(log nz ) query time (see [BCG +21] for an alternative solution achieving the same time-space bounds). The logarithmic space increase is a major drawback of such a solution: after all, the compression ratio might actually be of a similar magnitude (as is the case for a random input), so by building such a structure we are not benefiting at all from using a compressed representation. Fully understanding the complexity of random access for LZ77 compressed strings remains to be a major open problem in the area. Motivated by the lack of progress on understanding the complexity of random access for LZ77 compressed strings, Kreft and Navarro [KN13] introduced a restricted variant of LZ77, called LZ-End, in which the previous occurrence of each phrase needs to end at a phrase boundary. With this restriction, the greedy parsing is not necessarily optimal. However, finding the optimal LZ-End parsing of a string was shown to be NP-hard [BFK +23], when the greedy parse can be found efficiently [KK17b], and still behaves very well in practice [KK17a]. While already the original paper established that extracting substrings ending at a boundary can be done efficiently with only O(z)

space for LZ-End parse consisting of z phrases (in fact, this can be seen as the reason for restricting the parsing in such a way), it was not known whether LZ-End admits better compressed random access structures than that of a LZ77 until 10 years later Kempa and Saha [KS22] showed that, in fact, LZ-End does admit a data structure of size O(z) that supports random access queries in polylogarithmic time. However, their construction has two major drawbacks. 1. The original proof is purely existential: that is, they only showed that, for any string S[1 ..n ],there exists a data structure of size O(z) with polylogarithmic query time. The construction crucially uses probabilistic method, so we can either verify whether the obtained data structure is correct after construction, or we can compromise on a data structure for which the bound on the query time holds with high probability. 2. The guarantee on the query time in the original proof, while polylogarithmic, is actually quite high: O(log 4 n·log log n). This should be compared with the only known lower bound, which is 

Ω(log n/ log log n) for structures of size O(z polylog n). Further, the time-space product of the original structure (a natural measure for data structures) is as high as O(z log 4 n · log log n),while for the LZ77 compression it is a much more reasonable O(z log 2(n/z )) .

Our results. Our contribution is twofolds. First, we adapt the high-level idea used by Kempa and Saha to obtain, for a string of length n described by a LZ-End parse consisting of z phrases, a random access structure of size O(z) and polylogarithmic query time that can be constructed deter-ministically in ˜O(z) time. As explicitly mentioned by Kempa and Saha, their data structure can be either constructed in expected O(poly( n)) time with worst-case queries or in worst-case O(poly( n)) 

time with expected-time queries. Thus, we not only completely remove the randomization, but further bring down the construction time to depend on the size of the compressed representation of the text instead of its length. Second, with further combinatorial insight we are able to significantly improve the query time, which was as high as O(log 4 n · log log n) in the original construction. More specifically, our query time is only O(log 2( nz )) .

Theorem 1.1. Given an LZ-End factorization Z consisting of z phrases of a string S[1 ..n ], we can compute in time O(z log 2( nz )) a data structure of size O(z) supporting a random access query in 

O(log 2( nz )) time. 

We note that the time-space tradeoff of our structure is O(z log 2( nz )) , which matches the best known tradeoffs for the LZ77 random access structure. Our techniques can be applied and enhanced to additionally support the stronger substring extraction query. 3Theorem 1.2. Given an LZ-End factorization Z consisting of z phrases of a string S[1 ..n ], we can compute in time O(z log 2( nz )) a data structure of size O(z) that given two indices i, j ∈ [n] outputs 

S[i..j ] in O(j − i + log 2( nz )) time. 

# 2 Preliminaries 

Integer intervals. We use standard notation to denote consecutive sets of integers. For integers 

i, j , we denote [i..j ] = {i, i + 1 , . . . , j } (if j < i then [i..j ] = ∅). We also denote [i] = [1 ..i ], and 

(i..j ) = ( i..j − 1] = [ i + 1 ..j ) = [ i + 1 ..j − 1] .

Strings. A string S with length n over alphabet Σ is a sequence of symbols S[1] S[2] . . . S [n] where 

S[i] ∈ Σ for every i ∈ [n]. For integers i, j ∈ [n] we denote S[i..j ] = S[i]S[i + 1] ..S [j]. We call S[i..j ]

a substring of S.

LZ-End factorization. An LZ-End factorization of a string S[1 ..n ] is a partition of S into sub-strings P1, P 2, . . . P z called phrases. For each i, we have Pi = S[ai..b i], with a1 = 1 , bz = n, and 

ai = bi−1 + 1 for every i ∈ (1 ..z ]. Each phrase satisfies one of the following conditions. Either Pi is a phrase of length one, i.e. Pi = S[ai], we call such a phrase a terminal phrase. Or, Pi = S[ai..b i], and there is some reference phrase Pj with j < i such that S(bj − | Pi|..b j ] = Pi. We call such a phrase a reference phrase, and say that Pj is the source of Pi. When we are given an LZ-End factorization, we assume that every reference phrase Pi is given alongside the index j of its source phrase. We stress that, in contrast to prior work [KS22], we are not requiring that the given parsing is greedy, that is, it is not necessarily the case that every Pi is the longest phrase that occurs earlier ending at a phrase boundary. In Section B, we show that any LZ-End factorization with z phrases of a string with length n

can be efficiently transformed into a similar size LZ-End factorization of the same string, with all the phrases having size at most nz .

Lemma 2.1. There is an algorithm that given Z, an LZ-End partition of a string S, returns Z′,an LZ-End partition of S with size at most 4z such that every phrase P ′ ∈ Z ′ has |P ′| ≤ nz . Here, 

n = |S| and z = |Z| . The algorithm runs in time O(z).

Throughout the paper, we often partition integer from [0 .. nz ] into exponential levels. We fix the notation K = {1.5i | i ∈ [0 .. log 1.5 nz ]} for the set of integer powers of 1.5 smaller than nz .

Computational model. We describe our algorithm in the standard Word RAM model, see e.g. [Hag98]. We assume that n fits in a single machine word, and basic arithmetical operations on numbers that fit in a constant number of machine words take constant time. In particular, constant-time indirect addressing is available, allowing us to implement arrays with constant lookup time. In this model, we have the following result (called deterministic perfect hashing). 

Lemma 2.2 ([Ruz08]) . Given a set S consisting of n integers, we can build in deterministic 

O(n(log log n)2) time a data structure of size O(n) that allows checking if x ∈ S and accessing its associated information in O(1) time. 

# 3 Framework and Overview 

Let us start by establishing notation and terminology to be used for the rest of this section. We are given as input an LZ-end factorization Z = P1, P 2, . . . , P z of string S with length n. Each phrase is 4given with its endpoints ai and bi such that Pi = S[ai..b i]. Additionally, each phrase Pi with length at least 2 is given with the index j < i such that Pj is the source of Pi. For every index i ∈ [1 ..n ] in the text, we denote as ri and ℓi the distance to the closest phrase boundary to the right of i and to the left of i, respectively. Formally, if the phrase containing index i is P = S[a..b ], then ri = b − i

and ℓi = i − a. We call ri and ℓi the r-value and the ℓ-value of i, respectively. 

3.1 Intuition and High-level Approach 

Let us first describe the approach of Kempa and Saha [KS22]. We are storing an array B with 

B[i] = S[bi] for every i ∈ [z]. For an index i ∈ [n] with ri = 0 , we have that i = bj for some 

j ∈ [z] and we can output S[i] = B[j]. Our goal is to reduce an arbitrary index i ∈ [n] given at query time to an index i′ ∈ [n] with ri′ = 0 and S[i] = S[i′]. Kempa and Saha [KS22] achieve this goal by defining a ’step’ rule N (i) for every index i ∈ [n]. This step rule maps i with ri > 0 to another index N (i) < i such that S[i] = S[N (i)] . The rule defined by Saha and Kempa is both 

compactly computable and terminates quickly . By compactly computable, we mean that there is a data structure with size O(z) that allows computing N (i) in polylogarithmic time. By terminates quickly, we mean that for every i ∈ [n], repeatedly applying N (i), N 2(i), . . . will result in some index 

i′ with ri′ = 0 after a polylogarithmic number of steps. It should be clear that given such a step rule, one can support random access in O(z) space and polylogarithmic time. We provide a high level description of the step rule of [KS22]. The step function N is a com-position of three different functions: J, sJ , and M ("Jump" , "Stable Jump" , and "Marked" ). The function J is defined for every index i ∈ [n] with ri > 0, while sJ and M are only defined for a strict subset of those indices. For an index i with ri > 0, we will prioritize using M (i) as the step from i, i,e, we have N (i) = M (i) if M (i) is defined. Otherwise, we set N (i) = sJ (i), and only if both M (i) and sJ (i) are undefined, we have N (i) = J(i).Consider a sequence i, N (i), N 2(i), . . . of N steps. Each of J, sJ , and M have a particular role in guaranteeing that this sequence quickly converges to an index i′ with ri′ = 0 . This goal is achieved in O(log n) step sequences called epochs . An epoch is a subsequence of N steps such that at the end of the epoch, the r-value is in a smaller exponential level than the one seen at the start of the epoch. Since initially ri ≤ n, after applying O(log n) epochs we reach an index with r-value 0 as required. Let us formally define the goal of the epoch. We fix the set Kn = {1.5k | k ∈ [0 .. log 1.5 n]}

of integer powers of 1.5 smaller than n. An epoch starts at an index i that has ri ∈ [k.. 1.5k) for some k ∈ Kn, and ends upon reaching an index j with S[i] = S[j] and rj < k .Let us describe the role of each function in an epoch. First of all, all the functions share the property of being r-non increasing. That is, we will have rF (i) ≤ ri for every F ∈ { J, sJ, M } for which F (i) is defined. The function J will act as a naive ’searching’ step. It transforms i into an index to the left of 

i, without increasing the r-value and while maintaining S[i] = S[J(i)] . We are not guaranteed to have any progress towards lowering the value of r when using J, but we ’hope’ that by repeatedly applying J, we will eventually reach an index for which M is defined. We call the part of the epoch before M is activated for the first time the naive jumping part. The function M has the role of a trigger function and a progress function, and it has a symbiotic relationship with sJ . By trigger, we mean that after M (i) applies, the naive jumping part of the epoch ends, and it enters a different state called the stable part . The epoch remains in the stable part until the end of the epoch. During the stable part, all N steps are guaranteed to be either M

or sJ until we will eventually reach some index i′ with ri′ < 23 ri, finishing the epoch. When we say that M is a progress function, we mean that M (i) is guaranteed to have a signifi-cantly reduced ℓ-value or a significantly reduced r-value. Namely, it holds that min( rM (i), ℓ M (i)) ≤

5min( ri/2, ℓ i/2) . Following this notion, we call an index i for which M (i) is defined a progress index .The key property of sJ is that it is ℓ-non-decreasing, in the following sense. For every index i

for which j = sJ (i) is defined, either ℓj ≤ ℓi or rj < 23 ri. Notice that in the latter case, the epoch is finished. In words, sJ guarantees that in each step we either do not increase ℓ, or we achieve the end of the epoch. We note that the function M also has this property. We have that throughout the stable part, both r and ℓ values are non-increasing. Furthermore, every time we reach a progress index, we have that r or ℓ are reduced by a constant factor. Since 

ℓi ≤ n, it can only be reduced by a constant factor O(log n) times throughout the epoch. It follows that after visiting O(log n) progress indices, we will finally reach some i′ with ri′ < 23 ri, finishing the epoch. The last important property of M is the property of being common. That is, for every index i,it is guaranteed that withing a polylogarithmic number of N steps, we reach a progress index. Therefore, we are guaranteed to reach a progress index after a polylogarithmic number of steps, triggering the start of the stable part. The epoch ends after we visit O(log n) progress indices, and we reach a new progress index every polylogarithmic number of steps. It follows that the epoch ends within a polylogarithmic number of steps. 

i

SNJ SNJ . . . SNJ bj

O(log n)

Figure 1: The high level structure of the query algorithm of Kempa and Saha. Starting with the input index i, we apply a sequence of O(log n) epochs (depicted as larger rectangles). Every epoch consists of two parts: the naive jumping part (depicted as an ’NJ’ labeled square) and the stable part (’S’ labeled square). Each epoch outputs an index with r-value in a smaller exponential level than that of the input index of the epoch. After the last epoch, we conclude in some right boundary of a phrase bj , as the r-value is 0. In our implementation of this approach, the native jumping part corresponds to Lemma 3.1 and the stable part corresponds to Lemma 3.2. Each is implemented in 

O(log nz ) time, and the number epochs is reduced to O(log nz ) due to Lemma 2.1. 

Implementing J, sJ , and M . The implementation of M follows from a well-known approach from random access on Lempel-Ziv compressed strings. A phrase P = S[a..b ] ∈ Z can be processed such that every index i ∈ [a..b ] is mapped to an index j with S[j] = S[i], and either min( rj , ℓ j ) ≤

min( ri/2, ℓ i/2) (and in either case, rj ≤ ri). However, in order to support an O(1) computation of this mapping, one needs to use O(log n) space. The O(log n) in the space complexity for supporting M arises from a partition of P into 

Θ(log |P |) intervals. For each interval, the data structure stores O(1) space to support comput-ing M for indices in this interval. Kempa and Saha choose one random interval from each phrase, thus supporting the computation of M only with probability 1/ log n for an index in P . By making this compromise, they reduce the space consumption per phrase to O(1) . It should be easy to see that if we design N to always move between phrases (i.e, N (i) is always in a phrase to the left of the phrase containing i), the common property of M is satisfied with high probability (with an appropriate choice of a polylogarithmic function). 

J is the simplest function to implement: for every index i contained in a phrase P = S[a..b ]

with source S[a′..b ′], we define J(i) = i − b + b′. Notice that this maps i to the index aligned with 6b epre (b, e )

Figure 2: A demonstration of the function pre (b, e ). The dotted arrows point from a right-end point of a phrase to the right endpoint of its source phrase. The bottom arrows demonstrate the process of finding another copy of S[b..e ] to the left repeatedly, as long as the leftmost copy found so far is contained in a phrase. 

i in S[b′ − | P | + 1 ..b ′] = P , so we have both S[i] = S[J(i)] and rJ(i) ≤ ri.In order to define the function sJ , Kempa and Saha introduce the pre function. The function 

pre (b, e ) receives two parameters b, e representing an interval of indices within S. The output of the function corresponds to result of the following process: check if b and e are contained in the same phrase of Z. If they are not then return b. Otherwise, replace (b, e ) with (J(b), J (e)) and repeat. Intuitively, one can think of pre (b, e ) as a pointer to an occurrence S[b′..e ′] of S[b..e ] that is achieved by following the source links of Z (potentially many times), and contains a phrase boundary. In order to support efficient computation of sJ (i), the data structure of Kempa and Saha stores pre (b, e ) values for O(z) pairs (b, e ).

The challenges of constructing [KS22]. We wish to provide an efficient, deterministic con-struction based on the ideas of Kempa and Saha. There are two primary obstacles. Firstly, the construction of Kempa and Saha requires computing O(z) values of pre (b, e ). Astraightforward computation of pre (b, e ) can potentially take O(z) time, for a total of O(z2) run-ning time across all required pre (b, e ) values. We overcome this challenge by providing a batched algorithm for computing multiple queries (b, e ) simultaneously, reminiscent of the winding phase of Farach and Thorup’s algorithm for pattern matching in LZ-compressed text [FT98]. This is achieved by processing a batch I = ( b1, e 1), (b2, e 2), . . . of queries via a single right-to-left scan of Z.When a phrase P = S[a..b ] is processed, we can ’shift’ all queries contained within P to the source of P as a single block in polylogarithmic time. Then, we can find every (bi, e i) such that bi and ei

are separated by a in polylogarithmic time per such query. This leads to a total running time of 

O(( z + |I|) polylog( nz )) .The second challenge is deterministically implement M . We take a different approach than the one of Kempa and Saha for reducing the total space complexity for M . While they partially computed the function M for every phrase, we pick a subset ZM ⊆ Z of size O(z/ log nz ), and compute M fully for every phrase in ZM .Intuitively, one would like to pick a set ZM that acts as a cover in the following sense. Consider for every i ∈ [n] the sequence P (i) of polylog n phrases visited by iteratively applying J exactly 

polylog n times (for some fixed polylog n) starting from i. A good choice of ZM corresponds to a set of phrases that is both compact, and covers all P (i) sequences. Since we aim for a preprocessing time of O(z polylog( nz )) , we can not even afford to inspect all P (i) within our preprocessing time. We sidestep this issue by substituting the demand that ZM covers all such sequences with a relaxed notion of covering that is satisfactory for our purpose. First, let us simplify our analysis by assuming that prior to M , we only make naive steps using J (and never use sJ ). Now, for 7every i ∈ [n], we say that ZM light-covers i if there is some x ∈ [0 .. log nz ] such that Jx(i) is either contained in some phrase of ZM or Jx(i) has r-value less than 23 ri. In words, we do not require 

ZM to intersect the first log nz steps following i in the event when we get ’lucky’, and the repeated applications of J happen to incur a significant reduction in r-value without invoking M .With this notion of covering, our task is reduced to covering all sequences i,J(i),J2(i),. . . 

,Jlog( nz )(i) such that the starting and ending r-values are within the same exponential level. As it turns out, sequences with this property have a very strict structure. Namely, we observe that given i in a phrase P with ri ∈ [k.. 1.5k) for some k ∈ Kn, if J(i) has r-value that is also in [k.. 1.5k),there is only one phrase P ′ (independent of i) that may contain J(i). In other words: all ’bad steps’ from phrase P are mapped to the same phrase P ′. This induces a tree structure over the phrases, where the parent of each phrase P is the unique phrase P ′ that may be its successor in a sequence of bad steps. Given such a structure, we can apply the standard approach of selecting a sparse tree layer as a compact path-covering set. 

Improving the query time. While our implementation is inspired by the construction of Kempa and Saha, our approach is different in the following sense. Kempa and Saha defined a ’step’ function 

N , and prove that it has certain properties. Then, the query algorithm iteratively applies the function, and it is guaranteed to converge quickly to a boundary index due to the properties of the function. We use similar function definitions to the ones of Kempa and Saha, but our query algorithm does not iteratively apply these functions. Instead, our goal is to find the critical value x such that 

N x(i) has a sufficiently small r-value, and compute N x(i). By further studying the properties of 

N (and slightly modifying its definition), we are able to design algorithms that find x and N x(i)

without explicitly iterating over x = 0 , 1, 2, . . . .For instance, the component of our algorithm equivalent to the stable part of Kempa and Saha does not use M . We only use M to trigger the stable part, but not to make progress within it. Instead, we simply consider a sequence of applications of sJ throughout the stable part of the epoch. Since we gave up the progress obtained by occasionally reaching M , the sequence of sJ applications before some progress in r is achieved may be arbitrarily large. However, by dropping M we obtain a cleaner structure to the stable part. That structure allows us to find the first x such that sJ x(i)

has a significantly reduced r-value in O(log nz ) time. Finally, since we wish to have O(log nz ) epochs rather than O(log n), we introduce a method for reducing any LZ-end partition with z phrases into a partition with O(z) phrases such that every phrase has length O( nz ) (Lemma 2.1). This allows us to assume that the initial r-value is at most  

> nz

.Formally, we break the task of previn Theorem 1.1 into the following two lemmas. 

Lemma 3.1 (Naive Jumping Part) . Given Z an LZ-end factorization of S, we can construct a data structure supporting the following query. Given an index i ∈ [n] with ri ∈ [k.. 1.5k) for some k ∈ K,compute an index j satisfying rj ≤ ri, S[i] = S[j], and either rj < k or ℓj ≤ 1.5k.The data structure can be constructed in O(z log 2 nz ) time, consumes O(z) space, and supports queries in O(log nz ) time. 

Lemma 3.1 corresponds to applying J repeatedly until the r-value is either reduced significantly, or an index i for which M (i) is defined is met. We choose a set ZM for the definition of M such that this is guaranteed to happen within O(log 2 nz ) applications of J. The O(log nz ) query time is achieved by introducing shortcuts, which allows us to compute Jlog nz in constant time, under some conditions. 8Lemma 3.2 (Stable Part) . Given Z an LZ-end factorization of S, we can construct a data structure supporting the following query. Given an index i ∈ [n] with ri ∈ [k.. 1.5k) for some k ∈ K and 

ℓi ≤ 1.5k, compute an index j satisfying rj < k and S[i] = S[j].The data structure can be constructed in O(z log 2 nz ) time, consumes O(z) space, and supports queries in O(log nz ) time. 

Lemma 3.2 corresponds to receiving the first index of the stable part, and efficiently finding the last index of the epoch. As we discussed before, it is achieved by analyzing the structure of the stable part to obtain a clean characterization of the first index with r-value below the required threshold. By combining Lemma 3.1 and Lemma 3.2, we obtain our main theorem. 

Proof of Theorem 1.1. Given Z an LZ-End parsing of S, we store the array B with Bi = S[bi]

for every i ∈ [z]. We then construct the data structures of both Lemma 3.1 and Lemma 3.2 in 

O(z log 2 nz ) time. Given a query index i with ri ∈ [k.. 1.5k) for some k ∈ K, we apply the data structure of Lemma 3.1 to obtain i′ with S[i′] = S[i] and either ri′ < k or ℓi′ ≤ 1.5k. If the latter occurs, we apply the data structure of Lemma 3.2 with input i′ to obtain j′ with rj′ < k and S[j′] = S[i′] = S[i].In both cases, we spend O(log nz ) time and obtained some index t ∈ { i′, j ′} with S[t] = S[i] and 

rt < k . We call the above routine an epoch. We repeatedly apply epochs, each receiving as input the output of the previous one, until we obtain an index with r-value zero. Since each epoch results in an index i with ri bounded by a smaller element of K, we will reach an index with rj′ = 0 and 

S[j′] = S[i] within O(|K|) = O(log nz ) epochs. In particular, j′ = bj for some phrase Pj . We can find the phrase Pj (and the index j) containing 

j′ in O(log nz ) time (see Lemma 4.7) and then report S[i] = S[j′] = S[bj ] = B[j].The running time of each epoch is O(log nz ), and there are O(log nz ) epochs, so the total running time of a query is O(log 2 nz ) time. The space used by our data structure consists of sorting an array of length z storing all S[bi],and of storing the data structures of Lemma 3.1 and of Lemma 3.2, which require O(z) space each. The total space complexity is O(z), as required. The rest of the paper is dedicated to proving Lemma 3.1 and Lemma 3.2. We prove Lemma 3.1 in Section 4 and Lemma 3.2 in Section 5 In Section B, we prove Lemma 2.1 , which allows us to assume without loss of generality that there are no long phrases in our input factorization Z.In Sections A, 6 and 7 we separately provide proofs for various auxiliary data structures used throughout the paper. Finally, in Section C we show how to modify and enhance our techniques to provide a compressed data structure for substring extraction. 

# 4 Naive Jumping 

In this section, we prove Lemma 3.1. We start by presenting a high-level description of the algorithm and of the concepts used by the algorithm, and then proceed to present the full version with all the required definitions and proofs. 

4.1 High-Level Overview 

The objective of this overview is to equip the reader with a conceptual understanding of the machin-ery behind Lemma 3.1. The framework developed in this section provides an intuitive abstraction of our method, meaning that the formal definitions and lemmas stated here will not be explicitly 9invoked in the full description of the data structure. Finally, we express all running times using 

log n instead of log nz to ensure a smoother presentation. For every phrase P = S[a..b ] with source P ′ = S[a′..b ′], denote δP = b − b′. Clearly, for every index i ∈ [a..b ], the index j = i − δP satisfies S[i] = S[j] and rj ≤ ri. We denote by J the function that maps any i in a phrase P to J(i) = i − δP .Recall that our goal is to efficiently support the following query. Given some index i with 

ri ∈ [k.. 1.5k), return j with rj ≤ ri, S[j] = S[i], and either rj < k or ℓj < 1.5k.Consider the following simple algorithm for achieving this goal: Given an input index i, let P be the phrase containing i in Z. Notice that |P |̸ = 1 , as |P | = 1 would imply ri = 0 , which contradicts 

ri ≥ k. Therefore, |P | > 1 and P has a source. Consider j = J(i). If rj < k , return j. Otherwise, repeat this process recursively on j.This algorithm clearly terminates, as every recursive call is for an index j strictly smaller than i.However, it may be very inefficient. Our key insight is that the pathological case where this simple algorithm fails is highly structured. This structure can be described as follows. 

Lemma 4.1. For every k ∈ K there is a forest Tk with node set Z such that every index i ∈ [1 ..n ]

contained in phrase P with ri ∈ [k.. 1.5k) and j = J(i), one of the following holds. 1. rj < k .2. j is in the parent of P in Tk.The tree Tk can be computed in O(z log n) time given Z.

In words, a sequence of applications of J where the r-value remains large corresponds to an upwards path in Tk (see Fig. 5). To take advantage of the tree structure of the bad steps, we use the concept of a good mapping. 

Definition 4.2. A good mapping M for a phrase P = S[a..b ] is a data structure that given an index i ∈ [a..b ] returns in constant time an index j = M (i) with rj ≤ ri and S[j] = S[i]. Further, 

min( rj , ℓ j ) ≤ min( ri/2, ℓ i/2) .

Notice that if ri < 1.5k, a good mapping M maps i to j = M (i) with min( rj , r i) < k . Therefore, if i is in a phrase for which we have a good mapping, we can immediately return a valid output. We prove that given a set M ⊆ Z of phrases, it is possible to construct good mappings for every phrase in M with the following complexities. 

Lemma 4.3. Given Z and a set M ⊆ Z of phrases, we can compute a good mapping for each phrase in M in O(z log 2 n) time. The total space consumed by the good mappings is O(z + |M | log n).

Notice that we cannot afford to apply Lemma 4.3 with M = Z, as this would result in a data structure with O(z log n) space. Instead, we apply Lemma 4.3 with a sparse set that covers all paths of length log 2 n in Tk. Such a set can be obtained by partitioning the vertices of Tk into log 2 n sets based on their distance from the root modulo log 2 n. From the pigeonhole principle, we have that one of these sets has size at most z/ log 2 n - we pick Mk to be this set. Notice that Mk intersects any upwards path of length log 2 n in Tk.The total size of Mk sets over all values of k is O(z/ log 2 n · | K|) = O(z/ log n). Therefore, by applying Lemma 4.3 to the union of all Mk’s, we obtain a good mapping for every phrase within some Mk in total space O(z + z/ log n · log n) = O(z).We are almost ready to present an O(z) space data structure with a slightly slower O(log 2 n)

running time for Lemma 3.1. To this end, we only require the following two technical subroutines. 10 First, we need a data structure for efficiently finding the phrase P containing an index i. This can be trivially obtained in O(log n) time via binary search. Second, we require a data structure that given a phrase P and k ∈ K, finds the parent of P in Tk in O(1) time. Notice that we cannot afford to store all trees Tk within our O(z) space limitation. We show in Lemma 7.7 that such a data structure can be constructed in O(z) space and O(z log n) construction time. Now, given an index i, we first find the phrase P containing i in O(log n) time. If P ∈ Mk, we simply return M (i). Otherwise, we compute j = J(i) and P ′ the k-bad parent of P . If j is not contained in P ′, we are guaranteed to have rj < k . We can therefore return j. If j is contained in 

P ′, we check if rj < k and if it is we return j. If rj ≥ k, we recurse on j. Because j is guaranteed to be in P ′, we additionally send P ′ to the recursive call. Thus, successive recursive calls do not need to dedicate O(log n) time to compute the phrase containing j.Every recursive call except for the first one is implemented in O(1) time. From the path covering property of Mk, we have that if more than log 2 n calls are made, it is guaranteed to reach an index 

i contained in a phrase of Mk. The running time is therefore O(log 2 n).In order to obtain further speedup, we apply a shortcut strategy. To this end, we analyze a more general notion of ’lack-of-progress’ in the simple algorithm. In our former analysis, we focused on the case where a single application of J leads to an index with a large r-value. Now, we wish to analyze the case where x consecutive applications of J still lead to a large r-value. We present the notion of an x-bad index. Consider a phrase P and the sequence 

P = P (0) , P (1) , P (2) , . . . , P (x)

of ancestors of P in Tk (with P (x) denoting the x-th parent of P in Tk). We say that an index i in 

P is x-bad if for every y ∈ [0 ..x ], it holds that Jy(i) is in P (y). It can be shown by induction that the set of x-bad indices within P forms a consecutive interval in P . We call this the x-bad interval of P .Furthermore, we can use the fact that the sequence of visited phrases is shared by all x-bad indices in P to efficiently compute J(x)(i) for an x-bad index i. Namely, it holds that for an x-bad index i, we have Jx(i) = i − ∆P where ∆P = δP + δP (1) + . . . + δP (x−1) .It follows from the above discussion that given the x-bad interval of P and ∆P , we can decide for an index i in P if it is x-bad, and if it is, find Jx(i), all in constant time. Fix x to be log n from now on. Since the total size of all Tk is O(z log n), we cannot afford to store this information for every node in every Tk. Instead, as in the previous algorithm, we chose a sparse set M ′ 

> k

of size O(z/ log n)

for each Tk. We only store this shortcut information for the vertices in M ′

> k

. We also store for each phrase in M ′ 

> k

its nearest ancestor in Tk that is marked (i.e, in Mk). We are ready to describe our faster query (see Fig. 7). First, apply at most log n steps of J.If at any point we reach an index j with rj < k or in Mk, we halt and return as in the previous algorithm. Otherwise, after log n steps the algorithm must reach some phrase in M ′

> k

. At this point, we start using shortcuts instead. In every shortcut step, we check if the current i is (log n)-bad using the (log n)-bad interval. Additionally, we check the distance d from the phrase P containing i to its nearest ancestor in Tk that is also in Mk.If i is not (log n)-bad, or if d < log n, apply J at most log n more times instead of using a shortcut – we claim that the algorithm terminates with a valid output within those log n steps. If none of these steps reach an index j with rj < k , it must hold that i is (log n)-bad. Therefore, this case must have been trigger due to d < log n. It follows that one of the log n phrases visited after i

is in Mk, and we can use the good mapping associated with it to return a valid output. 11 Otherwise, we have that i is x-bad, and we use ∆P to obtain Jlog n(i) in constant time. Notice that since M ′ 

> k

is selected as a set of all phrases with the same depth modulo log n in Tk, it is guaranteed that Jlog n is in a phrase of M ′

> k

.Let us analyze the running time. We start using shortcuts after applying J at most log n times. Then, every shortcut reduces our distance from a phrase in Mk by log n. This distance is initially at most log 2 n. Therefore, we will use a shortcut at most O(log n) times. Once we stop using shortcut for any reason, we are guaranteed to terminate with a valid output within O(log n) applications of 

J. The total running time is therefore O(log n), as required. In order to implement the above in O(log n) time, we also require a mechanism for testing in constant time if a phrase P is in M ′

> k

. This can be obtained by storing a machine word MP for every phrase in Z, where the i’th bit indicates whether or not P is in M ′ 

> k

for k = 1 .5i.

4.2 Data Structure for Naive Jumping 

To prove Lemma 3.1, we study the process of following the source phrase of the phrase P containing 

i, ’jumping’ to the index aligned with i within the occurrence of P ending with the source. To formalize this, we define a jumping rule J(i). Our ultimate goal is to find an index j with rj < k 

and S[i] = S[j]. Intuitively, we make progress towards this goal when we jump to an index with a lower r-value, and lose progress if we jump to an index with a higher r-value. This motivates us to define jumping rules that do not lose progress, in the following sense. 

Definition 4.4 (Legal Jumping Rule) . For an index i ∈ [1 ..n ], we say that j ≤ i is a legal jump for i if S[i] = S[j] and rj ≤ ri.

For a phrase Pi = S[ai..b i] with source Pj = S[aj ..b j ], we denote δPi = bi − bj . We define the jumping rule J based on δPi .

Definition 4.5 (Jumping Rule J). For an index i contained in the phrase P , we define J(i) = i−δP .If P does not have a source, J(i) is undefined. 

The following lemma follows directly from the definition of LZ-end. 

Lemma 4.6. J is a legal jumping rule. Proof. Let i be an index contained in reference phrase P = S[a..b ] with source P ′ = S[a′..b ′]. Since 

S[a..b ] = S(b′ − | P |..b ′], we have that S[i] = S[b − ri] = S[b′ − ri] = S[J(i)] . Also, since b′ is a phrase boundary, we have rJ(i) ≤ b′ − J(i) = ( b − δP ) − (i − δP ) = b − i = ri, as required. Since J(i) is defined based on the phrase containing i, our algorithms are often required to find the phrase containing an index i. This can be supported in O(log n) time and O(z) space by storing 

{ai | i ∈ [z]} in a balanced search tree. We would like to support this query slightly faster. To this end, we provide the following data structure. 

Lemma 4.7. Z can be processed in O(z log nz ) time to construct a data structure that given i returns 

j such that i ∈ [aj ..b j ] in O(log nz ) time. The data structure uses O(z) space. Proof. We partition the universe [1 ..n ] into z uniform pieces U0, U 1, . . . U z−1 such that Ux =(x nz .. (x + 1) nz ]. We scan the elements of AZ = {ai | i ∈ [z]} and partition them in O(z) time to obtain Ax = Ux ∩ AZ for every x ∈ [0 ..z − 1] . We construct a balanced search tree over each of Ax in O(|Ax| log |Ax|) = O(|Ax| log nz ) time and O(|Ax|) space. The total construction time is 

O(z log nz ) and the total space is O(z), as required. We also store, for every x ∈ [0 ..z − 1] , the 12 maximal index a∗ 

> x

in AZ ∩ [1 ..x nz ] and the corresponding phrase P ∗ 

> x

starting in a∗

> x

. These values can be computed straightforwardly in O(z) time. Given a query index i, we first find in O(1) time the value x such that i ∈ Ux. It can be easily verified that aj such that Pj contains i is either in Ux or is a∗

> x

. We use the balanced search tree of 

Ux to find the predecessor of i in Ux and check which of the two candidates contains i. The query time is O(log nz ), and then verify which one of the candidates contains i in O(1) time. Given an index i ∈ [n], we can use Lemma 4.7 to find the phrase P containing i in O(log nz ) time. Since every phrase Pi is stored alongside the index j indicating its source phrase Pj , we can compute 

δPi in O(1) time given Pi, by accessing the array storing the phrases alongside their reference data. We proceed to define the function pre , which is used to define the second jumping rule used in the naive jumping part. 

Definition 4.8 (The function pre , see Fig. 2) . For a string S and two integers b ≤ e ∈ [1 ..n ] we define pre (b, e ) as follows. If b and e are contained in the same phrase P ∈ Z , and P has a source, then pre (b, e ) = pre (J(b), J (e)) . Otherwise, pre (b, e ) = b.Additionally, we define δ-pre (b, e ) = b − pre (b, e )

In words, pre (b, e ) is obtained by finding previous occurrences of S[b..e ] by following the source links of Z, until it is no longer possible. Halting this process can occur either because b and e are separated by a phrase boundary, or because the phrase P containing [b..e ] has no source (which can only occur if P = S[b] = S[e]). The left endpoint of the occurrence on which we stop in this process is pre (b, e ). Since pre (b, e ) is obtained by applying J repeatedly, the following is a consequence of Lemma 4.6. 

Observation 4.9. Let [b..e ] ⊆ [1 ..n ] be an interval. For every i ∈ [b..e ], it holds that i − δ-pre (b, e )

is a legal jump for i.

We will define a jumping rule M that relies on δ-pre . Unlike δP , that is essentially given alongside 

P in Z, computing δ-pre (b, e ) from Z require some non-trivial work. A naive approach would be to check if [b..e ] contains a phrase boundary, and if it does not - proceed according to the recursive definition. This may result in O(z) time for computing a single pre (b, e ) value. In Section 6, we present the following algorithm, which will be instrumental for efficiently computing δ-pre -dependent jumping rules. 

Lemma 4.10. Given Z an LZ-End factorization of a string S and a set of pairs I ⊆ [1 ..n ]2 such that 

e ∈ [b..b + nz ] for every (b, e ) ∈ I, we can compute pre (b, e ) for every [b..e ] ∈ I in O(( z+|I|) log 2(n/z )) 

time. Here, z = |Z| and n = |S|.

In order to define M , we need to introduce the halved canonical partition of an interval. This partition appears under a variety of names in compressed random access data structures (in partic-ular in [KS22]). 

Definition 4.11 (Canonical Partition, Halved Canonical Partition) . For an interval [a..b ] of inte-gers, we define the canonical partition of [a..b ] as the following intervals. 1. For every i ∈ [0 .. log( b−a 

> 2

)] , the interval [a + 2 i − 1.. min( a + 2 i+1 − 2, b−a 

> 2

)] 

2. For every i ∈ [0 .. log( b−a 

> 2

)] , the interval [max( b − 2i+1 + 2 , b−a 

> 2

)..b − 2i + 1] 

We define the Halved Canonical Partition based on the Canonical Partition. The halved canonical partition of [a..b ] is obtained by taking the canonical partition and splitting every interval larger than 1 into two equal length interval. Formally, for every interval I = [ x..y ] in the canonical partition of 

[a..b ] such that |I| > 1, the halved canonical partition of [a..b ] contains [x..x +|I|/2) and [x+|I|/2..y ].

13 a b

Figure 3: A demonstration of a canonical partition. The key property of the canonical partition is that for an index i ∈ [a..b ], the interval I that contains i in the canonical partition has length at most m = min( ri, ℓ i) + 1 . It immediately follows that in the halved canonical partition, the length of the interval containing i is bounded by 

max( m/ 2, 1) . We are now ready to define the jumping rule M .

Definition 4.12 (Jumping Rule M ). Let i be an index contained in a marked phrase P = S[a..b ].Let [x..y ] be the interval in the halved canonical partition of [a..b ] that contains i. We define M (i) = 

i − δ-pre (x, y ).

It follows from observation 4.9 that M is a legal jumping rules. 

Corollary 4.13. M is a legal jumping rule. 

Notice that M (i) = i−δ-pre (b, e ) transforms i ∈ [b..e ] into an interval of size |[b..e ]| that contains a phrase boundary. Therefore M (i) has a phrase boundary that is quite close to it either from the left or from the right, inducing a significant decrease either of r-value or of ℓ-value compared to i.Formally, we prove the following useful property of M .

Claim 4.14. For an index i that is contained in a phrase P ∈ M , denote j = M (i). It holds that 

rj ≤ m or ℓj ≤ m where m = min( ri/2, ℓ i/2) .Proof. Let [x..y ] be the interval in the halved canonical partition of P containing i. Notice that 

|[x..y ]| ≤ max( m, 1) . If |[x..y ]| = 1 it must be the case that S[j] is a phrase of length 1. Therefore 

rj = ℓj = 0 ≤ m, as required. Otherwise, we have |[x..y ]| ≤ m. Let x′ = pre (x, y ) and y′ = x′ + |[x..y ]|. The index j = M (i) is in [x′..y ′], and from the definition of pre , there is some phrase boundary b′ ∈ [x′..y ′]. If j ≤ b′, then 

rj ≤ b′ − j ≤ | [x′..y ′]| = |[x..y ]| ≤ m. Otherwise, j > b ′ and we have ℓj ≤ j − b′ ≤ | [x′..y ′]| ≤ m.claim 4.14 implies that if our sequence of applications of J reaches upon some index j in a marked phrase, then M (j) is a valid output for Lemma 3.1. Our first goal will be to define a ’good’ set M , such that every sufficiently long sequence of J applications eventually reaches M (Roughly, see Lemma 4.16 for the precise definition). In order to exploit this property, our data structure has to support efficient computation of M (i) for an index i in a marked phrase. We present the following data structure for efficient M computation. 

Lemma 4.15. Given Z, we can construct a data structure of size O(|M | log nz ) that given i ∈ [n],and the phrase P containing i, computes M (i) in O(1) time. The data structure can be constructed in O(( M log nz + z) log 2 nz ) time. Proof. We apply Lemma 4.10 to compute for every P = S[a..b ] ∈ M and every I = [ x..y ] in the halved canonical partition of P the value PI = pre (x, y ) in O(( |M | log nz + z) log 2 n) total time (Due to Lemma 2.1, the halved canonical partition of every P ∈ Z has O(log nz ) intervals). Notice every query interval is contained in some phrase Pi, and |Pi| ≤ nz according to Lemma 2.1. Therefore, every (b, e ) query has e ∈ [b..b + nz ], and this is a legal input for Lemma 4.10. 14 We then compute for each interval I = [ x..y ] in the halved canonical partition of P the value 

δ-pre (I) = x − δ-pre (x, y ), and store those values in an array AP specifically constructed for P . We store in each phrase P a link to its corresponding array AP .Given an index i and its containing phrase P , we can retrieve the interval I containing i in the halved canonical partition of P in O(1) time using O(1) bitwise operations on a, b, i (We need the most significant bit set to 1 in i − a if i is in the first half of P , or the most significant bit set to 1

in b − i otherwise). We can then use AP to find δ-pre (I) and return M (i) = i − pre (I). If P is not linked to an array AP , we reach upon the conclusion that P / ∈ M , and therefore M (i) is undefined. The construction time is dominated by O(( |M | log nz + z) log 2 n), the query time is O(1) for applying a predecessor query, and the size is O(M log nz ) for storing all δ-pre values. 

4.3 Finding a Good Set of Marked Phrases 

Our first goal is to find a good set M of marked phrases. Before formally defining a good set (see Lemma 4.16), let us provide some intuition. We wish to find a set M that is both sparse and 

covering . We require M to be small, since we will support a query for M (i) using Lemma 4.15, which consumes O(z + |M | log nz ) space. Since we wish to have O(z) space complexity, we need to have |M | ∈ O(z/ log nz ).As for the covering property, we would like to guarantee that every long enough sequence of steps i, J (i), J 2(i), . . . eventually reaches some index contained in a phrase of M . Instead, we will obtain M that satisfies a weaker, yet sufficient, notion of covering. Namely, we will guarantee that every long enough sequence of steps either reaches a phrase of M , or reaches an index with small enough r-value. Formally, we prove the following. 

Lemma 4.16. There is an algorithm that given Z, outputs a set M of size O(z/ log nz ) such that for every index i with ri ∈ [k.. 1.5k) for some k ∈ K, there is x ∈ [1 .. log 2 nz ] such that either j = Jx(i)

is contained in a phrase of M , or rj < k .The algorithm works in O(z log nz ) time. 

We introduce the concept of a bad jump, that corresponds to a step J(i) that remains in the same exponential level. 

Definition 4.17 (Bad Jump, Good Jump) . Let i be an index and let k ∈ K be the unique integer power of 1.5 such that ri ∈ [k.. 1.5k). We say that i is a k-bad jump if j = J(i) has rj ≥ k. If i is not a k-bad jump, we say i is a k-good jump. 

When k is clear from context or irrelevant, we omit it from the notion of k-bad jumps and 

k-good jumps, using bad jump and good jumps instead. The existence of bad jumps is the main obstacle in finding a value j such that rj < k via iterative application of J. The pathological case would be where the sequence i, J (i), J 2(i), . . . , J x(i) consists only of bad jumps for high values of x. However, we show that bad jumps are very structured. Namely, we show that the phrase P ′ that contains J(i) for some k-bad jump i can be decided only based on the phrase P containing i and k. In other words, all k-bad jumps within a phrase P share some uniform behavior. We exploit this structure to efficiently cover every long sequence of bad jumps. We make the following claim regarding bad jumps. 

Lemma 4.18. Let k ∈ K. Let P be a phrase and let i and i′ be two k-bad jumps in P with 

ri, r ′ 

> i

∈ [k.. 1.5k). Then, J(i) and J(i′) are in the same phrase. 

15 Proof. For an illustration, see Fig. 4. Assume i < i ′. Denote j = J(i) and j′ = J(i′). Since both ri

and ri′ are in [k.. 1.5k), and i and i′ are in the same phrase, we have i′ − i < 0.5k. Since j = i − δP

and j′ = i′ − δP , we have j′ − j = i′ − i < 0.5k. Now, assume to the contrary that j and j′ are in different phrases. This would mean that there is a phrase boundary between j and j′, which implies 

rj ≤ j′ − j < 0.5k < k , contradicting the assumption that i is a bad jump.    

> ii′
> jj′
> ri
> ri′
> <0.5k
> rj

P

source (P )

Figure 4: The proof of Lemma 4.18. The top rectangle represents P , while the bottom rectangle represents the source of P (In the text, the source appears to the left of P . In this figure it is displayed below P ). The dashed line between j and j′ represents a phrase boundary separating them. It can be seen that the existence of such phrase boundary leads to rj < k .Following Lemma 4.18, we introduce the terminology of a k-bad parent. For a phrase P and 

k ∈ K, we call ’the k-bad parent’ of P the unique phrase P ′ such that every bad jump i in P with 

ri ∈ [k.. 1.5k) has J(i) ∈ P ′ (uniqueness follows from Lemma 4.18). If there is no such bad jump in 

P , then P does not have a k-bad parent. Notice that P ′̸ = P , since J(i) is necessarily in a phrase strictly to the left of the phrase containing i. As implied by the parent terminology, the k-bad parent relation induces a forest structure over the phrases of Z. We denote as Tk the forest with vertices being the phrases of Z, and the parent of every P is the k-bad parent of P .In Section 7, we prove the following. 

Lemma 4.19. Given Z, there is a data structure supporting the following query: Given P ∈ Z and 

k ∈ K, return the k-bad parent of P , or report that P does not have a k-bad parent. The construction time of the data structure is O(z log 2 nz ), the space is O(z) and the query time is O(1) .

We are now ready to prove Lemma 4.16. 

Proof of Lemma 4.16. We start by defining the set M , and then show how to efficiently compute 

M .

Existence of proper M . For every k′ ∈ K, we define a set Mk′ of phrases according to the structure of Tk′ . If the depth of Tk′ is less than log 2 nz , we set Mk′ = ∅. Otherwise, let c ∈

[0 .. log 2 nz − 1] be an integer such that the number of phrases in Tk′ with depth that has remainder 

c modulo log 2 nz is at most z/ log 2 nz . This value of c exists due to the pigeonhole principle. We set 

Mk′ to be the phrases with depth c modulo log 2 nz in Tk′ . We claim that M = ∪k′∈KMk′ satisfies the condition of the lemma. Clearly, we have |M | ∈ O(z/ log nz ). Consider some i ∈ [1 ..n ] with ri ∈ [k.. 1.5k). Consider 

j = Jx(i) for some integer x. If j is a bad jump in phrase P with ri ∈ [k.. 1.5k), than J(j) is in the parent of P in Tk. It follows that a sequence of phrases containing the bad jumps corresponds to an upwards path in Tk, and therefore if all of the first log 2 nz steps following i are bad jumps, one of these must be in a phrase P ∈ Mk.16 Assume that Jx(i) is not in a marked phrase for every x ∈ [0 .. log 2 nz − 1] . It follows from the above that there is an index x′ ∈ [0 .. log 2 nz − 1] such that i′ = Jx′

(i) is a good jump. Let j′ = J(i′).From the definition of a good jump, we have that rj′ < k .We have shown that if there is no x ∈ [0 .. log 2 nz − 1] such that Jx(i) is not in a phrase of M ,then there is x ∈ [0 .. log 2 nz ] such that the r-value of Jx(i) is below k, which concludes the proof. 

Efficient construction. For every k′ ∈ K, the algorithm construct Tk′ . This is done by first constructing the data structure of Lemma 7.7 in O(z log 2 nz ) time, and then querying every P ∈ Z 

for its k′ parent in O(1) time. Once Tk′ is constructed, we can straightforwardly count the number of vertices in each depth of Tk′ and select the correct value of c to use for Mk′ (or decide that 

Mk′ = ∅). Since we construct each Tk′ in O(z) time, and then apply O(|Tk′ |) = O(z) additional processing to find Mk′ , the total running time for constructing and processing all Tk′ is O(z|K|) = O(z log nz ).This is dominated by the O(z log 2 nz ) time for constructing the data structure of Lemma 7.7. 

> i1
> i2
> i3

P5

P4

P3

k 1.5k

J(i1)

J(i2)  

> (a) A sequence of Jsteps. In each step, the containing rectangle represents the phrase containing the reached index.

P1

P2 P3

P4

P5

Bad path  

> (b) The tree Tk

Figure 5: A demonstration of the relationship between the tree Tk and a sequence of J steps where the r-value remains in [k.. 1.5k). The phrases visited throughout the sequence correspond to an upwards path in Tk.

Reaching a marked phrase fast. So far, we have guaranteed that if we repeatedly apply J

starting from some index i, we will either reach a marked phrase or reduce the r-value to the next exponential level within O(log 2 nz ) jumps. Now, we introduce ’shortcuts’, allowing us to find Jlog nz (i) of an index i in constant time rather than by computing J iteratively step-by-step. The shortcut mechanism can only be applied if the next log nz steps are all ’bad’. This will allow us to retrieve a valid output for the naive jumping part in O(log nz ) time, as if one of the next log nz steps are not bad - we can afford to make these steps one-by-one as we reach a good enough index after reaching a good step. For a phrase P , k ∈ K and a non-negative integer x, we denote as π(x) 

> k

(P ) the x’th ancestor of P in Tk. In particular, π(0)  

> k

(P ) = P for every phrase P and k ∈ K. We also call π(x) 

> k

(P ) the 

(x, k )-bad parent of P .We introduce the notion of (x, k )-bad jumps. 17 Definition 4.20 ((x, k )-bad jumps) . For a phrase P , k ∈ K and non-negative integer x, we say that i is an (x, k )-bad jump of P if for every y ∈ [0 ..x ] it holds that Jy(i) is contained in π(x) 

> k

(P ).

Notice that every index i contained in a phrase P is (0 , k )-bad. For a node P , k ∈ K and non-negative integer x we denote δx(P ) = Pxy=0 δπ(y)  

> k(P)

. In words, δx(P ) is the sum of δ values taken over the first x + 1 ancestors of P in Tk (starting from P itself). We make the following simple observation. 

Observation 4.21. Let i ∈ [n] and a be a non-negative integer. Let Pi0 , P i1 , . . . , P ia be the phrases containing i, J (i), . . . , J a(i), respectively. We have Ja(i) = i − Pa−1 

> x=0

δPix .

The following follows directly from observation 4.21 and from the definition of an (x, k )-bad jump. 

Corollary 4.22. Let P be a phrase, k ∈ K, and non-negative integer x. If i is an (x, k )-bad index of P , then Jx+1 (i) = i − δx(P ).

We show that the (x, k )-bad jumps of P form a consecutive interval within P .

Lemma 4.23. Let P = S[a..b ] be a phrase and let k ∈ K and x be a non-negative integer. The set of (x, k )-bad jumps of P is an interval [a′..b ′]. Furthermore, let P ′ = [ c..d ] be the (x + 1 , k )-bad parent of P . The (x + 1 , k )-bad jumps of P are exactly [a′..b ′] ∩ [c + δx(P )..d + δx(P )] .Proof. For an illustration, see Fig. 6 For x = 0 , the first statement is trivial since the (0 , k )-bad jumps of P = S[a..b ] are simply [a..b ].We inductively prove that the second statement is true for every x ≥ 0. This, in turn, leads to the first statement also being true, as it shows that the (x, k )-bad jumps are obtained by an intersection of intervals. Assume that for some non-negative x, the (x, k )-bad jumps of P are indeed an interval [a′..b ′].Let us prove both directions of the equality between [a′..b ′]∩[c+δx..d +δx] and the set of (x+1 , k )-bad jumps of P .First, let i ∈ [a′..b ′] ∩ [c + δx..d + δx]. Since i ∈ [a′..b ′], it is an (x, k )-bad jump of P . By Corollary 4.22, we have Jx+1 (i) = i − δx(P ). Since we have Jx+1 (i) = i − δx(P ) ∈ [c..d ], it holds that Jx+1 (i) is in the (x, k )-bad parent of P , making i an (x + 1 , k ) bad index of P .For the other direction, consider an (x + 1 , k )-bad index i of P . Since i is, in particular, an 

(x, k )-bad index of P , we have i ∈ [a′..b ′]. Due to the same reasoning as before, since i is (x, k )-bad we have Jx+1 (i) = i − δx(P ). Since i is (x + 1) bad, we know that Jx(i) is in [c..d ], which means that i − δx(P ) = Jx(i) ∈ [c..d ]. This is equivalent to i ∈ [c + δx(P )..d + δx(P )] , which concludes the proof. We call the interval containing exactly the (x, k )-bad jumps of P (implied by Lemma 4.23) the 

(x, k )-bad interval of P . We proceed to show that the (x, k )-bad interval can be computed efficiently. 

Lemma 4.24. Let P be a phrase, let k ∈ K and let x be a non-negative integer. The (x, k )-bad interval of P can be computed in O(x) time, given access to Tk.Proof. We use Lemma 4.23 to compute the (x, k )-interval of a phrase P = S[a..b ] iteratively. Firstly, the (0 , k )-bad interval is trivially [a..b ]. Now, we show how to find the (y, k )-bad interval for every y ∈ [1 ..x ], given the (y − 1) -bad interval [a′..b ′]. According to Lemma 4.23, the (y, k )-bad interval of P is [a′..b ′] ∩ [c′ + δy(P )..d ′ + δy(P )] where π(y+1)  

> k

(P ) = S[c..d ] is the (y + 1) ’th ancestor of P in Tk. We accumulate the δ values of the ancestors of P as we ascend up the tree Tk, so we 18 c d 

> a′b′

−δx(P )

c + δx(P ) d + δx(P )

P

π(x+1)  

> k

(P ) 

> (x+ 1 , k )

Figure 6: A demonstration of Lemma 4.23. An (x + 1 , k )-bad index must be in the (x, k )-bad interval. Furthermore, the process of jumping x + 1 times must map it to the (x + 1 , k )-bad parent of P . This makes x + 1 bad jumps correspond to decreasing the value of the initial index by δx(P ).Therefore, we must have that an (x + 1 , k )-bad index is in [c + δx(P )..d + δx(P )] , where S[c..d ] is the 

(x + 1 , k )-bad parent of P . The interval denotes as (x + 1 , k ) is the (x + 1 , k )-bad interval, obtained by intersecting [a′..b ′] and [c + δx(P )..d + δx(P )] .have access to δy(P ) when computing the (y, k )-bad interval of P . It follows that we can compute the (y, k )-bad interval from the (y − 1, k )-bad interval in O(1) time. In total, the time to obtain the 

(x, k )-bad interval is O(x), as required. We are now ready to prove Lemma 3.1. 

Proof. We start by describing the construction of the data structure. We construct the set M of 

O(z/ log nz ) marked phrases using Lemma 4.16, and then construct the data structure for calculating 

M (i) for an index i in a marked phrase using Lemma 4.15. We store a binary array AM of size 

z representing M with AM [i] = 1 if and only if Pi ∈ M . The data structure is constructed in 

O(( z + M log nz ) log 2 nz ) = O(z log 2 nz ) time and uses O(( z + |M | · log nz )) = O(z) space. We also construct the data structure of Lemma 4.19 for finding k-bad parents in O(1) time. The construction of Lemma 4.19 requires O(z log 2 nz ) time and O(z) space. For every k ∈ K, we construct Tk and do the following. If the depth of Tk is less than log nz ,we do not store any additional information. Otherwise, we pick some c ∈ [0 .. log nz ) such that the number of phrases in Tk of depth that has remainder c modulo log nz is at most z/ log nz . Such 

c should exist due to the pigeonhole principle. Let M ′ 

> k

be the set of vertices in Tk with height c

modulo log nz . For every phrase P ∈ M ′

> k

, we store: 1. The (log nz − 1, k )-bad interval of P

2. δ(log nz −1) (P )

3. The distance from P to the nearest marked ancestor of P

4. The (log nz , k )-bad parent of P .For each phrase P of Z, we attach a machine word mP such that the k’th bit of mP is 1 if and only if P ∈ M ′

> k

. While computing M ′ 

> k

for all k ∈ K, the values of mP can be set without asymptotically affecting the running time. 19 Tk

∈ Mk

∈ M ′

> k

∈ M ′

> k

∈ M ′

> k
> P0
> P1
> P2
> P3
> P4
> P5
> P6
> shortcut
> shortcut

log nz >

log nz

log nz

log nz >

Figure 7: A demonstration of the algorithm in Lemma 3.1. The figure demonstrates the phrases visited throughout the algorithm as seen in the tree Tk. From P0 to P2 the algorithm advances by applying J, which leads the algorithm to the bad parent of the current node at every step (assuming that all steps are bad). Since P2 is in M ′

> k

, it has a pointer to its log nz ancestor in Tk (and auxiliary information that allows check if all next log nz jumps from the current index are bad). Then, we keep applying shortcut jumps until we finally reach a layer that is within log nz from a marked layer (P2, P 3, P 4). When we are finally close enough to a marked layer, we return to jumping one-by-one (P4, P 5, P 6). At every initial point throughout this process, we may terminate prematurely since some jump was good. In this event, the containing phrase is not the parent of the previously visited phrase in Tk, but the good jumps leads to an index with a small r-value. 20 We use O(1) space for each P ∈ M ′ 

> k

so for each value of k we use O(|M ′

> k

|) = O(z/ log nz ) space for a total of O(z) space over all k ∈ K. We also store the word mP for each P ∈ Z which requires additional O(z) space. Finding the correct modulo c takes O(|Tk|) = O(z) time. Computing the (log nz , k )-bad parent and δ(log nz −1) (P ) can be straightforwardly done in O(log nz ) time for every vertex in M ′ 

> k

by traversing 

log nz edges towards the root of Tk and accumulating the δ values of the visited nodes. Computing the distance to the nearest marked ancestor for every P ∈ M ′

> k

, and the nearest strict ancestor in 

M ′ 

> k

could be implemented in O(|Tk|) = O(z) time by a simple iteration over Tk.Computing the (log nz − 1, k )-bad interval is done in O(log nz ) time per node in M ′ 

> k

by applying Lemma 4.24. The total running time per k ∈ K is O(log nz · | M ′

> k

|) = O(z). Over all values of k ∈ K,the total running time is O(z log nz ).

Query. For an illustration of the query algorithm, see Fig. 7. Let i be some index query and let 

k ∈ K such that ri ∈ [k.. 1.5k). By Lemma 4.16, there is some x ∈ [0 .. log 2 nz ] such that Jx(i) is either contained in a marked phrase, or has r-value below k.First, we compute J(i), J 2(i), . . . iteratively until we reach some j in a phrase P such that one of the following is satisfied. 1. rj < k 

2. P ∈ M

3. P ∈ M ′

> k

.Notice that each of the above conditions can be checked in O(1) time given j and P (using the endpoints of P , the array representation of M , and mP , respectively). We find J(i), J 2(i) . . . as follows. For the input index i, we use Lemma 4.7 to find the phrase containing i in O(log nz ) time. Now, we show how given some i′ and the phrase P containing i′, we can find J(i′) and the phrase P ′ containing J(i′) in constant time. To be more precise, we will either find the phrase P ′

containing J(i′), or guarantee that one of our conditions are met for J(i′). Assume we already have 

i′ and P at hand. We find j = J(i′) using δP in constant time, and we also find the bad parent P ′

of P in constant time using Lemma 7.7. If j is in P ′ , we have the phrase containing j at hand. Otherwise, we to have rj < k due to the definition of a bad parent. We can therefore return j and terminate. It follows that we can calculate J(i), J 2(i), . . . J y(i) such that Jy(i) satisfies one of our conditions in O(log nz + y) time. If we reach, at any point, some j with rj < k , the algorithm returns j and terminates. If we reach some j in a marked phrase P ∈ M , the algorithm returns M (j) and terminates. If we end up reaching some i′ ∈ P for some P in M ′

> k

, we stop iteratively applying J and switch to the following strategy instead. We use the stored data of P to check, in constant time, if i′ is in the (log nz − 1, k )-bad interval of P , and if the distance from P ′ to its nearest ancestor in M is less than log nz . We consider several cases depending on results of these two tests. 

Case 1. The distance to the next marked ancestor is more than log nz , and i′ is in the (log nz −1, k )-bad interval of P .In this case, we compute j′ = Jlog nz (i′) in constant time, using δlog nz −1(P ) and applying Corol-lary 4.22. We also have the phrase P ′ containing j′ stored in P as the (log nz , k )-bad parent of P , so we can find it P ′ in O(1) time. Notice that P ′ ∈ M ′ 

> k

since it has depth c modulo log nz . Therefore, the algorithm will keep iteratively using shortcuts as long as we remain in Case 1. 21 Case 2. i′ is not in the (log nz − 1, k )-bad interval of P .In this case, we switch back to applying J as at the start of the algorithm. We keep applying 

J until we reach an index j with rj < k . We will later show that this process is guaranteed to terminate within O(log nz ) steps. 

Case 3. The distance to the next marked ancestor is less than log nz .In this case, we switch back to applying J until we reach an index j contained in a marked phrase P ′ ∈ M or with rj < k . When this occurs, we return j if rj < k or M (j) if j is contained in 

P ′ ∈ M . We will later show that we are guarantee to find such j within O(log nz ) steps. 

Running time. The first part of the algorithm runs in O(log nz + y) time where j = Jy(i) is the first index we meet satisfying one of the specified conditions. Notice that if we jump from i ∈ P to a vertex i′ = J(i) that is not the k-bad parent of P , the algorithm terminates because ri′ < k by the definition of a bad parent. Therefore, the phrases containing i, J (i), J 2(i)... correspond to an upwards path in Tk, as long as the algorithm does not terminate. It follows that we are guaranteed to reach a phrase P that has level c mod (log nz ) in Tk, or terminate for some other reason within 

O(log nz ) steps. We have shown that y ∈ O(log nz ) and therefore the running time of the first part of the algorithm is O(log nz ).If the first part of the algorithm reaches some index j with rj < k or in a phrase P ∈ M , the algorithm terminates in O(1) , returning either j or M (j).Otherwise, the algorithm reaches some index j in a phrase P ∈ M ′

> k

. In this case, the algorithm checks into which of the three cases j falls and applies either Case 1, Case 2, or Case 3. Case 1 is applied in O(1) time. We claim that it can be applied at most log nz times. After log nz

application, we reach some j = Jy(i) for y ≥ log 2 nz ≥ x. Since we did not reach or skip over a marked phrase of P ∈ M , we have that Jx(i) /∈ M and therefore the r-value of Jx(i) is less than k.Since applying J never increases the r-value (Lemma 4.6), we have rj ≤ rJx(i) < k , which means that the algorithm terminates and returns j.It remains to analyze the running times of Case 2 and Case 3. Notice that each of them, by definition, occurs only once - when each of these two cases occur, they define the behavior of the remainder of the algorithm. Case 2 occurs where i′ is not in the (log nz − 1, k )-bad interval of P ′. Therefore, there is some 

y ∈ [0 .. log nz ) such that Jy(i′) is a good jump, which means that we will reach an index satisfying one of the desired conditions with O(log nz ) jumps. Case 3 occurs when the distance to the nearest marked ancestor is less than log nz . Clearly, either there is y ∈ [log nz ] such that Jy(i′) is a good jump, leading to termination as in the previous case, or all log nz jumps following i′ are contained in the ancestors of P containing i′, in the corresponding order. In the latter case, we are guaranteed to reach some j contained in P ∈ M within log nz steps, as required. In conclusion, the first part of the algorithm is executed in O(log nz ) time, Case 1 may be applied up to O(log nz ) times, with each application taking O(1) time, and Case 2 and 3 may take O(log nz )

time. The total running time of the query algorithm is O(log nz ).

Correctness. The algorithm terminates by finding some j = Jy(i) that either has rj < k or that is contained in a marked phrase. In the first case, the algorithm returns j which is clearly a valid output. In the latter case, the algorithm returns M (j) which satisfies either rj < r i/2 < 1.5k/ 2 < k 

or ℓj ≤ ri/2 < 1.5k due to claim 4.14. Therefore, M (j) is a valid output. In both cases, we have 22 S[x] = S[i] and rx ≤ ri (where x is the output index) since x is obtained by iteratively applying legal jumping rules (either J or M ) starting from i.

# 5 The Stable Part 

In this section, we prove Lemma 3.2. 

The jumping rule sJ . We define the jumping rule sJ . For every P = S[a..b ] ∈ Z , we denote 

L(P ) = S[a..a + 23 |P |−1] the prefix of size  23 |P | of P . Let us denote the set L = {L(P ) | P ∈ Z} .We define sJ (i) only for indices i that are contained in some L ∈ L.

Definition 5.1 (Jumping Rule sJ ). For an index i ∈ [1 ..n ] contained in the phrase P [a..b ]. If 

i ∈ [a..a +  23 |P | − 1] , we define sJ (i) = i − δ-pre (a, a +  23 |Pi| − 1) . Otherwise, sJ (i) is undefined. 

Although not explicitly stated as a jumping rule, the logic expressed by sJ is used in [KS22] as well. It follows from observation 4.9 that sJ is a legal jumping rule. On several occasions throughout the section, we are required to prove that sJ (i) is defined for some index i. The following provides a clean characterization of the event in which sJ (i) is defined for an index i.

Observation 5.2. sJ (i) is defined for an index i ∈ [n] if and only if ℓi ≤ 2ri.

As we did for M in Section 4, we use Lemma 4.10 to construct the following data structure for computing sJ .

Lemma 5.3. Given Z, we can construct a data structure of size O(z) that given i ∈ [n] and the phrase P containing i, computes sJ (i) in O(1) time. The data structure can be constructed in 

O(z log 2 nz ) time. Proof. We apply Lemma 4.10 to compute for every P = S[a..b ] ∈ Z the value Pa = pre (a, a + 23 |P | − 1) in O(z log 2 nz ) time. Notice that every pair (b, e ) in the query set satisfies [b..e ] ⊆ [ai..b i]

for some Pi, and we can assume due to Lemma 2.1 that |Pi| ≤ nz . Therefore, we indeed have 

e ∈ [b..b + nz ] for every query pair, so this is a valid input for Lemma 4.10. We then compute for each phrase P the value δ-pre (P ) = δ-pre (a, a +  23 |P | − 1) = a − Pa. We store an array of length z, where the i’th entry stores δ-pre (Pi). Now, given an index i and the phrase 

P containing i, we can simply inspect the array to obtain δ-pre (i) and return sJ (i) = i − δ-pre (i).The construction time is dominated by O(z log 2 nz ), the query time is O(1) , and the size is O(z) for storing all δ-pre (P ) values. We assume that for every phrase P with L(P ) = S[b..e ], we have access to δ-pre (b, e ). This is achieved as a consequence of applying Lemma 4.10 with the query set L.

High-level idea for proving Lemma 3.2 Let us fix some index i0 as the input for the stable part, and denote as k ∈ K the unique integer power of 1.5 such that ri0 ∈ [k.. 1.5k). We are interested in finding the first integer x∗ such that sJ x∗

(i0) has r-value less than k. As in Section 4, we will show that there is a tree structure capturing the bad sequences of applications of sJ . Unlike in Section 4, where we had a separate tree Tk for every possible k ∈ K capturing the bad sequences with r-values roughly k, here we will have a single tree Tbad capturing all bad sequences. This allows us to store Tbad and preprocess it for level-ancestor queries. We provide a charac-terization of the sought value x∗ such that sJ x∗

(i0) has r-values less than k. The characterization 23 allows us to assign a numerical values δℓ(P ) and δr(P ) to every phrase P . Then, when given i0 con-tained in some phrase P0, we can identify x∗ as a level in Tbad where the aggregated sums of δℓ(P )

and δr(P ) on the path from P0 to its ancestor in this level reach some threshold. We compute this sum in logarithmic time using level-ancestor queries. Given x∗, and auxiliary information stored in 

Tbad , we can obtain sJ x∗

(i0) in constant time. 

Existence of x∗ Recall that sJ (i) is not defined for every index in i ∈ [n]. Therefore, it is not immediate that x∗ exists, as it may be the case that when repeatedly applying sJ , we reach an index i′ = sJ x′

(i0) for which sJ (i′) is undefined. In the following lemma, we prove that if this process reaches such an index, we have ri′ < k , which means that x′ = x∗. In [KS22], the following properties are proven, but they are hidden within the proof of another claim (See Lemma 4.4 in [KS22]). We repeat their proof for the sake of self-containment. 

Lemma 5.4 (Stable Part Properties) . Let i be an index with ri ∈ [k.. 1.5k) and ℓi < 1.5k. Let 

j = sJ (i). Either rj < k , or ℓj ≤ ℓi

Proof. For an illustration, see Fig. 8. Since ℓi ≤ 1.5k ≤ 1.5ri < 2ri, we have that sJ (i) is well defined by observation 5.2. Let 

P = S[a..b ] be the phrase containing i. Let pre (a, a +  23 |P | − 1) = a′ and b′ = a′ +  23 |P | − 1. By definition of j = sJ (i), we have that j ∈ [a′..b ′], and b′ −j = a+ 23 |P |−1−i, and a′ −j = a−i. By the definition of pre there is a phrase boundary ˆb ∈ [a′..b ′]. If j < ˆb, we have that rj ≤ ˆb − j ≤ b′ − j,which implies 

rj ≤ b′ − j = a +

 23 |P |



− 1 − i = b − i −

 |P |

3



= ri −

 |P |

3



≤

 23 ri



< 2/3 · 1.5k = k

. And we have rj < k , as required. Otherwise, we have j > ˆb which means that ℓj ≤ j − ˆb ≤ j − a′ = i − a = ℓi, as required. 

> i
> j
> ri

|P |/3

> rj

P

pre      

> (a) If jis to the right of the phrase boundary, its
> r-value is reduced by at least |P|/3, which results in its total value decreasing by a factor of 2/3.
> i
> j
> ℓi
> ℓj

P

pre     

> (b) If jis to the left of the phrase boundary, its
> ℓvalue is clearly smaller than that of i.

Figure 8: Illustration of the proof of Lemma 5.4. The dotted upper rectangle represents L(P ) and the bottom doted rectangle represents the occurrence of L(P ) mapped by pre (a, a + 23 |P |). By definition of pre , the lower rectangle contains a phrase boundary. The following is an immediate corollary of Lemma 5.4. 

Corollary 5.5. Let x be a non-negative integer. If j = sJ x(i0) is defined, either rj < k or sJ (j) is also defined. 

24 Proof. Initially have ℓi0 ≤ 1.5k. Assume that rj ≥ k. Since application of sJ can not decrease the 

r-value (Corollary 4.13), we have that rsJ y (i0) ≥ k for every y ∈ [0 ..x ]. It follows from Lemma 5.4 that if ℓsJ y ≤ ℓsJ y−1 for every y ∈ [1 ..x ] and in particular ℓj ≤ ℓ0 ≤ 1.5k. We therefore have 

ℓj ≤ 1.5k ≤ 1.5rj ≤ 2rj . By observation 5.2, we have that sJ (j) is well defined. With Corollary 5.5, we have shown that x∗ exists. For the rest of the section, we show how to efficiently find x∗ and sJ x∗

(i0).

Tree structure. We override the definition of bad jumps from Section 4 to match our current context. First, we define the bad phrase and the bad suffix of L ∈ L as follows. 

Definition 5.6 (Bad Suffix ,Bad Phrase) . For L = S[a..b ] ∈ L, the bad phrase P ′ of L is the phrase containing sJ (b) = b − δ-pre (a, b ). The bad suffix of L is the suffix [c..b ] containing exactly the indices i such that sJ (i) is in P ′. We lift this terminology to phrases. The bad suffix (resp. bad phrase) of a phrase P is the bad suffix (resp. bad phrase) of L(P ) (notice that the bad suffix of P is typically not a suffix of P ). 

Notice that unlike in the previous section, where we associated each phrase P with possibly 

|K| ∈ O(log nz ) ’bad’ phrases, here we only define one bad phrase for P .In the following, we establish the importance of bad phrases and bad suffixes, linking them to the terminal step that finally leads to an index with sufficiently small r-value. 

Lemma 5.7. Let P = S[a..b ] be a phrase. Let i be an index in L(P ) with ri ∈ [k.. 1.5k) for some 

k ∈ K and i′ = sJ (i). If ri′ ≥ k, then i is in the bad suffix of L.Proof. Denote L = L(P ) = S[a..c ] and notice that c = a +  23 |P | − 1. Notice that ri = ( c −

i) +  13 |P |. Also, since i is in the phrase P , we have ri < |P |. It follows that c − i < 23 ri. Let 

pre (a, c ) = S[a′..c ′]. Clearly, c′ is in the rightmost phrase that intersects [a′..c ′]. We also have that 

sJ (c) = c′ and c − i = c′ − i′ since sJ subtracts δ-pre (a, c ) from both i and c. We will now show that if i′ is not in the same phrase as c′, then ri′ < k . If i′ and c′ are not in the same phrase, there is a boundary between i′ and c′. This would imply that ri′ ≤ | i′ − c′| = |i − c| < 23 ri < k , as required. We define a tree structure over the phrases. Let Tbad be a tree with vertices being the phrases of 

Z. For every phrase P , the parent of P in Tbad is the bad phrase of P . We denote the x’th ancestor of P in Tbad as π(x)

> bad

(P ).Similarly to Section 4, we have that if applying sJ (i) does not result in an r-value below k, the index i is in the bad suffix of P . Therefore, we focus on analyzing the structure of a sequence of 

sJ (i) applications in which every step falls within the bad suffix of the phrase containing it. We make the following simple observation, showing that the difference in ℓ and r values as a result of applying sJ (i) is identical across all i in the bad suffix of a phrase P .

Lemma 5.8. Let i1 and i2 be two indices in the bad suffix of a phrase P . Let j1 = sJ (i1) and 

j2 = sJ (i2). It holds that ℓi1 − ℓj1 = ℓi2 − ℓj2 and ri1 − rj1 = ri2 − rj2 .Proof. Let L = S[x..y ] = L(P ). We have j1 = i1 − δ-pre (x, y ) and j2 = i2 − δ-pre (x, y ). Therefore, 

i1 − i2 = j1 − j2. Denote P = S[a..b ] and let P ′ = S[a′..b ′] be the bad phrase of P . By definition, 

ri1 = b−i1, rj1 = b′ −j1, ri2 = b−i2, and rj2 = b′ −j2. It immediately follows that ri1 −rj1 = ri2 −rj2 .The claim follows for ℓ-values in a similar manner. 25 Following Lemma 5.8, we introduce additional notation to capture the common change in ℓ-values and in r-values among indices in the bad suffix. For a phrase P = S[a..b ], with L(P ) = L =[a..b ′], we define δℓ(P ) = ℓb′ − ℓsJ (b′) and δr(P ) = rb′ − rsJ (b′). Notice that b′, as the last index of L,is in the bad suffix of L.The following is directly implied by Lemma 5.8. 

Corollary 5.9. Let i be an index in the bad suffix of a phrase P and let j = sJ (i). It holds that 

rj = ri − δr(P ) and ℓj = ℓi − δℓ(P ).

We notice that for every P we have that δℓ(P ) and δr(P ) are always non-negative. 

Lemma 5.10. For every phrase P ∈ Z , it holds that δr(P ) ≥ 0 and δℓ(P ) ≥ 0.Proof. Let P = S[a..c ] with L(P ) = S[a..b ]. Let a′ = pre (a, b ) and b′ = a′ +|L(P )|− 1. By definition, we have sJ (b) = b − δ-pre (a, b ) = b′. From the definition of pre , we have some phrase boundary 

ˆb ∈ [a′..b ′]. Therefore, ℓb′ ≤ b′ − ˆb ≤ b′ − a′ = b − a = ℓb. It follows that δℓ(P ) = ℓb − ℓb′ ≥ 0, as required. The fact that δr(P ) ≥ 0 follows from sJ being a legal jumping step (observation 4.9), which implies rb′ ≤ rb.Let us define the set of (x)-bad indices of a phrase. 

Definition 5.11. For an integer x ≥ 0, an index i in phrase P is (x)-bad if for every y ∈ [0 ..x ] it holds that sJ y(i) in the bad suffix of P (y)(i).

We also define, for every phrase P with L(P ) = S[a′..b ′] the value δbad (P ) = δ-pre (a′, b ′). Recall that for an index i in L(P ), it holds that sJ (i) = i − δ-pre (a′, b ′). The following follows directly from the definition of an (x)-bad jump. 

Observation 5.12. If i is a (x)-bad in a phrase P for some non-negative integer x, it holds that 

Jx+1 (i) = i − Pxy=0 δbad (π(y)

> bad

(P )) .

We exploit observation 4.21 to show that we can quickly find sJ x+1 (i), provided that i is (x)-bad. 

Lemma 5.13. Given Tbad , we can construct in O(z) time a data structure of size O(z) supporting the following query. Given an index i ∈ [1 ..n ] , a non-negative x such that i is (x)-bad, the phrase 

P containing i, and P ′ = π(x)

> bad

(P ), report sJ x+1 (i). The query time is O(1) .Proof. We attach to every node of Tbad the number ∆′

> bad

(P ) that is equal to the sum of δbad values on the path from P to the root. Formally, ∆′

> bad

(P ) = Pdy=0 δbad (π(y)

> bad

(P )) where d is the depth of 

P in Tbad . This concludes the construction of the data structure. Given a query i,x, P , and P ′ = π(x)

> bad

(P ), we retrieve ∆bad (P ) and ∆bad (π(x+1)  

> bad

(P )) from 

Tbad . Notice that we can find π(x+1)  

> bad

(P ) in constant time, as the parent of P ′. We compute 

δ = ∆ ′

> bad

(π(x+1)  

> bad

(P )) − ∆′

> bad

(P ) + δbad (P ) = Pxy=0 δbad (π(y)

> bad

(P )) . We output sJ x+1 (i) = i − δ,which is correct by observation 5.12. The preprocessing time consists of finding all ∆′ 

> bad

values, which can be done in O(|Tbad |) via a straightforward iteration on Tbad (given the values δbad (P ) of all phrases). The total construction time is O(|Tbad |) = O(z), as required. The query consists of retrieving data stored in the tree. Assuming that every phrase stores a link to the corresponding tree node, the stored data can be retrieved in constant time, as required. 26 δℓ(P0)

> δℓ(P1)
> δℓ(P2)

i0

i1

i2

ℓi0

P0

P1

P2

P3

> sJ (i0)
> sJ (i1)

Figure 9: The relationship between the sum of δℓ values and membership in the bad suffix. In this figure, the top phrase is the phrase P0 containing i0. Below each phrase, the figure displays its bad phrase. The bad suffix of each phrase is denoted above the phrase as a dotted line. For the sake of clear presentation, the figure displays each bad suffix as a suffix of the phrase (in reality, the bad suffix of P is a suffix of L(P ).) Notice that δℓ(P ) is always equal to the offset between the occurrence of L(P ) mapped by pre and the bad phrase of P . The index i0 is in the bad suffix of 

P0 since ℓi0 ≤ δP0 . It is therefore mapped by sJ (i0) to an index i1 that is in the bad parent of P0,which is P1. Since ℓi0 > δ ℓ(P0) + δℓ(P1), we also have that i1 is in the bad suffix of P1, so it is mapped to i2 in P2. Since ℓi0 is not larger than the sum δℓ(P0) + δℓ(P1) + δℓ(P2), we have that i2

is not in the bad suffix of P2 and is not mapped to P3.27 Characterizing x∗. We will use the δr and δℓ values of the phrases to provide a characterization of the first integer x∗ such that j = sJ x∗

(i0) has rj < k . Before formally providing the characterization (see Lemma 5.14), let us provide some intuition. Consider the process of iteratively applying sJ , starting from i0 until j is reached. Before j

is reached, it should be clear that all visited indices are bad, and it follows from Lemma 5.7 that every visited index is in the bad suffix of the phrase containing it. Therefore, the phrases visited correspond to an upwards path in Tbad . From Corollary 5.9, it follows that the decrease in ℓ and 

r values when we jump from i to j is exactly the δℓ and δr values of the phrase containing i,respectively. In particular, if iD = sJ D+1 (i0) for some integer D, it holds that ℓiD = ℓi0 − PDy=0 δℓ(P (y))

and similarly, riD = ri0 − PDy=0 δr(P (y)). The two sums representing the total decrease in ℓ and r

correspond to the sum δℓ values and δr values of an upwards path from P in Tbad , respectively. We can use level ancestor queries to quickly identify the lowest ancestor of P in which the sum crosses a given threshold. A natural threshold to consider for the sum of δr is k−ri0 , as once this threshold is surpassed, the 

r-value drops below k, which means that we have reached an index with sufficiently small r-value. However, this bound alone is not sufficiently precise. It may be the case that at some point, before the δr values accumulate to k − ri0 , we visit some index j that is not in the bad suffix of the phrase containing it. If that is the case, our claim that the r value decreases according to the δr values no longer holds (as its jumps no longer correspond to an upward path). Fortunately, according to Lemma 5.7, if such j′ is reached, either j′ or its successor sJ (j′) have r-value below k. Furthermore, it turns out that the first such j can be characterized using the accumulated δℓ values. To see this relationship between the δℓ values in the first j that is not in a bad suffix, consider first δℓ(P ) (See Fig. 9). Let L = S[a′..b ′] be the bad suffix of P = S[a..b ]. It should be relatively easy to see that δℓ(P ) = a′ − a. Now, it is clear that i is in the bad suffix of P if and only if 

ℓi = i − a ≥ a′ − a = δℓP . Assume that i is indeed in the bad suffix of P and consider the bad phrase P ′ of P that contains j = sJ (i). We can apply the same reasoning and show that j is in the bad suffix of P ′ if and only if ℓj ≥ δℓ(P ′). But when we reached P ′, we already decreased the ℓ

value of i by δℓ(P ). So, the condition ℓj ≥ δℓ(P ′) translates to ℓi ≥ δℓ(P ) + δℓ(P ′).This pattern persists inductively, and we have that sJ x(i) is in the bad suffix of the phrase containing it ’if and only if’ the sum of δℓ values does not exceed ℓi. Again, ’if and only if’ is not completely precise here, because it may be the case that the r-value drops below k while visiting a bad suffix. Recall that we are only guaranteed to have the value of sJ well defined for the indices we meet in this process as long as their r-value exceeds k (Corollary 5.5). In conclusion, there are two events that may lead to the r-value dropping below k for the first time. One event corresponds to passing a certain threshold of accumulated δr values and the other event corresponds to passing a certain threshold of accumulated δℓ values. We need to specifically find the earlier of the two events, as the characterization of the r-values and the ℓ-values on which we rely persists only as long as none of these two events occur. We formalize the above intuition as follows. For any phrase P ∈ Z and integer B, we define 

∆L(P, B ) = PBx=0 δℓ(π(x)

> bad

(P )) . Similarly, we define ∆R(P, B ) = PBx=0 δr(π(x)

> bad

(P )) .We prove the following fact. 

Lemma 5.14. Let P be the phrase containing i0 with ℓi0 = L and ri0 = R. Let B be a non-negative integer. 1. If i0 is a (B)-bad index, than ∆L(P, B ) ≤ L.2. If ∆L(P, B ) ≤ L and ∆R(P, B − 1) < R − k, then i0 is a (B)-bad index. 

28 Proof. For the sake of clear presentation, we omit P from the notations ∆L(P, B ) and ∆R(P, B ),and write ∆L(B) and ∆R(B) instead, respectively. Let us first prove the first statement. Assume that i0 is B-bad. Let j = sJ B+1 (i0) Since for every x ∈ [0 ..B ] we have that sJ x(i0) is in the bad suffix of π(x)

> bad

(P ), Corollary 5.9 yields that 

ℓj = L − ∆L(B). Since ℓj is never negative, we have ∆L(B) ≤ L as required. We proceed to prove the second claim by induction on B.

Base case. Let P = S[a..b ] and let the bad interval of L(P ) be S[a′..c ]. For B = 0 , we have that 

∆L(0) = δℓ(P ). By definition, ℓa′ = a′ − a. Notice that j′ = sJ (a′) must be the first index of the bad parent of P , so ℓj′ = 0 . By Corollary 5.9, we have δℓ(P ) = a′ − a.Recall that L = i0 − a, and observe that i0 is in the bad suffix of P if and only if a′ − a ≤ i − a

which is equivalent to ∆ℓ ≤ L, as required. 

Induction step. Assume that the claim is true for B ≥ 1. Assume that ∆L(B + 1) ≤ L and 

∆R(B) ≤ R − k. We have that ∆L(B) = ∆ L(B + 1) − δℓ(π(B) 

> B

(P )) and ∆R(B) = ∆ R(B) −

δr(π(B−1)  

> bad

(P )) . Since δℓ and δr value of a phrase are never negative (Lemma 5.10), we have that 

∆L(B) ≤ L and ∆R(B − 1) ≤ R − k. Therefore, from the induction hypothesis we have that i0 is 

B-bad. We will show that j = sJ B+1 (i0) is in the bad suffix of π(B+1)  

> bad

(P ).It follows from Corollary 5.9 that ℓj = L − ∆L(B) and that rj = R − ∆R(B). Since rj =

R − ∆R(B) ≥ k, we have that sJ (j) is well defined (i.e., is in j ∈ L(P )) due to Corollary 5.5. Since 

j is the result of B + 1 jumps from bad suffixes, we have that j is in π(B+1)  

> bad

(P ) = S[a′..b ′]. So 

ℓj = j − a′. Recall that δℓ(π(B+1)  

> bad

(P )) = ˆ a − a′ such that ˆa is the leftmost index in the bad suffix of π(B+1)  

> bad

(P ).We have j = a′ + ℓj = a′ + L − ∆L(B) = a′ + L − ∆L(B + 1) + δℓ(P (B+1) ) = ˆ a + L − ∆L(B + 1) .Since ∆L(B + 1) ≤ L, this implies j ≥ ˆa which implies that j is in the bad suffix of P (B+1) , as required. We proceed to use Lemma 5.14 to characterize x∗. Recall that i0 is the input index with 

ri0 = R ∈ [k.. 1.5k) for k ∈ K We also denote L = ℓi0 . Let P be the phrase containing i0. For an integer D, we denote ∆R(D) = PDx=0 δr(π(x)

> bad

(P )) and ∆L(D) = PDx=0 δr(π(x)

> bad

(P )) . In words, 

∆R(D) (resp. δL(D)) is the sum of δr values (resp. δℓ values) on the path of length D from P

towards the root of Tbad .We denote as DR the maximal integer such that ∆R(DR) ≤ R − k. Similarly, we denote DL as the maximal integer satisfying ∆L(DL) ≤ L. Notice that ∆L(−1) = ∆ R(−1) = 0 , and that L ≥ 0

and R ≥ k, so DL and DR are well defined. We prove the following fact. 

Lemma 5.15. Let D = max(0 , min( DL, D R + 1)) . We have that for every x ∈ [0 ..D ], the r-values of sJ x(i0) is at least k, and for j = sJ D+1 (i0) we have rj < k .Proof. We start by considering the case in which either DL = −1. Notice that if this occurs, we have D = 0 and the claim for x ∈ [0 ..D ] = {0} holds since sJ 0(i0) = i0 and ri0 ≥ k. If DL = −1,we have that ∆L(0) = δℓ(P ) > L . According to Lemma 5.14, it means that i0 is not (0) -bad. According to Lemma 5.7 we have that j = sJ D+1 (i0) = sJ (i0) has rj < k as required. From now on, we assume that DL ≥ − 1 which implies that D = min( DL, D R + 1) .From the definition of D, we have that PDx=0 δℓ(π(x)

> bad

(P )) ≤ L and PD−1 

> x=0

δr(π(x)

> bad

(P )) ≤ R − k.According to Lemma 5.14 we have that i0 is (D)-bad, and therefore j′ = sJ D(i0) is in the phrase 

π(D) 

> B

(P ).29 It follows from Corollary 5.9 and the fact that i0 is (D)-bad that rj′ = R − ∆R(D − 1) ≥ k, so from the monotonicity of r-values when applying sJ we have that for every x ∈ [0 ..D ] it holds that 

sJ x(i0) has r-values at least k.Since rj′ ≥ k, it follows from Corollary 5.5 that j = sJ (j′) = sJ D+1 (i0) is well-defined. We consider two cases. If j′ is not in the bad suffix of π(D) 

> bad

(P ), and rj′ ∈ [k.. 1.5k), we have 

rj < k by Lemma 5.7. Let us now consider the case where j′ is in the bad suffix of π(D) 

> bad

(P ). We have that i0 is (D + 1) -bad, and in particular π(D) 

> bad

(P ) has a parent π(D+1)  

> bad

(P ) in Tbad . According to Lemma 5.14 this implies that ∆L(D + 1) ≤ L, which means that DL > D .Therefore, we have D = DR + 1 and in particular D > D R. It follows that ∆R(D) > R − k.Since all the jumps from i0 to j were from the bad suffix of the block containing them, Corollary 5.9 suggests that rj = R − ∆R(D) < R − (R − k) = k. We have shown rj < k , as required. Having established the connection between DL,DR, and the sought value x∗, we are now inter-ested in constructing a data structure for efficiently computing DL and DR. We prove the following. 

Lemma 5.16. Given Tbad , we can construct in O(z) time a data structure taking O(z) space such that given i0, we can retrieve DL and DR in O(log nz ) time. Within the same query time, the data structure also returns the corresponding ancestors π(x)

> bad

(P ) for the phrase containing P and 

x ∈ { DL, D R}.Proof. We will describe a data structure with O(log n) query time. We will later describe how the running time can be improved to O(log nz ).We build two level-ancestor data structure over the same forest Tbad . In the first data structures, every phrase P is assigned ∆′

> L

(P ) which is the sum of δℓ values of phrases on the path from P to the root (excluding P ). Formally, ∆′

> L

(P ) = Pdi=1 δℓ(π(i) 

> B

(P )) where d is the depth of P in Tbad .Similarly, we build another level ancestor data structure in which each phrase P is associated with the sum of δr values on the path to the root (excluding P ). Formally, ∆′

> R

(P ) = Pdi=1 δr(π(i)

> bad

(P )) .Given i0 with ℓi0 = L and ri0 = R in phrase P , we query the level-ancestor data structure for the lowest ancestor AL of P with ∆′

> L

(AL) ≥ δℓ(P ) + ∆ ′

> L

(P ) − L. Similarly, we query for the lowest ancestor AR with ∆′

> R

(AP ) ≥ δr(P ) + ∆ ′

> R

(P ) − (R − k). A more natural interpretation of AR (and 

AL) is the lowest ancestor of P such that the path from P to AP (including both endpoints) has total δr values at least R − k (resp. the path from P to AL has total δℓ values at least L). Clearly, the depth difference between P and AR is exactly DR, and the depth difference between P and AL

is exactly DL (and if either of AL, A R two does not exist, the corresponding DL or DR value is −1). We have found each of DL and DR and the corresponding ancestors using a single weighted ancestor query, so the query running time is O(log n).The construction time consists of finding all ∆′

> L

(P ) and ∆′

> R

(P ) values and constructing two level-ancestor data structures for Tbad equipped with these values. We can compute δℓ(P ) and 

δr(P ) for every P in O(1) straightforwardly by taking index b such that L(P ) = S[a..b ], compute with b′ = sJ (b) and subtract the ℓ and r values of b and b′ (to find the r and ℓ values of b′, we need access to the phrase containing b′ which we have as it is the parent of P in Tbad ). Computing ∆′

> L

(P ) and ∆′

> R

(P ) for all P can be implemented in O(|Tbad |) = O(z) time by an iteration on Tbad , keeping track of cost of root-to-node path at every step. The total construction time is therefore O(z), as required. 30 Tbad 

> P0
> P1
> P2
> P3
> P4

First with total δr above R − k

> P5
> P6

First with total δℓ above L

Figure 10: A demonstration of the stable part of the epoch (Lemma 3.2). The algorithm uses ancestor queries to find the lowest ancestor of P0 such that the sum of δell values on the path P0

to that ancestor is above L. Similarly, the algorithm finds the lowest ancestor such that the sub om δr values is at least k − R. The algorithm specifically takes D the lowest of these two located ancestors. According to Lemma 5.15, the sequence of sJ steps starting at i0 traverse through the phrases in the path from P0 to D, and the following step leads to an index j with r-value below k.The auxiliary information stored in D and in P0 allows us to find j in constant time. 

Improving the query time to O(log nz ). In the above approach, the query boils down to per-forming weighted ancestor queries. We notice that the weighted ancestor queries required by the algorithm have the following property: When we query for the weighted ancestor of P with weight 

X, the weight of P is at most X + nz .Let us justify this claim. The algorithm queries for the first ancestor of P such that the path from P to P ′ has weight at most R − k or L, where L and R are ℓ and r values of the query index 

i0. Due to Lemma 2.1, we can assume that the phrase containing i0 has length at most nz . It follows that L, R ≤ nz and the property we stated holds. Assume that the weights assigned to the tree Tbad never result in a child having the same weight as its parent. Under this assumption, (and noticing that all weights are integers) we have that each child has weight at least 1 more than its parent. It follows that the answer for our query is at unweighted hight at most nz above P . We can find this answer by preprocessing Tbad for constant time unweighted level ancestor queries [BF04], and binary searching the range [0 .. nz ] for the first parent with weight below a certain threshold in O(log nz ) time. We can enforce the assumption that a child never has the same weight as its parent by contracting edges between same weight parent-child paris. We are finally ready to prove Lemma 3.2 

Proof of Lemma 3.2 (See Fig. 10). We construct Tbad in O(z log 2 nz ). We can find the bad parent of each P ∈ Z in O(log nz ) time by first constructing the data structures of Lemma 5.3 and Lemma 4.7 31 in O(z log 2 nz ). Then, for every P = S[a..b ], the bad parent of P is the phrase containing sJ (a + 23 |P |), which can be found in O(log nz ) time. Given Tbad , we construct the data structures of Lemma 5.13 and of Lemma 5.16. Upon query for i0, we use Lemma 4.7 to find the phrase P containing i0 in O(log nz ) time. Knowing P , we can compute L = ℓi0 , R = ri0 and the integer k ∈ K such that ri0 ∈ [k.. 1.5k).We use the data structure of Lemma 5.16 to find D = max(0 , min( DL, D R + 1)) in O(log nz )

time, alongside the corresponding ancestors. According to Lemma 5.15, we have rsJ x(i0) ≥ k for every x ∈ [0 ..D ] and j = sJ D+1 has rj < k . If D = 0 , we simply return j = sJ (i0) using Lemma 5.3. Otherwise, we have D = min( DL, D R + 1) and therefore ∆L(D) ≤ L and ∆R(D − 1) ≤ R − k. It follows from Lemma 5.14 that i0 is D-bad. We can therefore use the data structure of Lemma 5.13 to obtain j = sJ D+1 (i0) in O(1) time, and return it as a valid output. The construction time and the space of the data structure consist of the construction and space complexities of the data structures of Lemmas 5.3, 4.7, 5.13 and 5.16, which are all bounded by 

O(z) space and O(z log 2 nz ) time. The query time is dominated by one query to each of the data structures of Lemmas 5.13 and 5.16, which is dominated by O(log nz ), as required. 

# 6 Batched pre Computation 

In this section, we prove Lemma 4.10. We use the following data structure that has been designed (in a slightly more restricted variant) by Farach and Thorup [FT98]. For completeness, we provide a complete proof in Section A. 

Lemma 6.1. There is a data structure storing a set X of balanced search trees where each tree in 

X has elements in [1 ..n ]. The data structure supports the following updates: 1. Initialize X as a single empty balanced search tree. 2. Apply an insertion/deletion operation to a tree in X.3. Split (T, a ) : Remove T1 from X, and add T1 over the set T ∩ [1 ..a ] and T2 over the set 

T ∩ [a + 1 ..n ] to X.4. Merge (T1, T 2): Remove T1 and T2 from X, and add a tree T to X over the set T1 ∪ T2.5. Shift (T, δ ): Add δ to every item in the tree T .The initialization takes O(1) time. Each update is implemented in amortized time O(log n log N )

where N is an upper bound on the size of each tree in X.

We first present an algorithm with O(z log 2 n) running time. We will later show how to replace the log n factors with log nz . Denote I = ( b1, e 1), (b2, e 2), . . . , (b|I|, e |I|). Denote B = {bi | i ∈ [|I|]}.Let Z = {ai | i ∈ [z]} be the starting indices of the phrases of the Lempel-Ziv End partition Z of 

S. We define a node for every query in I as follows. For every (bi, e i) ∈ I, we define a node vi with initial value val (vi) = bi. The node vi contains as auxiliary information E(vi) = ei and I(vi) = i.We initialize a tree data of Lemma A.1 and add to X a balanced tree T containing all the vertices 

{vi | i ∈ [|I|]}. The tree is sorted in increasing order of val (v).For every node v ∈ T , we store as auxiliary information the value M (v) which is the maximal 

E(v) value of a node in the subtree rooted at v.We maintain the auxiliary information as the tree is manipulated by the operations of the data structure. Throughout the algorithm, when a node v is in a tree T ′ that is applied a Shift (T, δ )

32 PtPt′

Pt

Pt′

Figure 11: An illustration of the algorithm of Lemma 4.10. Every vertex v in the tree is represented as an interval with a dot end representing val (v) and a line end representing E(v). At iteration t,we take all the intervals with dots contained in Pt and shift them to the source of Pt (displayed as 

Pt′ ). Then, every remaining vertex with E value that exceeds into Pt is removed and reported (in the figure, there is only one such vertex). operation, we consider E(v) to be shifted by the same amount. However, I(v) does not change throughout the running time of the algorithm. 

The algorithm (See Fig. 11) The algorithm initializes an array A of size |I| intended to store the answers to each query. Initially, A[i] = nil for every i ∈ [|I|]. We will show that when the algorithm terminates, A[i] = pre (bi, e i). Throughout the algorithm, we will remove nodes from T .When we remove a node v from T , we assign A[I(v)] = val (v).The algorithm consists of z iterations indexed in decreasing order from z to 1. We maintain the invariant that at the start of every iteration, X contains a single balanced search tree. Initially, 

T = Tz is the tree described above. For every t ∈ [1 ..z ], in decreasing order, the algorithm applies the following. 1. Let T be the single tree in X at the start of iteration t. Let Pt = S[at..b t] be the t-th phrase in Z with source S[a′

> t

..b ′

> t

], and let δt = bt − b′

> t

. If Pt does not have a source, let δt = 0 .2. If δt̸ = 0 :(a) Split: split T into two trees T 1 = T ∩[1 ..a t −1] and T 2 = T ∩[at..b t] using a Split (T, a t−1)

operation. (b) Shift: Apply Shift (T 2, −δt) to obtain T 2 

> δ

.(c) Merge: merge T 1 and T 2 

> δ

into T using Merge (T 1, T 2 

> δ

)

3. Deletion: as long as there is a node v ∈ T with E(v) ≥ at, find v and remove it from T .After applying the subroutine for t = 1 , output A.In each iteration, the algorithm applies O(1) operations of type Split , Shift , and Merge to the data structure. Additionally, the subroutine deletes nodes, with each node deleted at most once throughout the running time of the algorithm. Navigating to nodes with maximal E value can be 33 done in O(log n) using the auxiliary M information. Since we apply |I| deletions and z operations of types Split , Shift , Merge , the total running time is O(( z + |I|) log 2 n).We proceed to show that the algorithm correctly computes all pre values. For every t ∈ [z],denote as Tt the single tree T stored in X at the start of the t-th iteration of the algorithm (where the z-indexed operation is the first one and the 1-indexed iteration is the last one) We start by proving the following invariant. 

Lemma 6.2. Let Pt = S[at..b t] be the t-th phrase in Z. At the start of the t’th operation, every node in Tt has 1 ≤ val (v) ≤ E(v) ≤ bt.Proof. The claim is correct for t = z, as bz = n and in the initial tree Tz every node v has 

[val (v)..E (v)] ∈ I. Since I contains only intervals in [1 ..n ], the claim follows. For t < z , the 

(t + 1) ’th iteration concludes by deleting all vertices v with E(v) ≥ at+1 = bt + 1 . It is therefore clearly holds that all vertices at the start of the t’th iteration have E(v) ≤ bt.It remains to show that we never apply a shift that results in a node v′ with val (v′) < 1. Consider a node v that is shifted in iteration t, and denote as v′ the resulting node. As we have shown, it holds that E(v) ≤ bt. It follows form the fact that v was shifted that it is placed in T 2 when the 

t-th iteration splits T into T 1 and T 2. This can only happen if val (v) ≥ at. In conclusion, we have shown at ≤ val (v) ≤ E(v) ≤ bt. Then, val (v) and E(v) are reduced by δt = bt − b′ 

> t

where S[a′

> t

..b ′

> t

] is the source of P = S[at..b t]. the vertex v′ is the version of v after the shift. It follows from the above that val (v′) = val (v) − δt ≥ at − (bt − b′

> t

) = b′ 

> t

− (bt − at). Since S[b′ 

> t

− (bt − at)..b ′

> t

] is an occurrence of Pt in S, we have that val (v′) ≥ b′ 

> t

− (bt − at) ≥ 1 as it is a proper index in S.We now prove the following invariant, intuitively stating that when we change the value of a node throughout the algorithm, the newly created node represents an equivalent query. 

Lemma 6.3. Let v be a vertex in T at any time throughout the algorithm. It holds that pre (bI(v), e I(v)) = 

pre (val (v), E (v)) .Proof. At the start of the algorithm, the claim immediately follows, as in the initial tree for every 

i ∈ [|I|] we have B(vi) = bi and E(vi) = ei.Consider an iteration t ∈ [2 ..z ] in which the val and E values of a vertex v with I(v) = i change. Let v′ be the modified v with the new val and E values. Denote b = var (v), e = E(b), b′ = var (v′)

and e′ = E(v′). Let Pt = S[at..b t] be the t’th phrase in Z and let S[a′

> t

..b ′

> t

] be the source of Pt.Notice that all vertices that have their value change in the t-th iteration have both their val and E

decreased by δt = bt − b′

> t

. So in particular, we have b′ = b − δt and e′ = e − δt.Due to Lemma 6.2, we have that e ≤ bt. Since the value of v was changed by the subroutine, it must be the case that v is in the tree T 2 created in the t’th iteration, which contains only vertices with val value at least at. In conclusion, we have shown that at ≤ b ≤ e ≤ bt. This means that b and e are both contained in the same phrase Pt. Therefore, by the definition of pre , we have 

pre (b, e ) = pre (b − δt, e − δt) = pre (b′, e ′). By the induction hypothesis, we have pre (b, e ) = pre (bi, e i).It follows that pre (b′, e ′) = pre (bi, e i), as required. The last invariant we need is that when the algorithm removes a node, the value assigned in A

is the correct pre value for the query represented by this node. 

Lemma 6.4. When a node v is removed from T , it holds that pre (val (v), E (v)) = val (v).Proof. Let t be the iteration in which v was removed. Let Pt = S[at..b t] be the t’th phrase in Z.If Pt does not have a source, we have P = [ at]. In this case, the fact that v is removed implies 34 E(v) ≥ (at). From Lemma 6.2 we have that E(v) < b t = at, which implies E(v) = at. We also have from Lemma 6.2 that val (v) ≤ E(v).If val (v) = E(v), then we have that [val (v)..E (v)] = [ at..a t] is completely contained in the phrase 

Pt which has no source. Otherwise, we have val (v) < E (v) = at, which means that val (v) is not in the phrase Pt, while 

E(v) is in the phrase Pt. It follows that val (v) and E(v) are in different phrases. We have shown that in both cases, val (v) and E(v) are not contained in the same phrase which has a source. Therefore, pre (val (v), E (v)) = val (v) from the definition of pre .Let us conclude the correctness of the algorithm. In every iteration, we either delete vertices or change their value. When the algorithm concludes, all vertices are deleted since the first phrase in 

Z must be S[1 .. 1] . Therefore, at iteration 1 we will remove all vertices v with val (v) ≥ 1, which is satisfied for all remaining vertices by Lemma 6.2. When the vertex v with I(v) = i is deleted, we set A[i] = val (v). By Lemma 6.4, it holds that 

val (v) = pre (val (v), E (v)) . By Lemma 6.3, it holds that val (v) = pre (bi, e i), as required. 

Substituting log n factors with log nz . We now describe how to implement the above approach, replacing the log n factors with log nz factors. We uniformly partition the domain [1 ..n ] into z

intervals Ui = ( i · nz .. (i + 1) nz ], each of size nz . Instead of maintaining one tree containing all queries, we maintain a separate tree Ti maintaining queries (b, e ) with b ∈ Ui. The queries are sorted according to the primary order b, and the secondary order e.When we implement a split, the split interval corresponds to some phrase Pi = S[ai..b i]. Due to Lemma 2.1, we have that |Pi| ≤ nz , which means that [ai..b i] spans at most two intervals Ux,Ux+1 .We partition the range [ai., b i] into at most two sub-ranges, and remove each of them from the corresponding Ui. Similarly, when we merge the shifted version of [ai..b i] back intro a tree, we first find in constant time the at most two intervals Ux, U x+1 intersecting the (shifted) range, split [ai..b i]

accordingly and merge each piece into the corresponding Ux,Ux+1 . Finding the Ux intersecting 

[ai..b i] in both of these cases can be done in constant time. We would maintain the invariant that every tree is of size O(( nz )2), so each search tree operation costs O(log nz ). Since Ti contains only queries (b, e ) with b ∈ Ui, the number of distinct b values in 

Ti is bounded by nz . Notice that the distance between b and e never changes throughout the lifetime of a node. Since initially we have e − b ≤ nz for every query, we have that this inequality holds for every query throughout the running time of the algorithm. Therefore, for every b there are at most nz possible values of e, and the possible distinct (b, e ) pairs that can be in a tree Ti is bounded by ( nz )2. In Ti, every node with values (b, e ) stores, as auxiliary information, a linked list of all i

such that the query qi in the initial input currently corresponds to this node. Initially, each node is attached with a single i, but we may combine nodes with identical b and e values throughout the running time of the algorithm, connecting their corresponding lists. Whenever the size of a tree Ti exceeds 2( nz )2, we iterate Ti and merge every duplication we find in O(1) time. Since |Ti| ≥ ( nz )2 and we have less than ( nz )2 distinct values of Ti that may belong in Ti, we are guaranteed to find at least ( nz )2 duplications in this process. It follows that we find a duplication in amortized constant time. After applying the above, the size of Ti is again bounded by ( nz )2.Since every merge increase the total number of queries across all trees by 1, we will have at most 

z merges throughout the running time of the algorithm, each found and implemented in O(1) time. Another change that the partition into intervals introduces to the algorithm is in the tests for the maximal value e across all queries in our data structures. We would not like to check all Ti’s individually, and even storing a heap over all of Ti’s maximal elements is too costly (This will 35 i′  

> b−k

J    

> J(b−k)
> j′
> ri′<1.5k

k< 0.5k

> rj′

P

source (P )

Figure 12: An illustration of the first part in the proof of Lemma 7.1. Assuming to the contrary that the phrase containing J(b − k) is not bad, and that i′ < b − k is k-bad, we have that there is a phrase boundary separating j′ = J(i′) and J(b − k). Therefore, the r-value of i′ is less than k, a contradiction. introduce an extra cost of O(log z)). Instead, recall that every (b, e ) pair has e ∈ [b..b + nz ]. We also recall that at every point throughout the running time of the algorithm, we have some prefix of the domain still containing queries, so U1, U 2, . . . U x are active, and every Uy with y > x is empty. Together, these two properties imply that the maximal e values is either in Tx or in Tx−1, so it is sufficient to query both Ti and Ti−1 for their maximal value in O(1) time. 

# 7 Data Structure for Finding a Bad Parent 

In this section, we prove Lemma 4.19. We will describe a data structure with running time 

O(z(log 2 nz +(log log n)2)) . Then, we will discuss how to replace the log log n factor with log log nz . In this section, k-bad parent corresponds to the definition presented in Section 4 (following Lemma 4.18), i.e., the k-bad parent of P is the parent of P in Tk.Our data structure consist of two main component. The primary ingredient is a mechanism that allows us, given a query phrase P and k ∈ K, to retrieve a set of O(1) candidates for being the 

k-bad parent of P in O(1) time. The second, complementary component is a verification algorithm, allowing us to check if a given phrase is the k-bad parent of P in constant time. We start by presenting the verification algorithm. The main part of the verification algorithm is the following characterization of the k-bad parent. 

Lemma 7.1. Let P = S[a..b ] be a phrase and let k ∈ K. Let P ′ = S[a′..b ′] be the k-bad parent of P

and let i∗ = max( a, b − 1.5k + 1 , a ′ + δP ). The following properties hold. 1. P ′ contains the index J(b − k).2. ri∗ ≥ k and rJ(i∗) ≥ k.Proof. We start by proving the first statement (See Fig. 12). Assume to the contrary that P ′ =

S[a′..b ′] exists and J(b − k) /∈ [a′..b ′]. Notice that J(b − k) = b − k − δP . Let i′ ∈ [a..b ] be a bad jump with ri′ ∈ [k.. 1.5k). In other words, i′ = b − ˆk for some ˆk ∈ [k.. 1.5k). Denote j′ = J(i′) = b − ˆk − δP .Since i′ is a bad jump with ri′ ∈ [k.. 1.5k), we have that j′ is in P ′(Lemma 4.18). Since j′ and 

J(b − k) are not in the same phrase, there is a phrase boundary between them. This guarantees that rj′ ≤ J(b − k) − j′ = b − k − δ − (b′ − ˆk − δ) = ˆk − k < k . A contradiction to i′ being a bad jump. We now prove that the second statement (See Fig. 13). Notice that i∗ = max( a, b − 1.5k + 1 , a ′ +

δP ) is exactly the leftmost index satisfying: 36 a′ + δP

J

a

> a′

1.5k − 1

P

P ′

Figure 13: An illustration of the second part in the proof of Lemma 7.1. The three indices from the definition of i∗ are presented. Clearly, an index i smaller than any of these three cannot be a 

k-bad index of P : If i < a , then i is not in P . If i < a ′ + δP , then J(i) is not in P ′ (which contains all J values of k-bad indices). If i < b − 1.5k (and i is in P ) then ri > 1.5k. It follows that there are no k-bad indices to the left of i∗. Further, i∗ has r-values below 1.5k, so if it is not k-bad it must be either due to its r-value being to small or due to the r-value of J(i∗) being too small. Any other candidate for being k-bad to the right of i∗ would have even smaller such values, so if i∗ is not k-bad, no index is k-bad in P .1. i∗ ∈ [a..b ],2. J(i∗) ∈ [a′..b ′], and 3. ri∗ ∈ [k.. 1.5k).Let i′ be a bad jump in P with ri′ ∈ [k.. 1.5k). We show that i′ also satisfies the above three conditions. By definition, i′ is in [a..b ] and ri′ ∈ [k.. 1.5k]. Since it is a bad jump, we also have 

j′ = J(i′) ∈ [a′..b ′] due to Lemma 5.7. This leads to i∗ ≤ i′, and since they are in the same phrase, it leads to ri∗ ≥ ri′ ≥ k. Since i′

is a k-bad jump, we have that j′ = J(i′) is in P ′. From the definition of i∗, we have that i∗ is also in P ′, and that i∗ ≤ i′. Since J(i∗) = j∗ and j′ are also in the same phrase, we have rj∗ ≥ k, as required. Lemma 7.1 immediately implies an O(1) verification algorithm, given below. 

Corollary 7.2. Given a phrase P = S[a..b ] in Z, δP , another phrase P ′ = S[a′..b ′] and some k ∈ K,we can check in O(1) time if P ′ is the k-bad parent of P .Proof. We straightforwardly check if J(b−k) = b−k−δP is in [a′..b ′] and if max( a, b −1.5k+1 , a ′+δP )

is a bad jump from P to P ′.We proceed to show that a set of O(1) candidates can be retrieved efficiently. As a means to that end, we show how to use Lemma 7.1 to find the k-bad parent of a given P in O(log nz ) time. This is not fast enough for our final goal - which is supporting such queries in constant time. However, it will be useful in the construction of the data structure. 

Lemma 7.3. There is a data structure that given a phrase P and k ∈ K, outputs the k-bad phrase of P or reports that it does not exist in O(log nz ) time. The data structure can be constructed in 

O(z log nz ) time and uses O(z) space. Proof. We construct the data structure of Lemma 4.7 in O(n log nz ) time and O(z) space. Now, given a query phrase P = [ a..b ] and k ∈ K, we can find J(b − k) (If b − k is not in P , then P clearly 37 do not have a k-bad parent) in O(1) time. We can then use the data structure of Lemma 4.7 to find the phrase P ′ containing J(b − k) in O(log nz ) time. Due to Lemma 7.1, we know that if P has a k-bad parent, it must be P ′. We check if P ′ is the bad parent of P in O(1) time using Corollary 7.2. We are now ready to describe the main part of the data structure for finding the k-bad parent of a given P . We define the notion of critical bad values. 

Definition 7.4 (Critical Bad Values) . For a phrase P in Z and k ∈ K such that |P | ≥ 2k. Let 

k′ = 1 .5k be the successor of k in K. We say that k is a critical bad value for P if P has a k-bad parent P ′, and P ′ is not the k′-bad parent of P .

Intuitively, one can consider the process of fixing a phrase P and increasing k from min( K) to 

max( K). The critical bad values of P are the values of k where the k-bad parent changes in this process (excluding very large k values with k > |P |/2). We prove several useful properties of the 

k-critical bad values. We start by showing that for k-critical values, the k-bad parents of P is of size roughly k.

Lemma 7.5. Let P = S[a..b ] be a phrase in Z with source S[a′..b ′]. Let k ∈ K be a critical value of P . Let Pk be the k-bad parent of P . It holds that |Pk| ∈ [k.. 1.5k)

Proof. It should be clear that |Pk| ≥ k, as Pk contains an index j with rj ≥ k. Assume to the contrary that |Pk| ≥ 1.5k. Since Pk = S[ak..b k] is a k-bad parent, there is an index i ∈ [a..b ]

with j = J(i) ∈ [ak..b k] and ri, r j ∈ [k.. 1.5k). Let us assume that i is maximal among the indices satisfying these conditions. This means that either ri = k or rj = k, as otherwise i + 1 satisfies these conditions as well. Notice that in both cases, it must hold that rj = k, since rj ∈ [k..r i].Now consider the index i′ = i − 0.5k and let k′ = 1 .5k. We will show that i′ has ri′ , r j′ ∈

[k′.. 1.5k′), which implies that Pk is the k′-bad parent of P . This is a contradiction to k being a critical bad value. Notice that ri′ = b − ri − 0.5k ≥ b − 2k. Since |P | ≥ 2k, we have that i′ is in P . We have that ri′ = ri + 0 .5k ∈ [1 .5k.. 2k) ⊆ [k′.. 1.5k′). Additionally, since i′ ∈ P we have that 

j′ = j − 0.5k = bk − rj − 0.5k = bk − 1.5k. Since we assume |Pk| ≥ 1.5k, it holds that j′ is in Pk

with rj′ = 1 .5k = k′.Next, we show that the set of values k such that a phrase P ′ is the k-bad parent of P form a consecutive interval. 

Lemma 7.6. Let P be a phrase and let k1 < k 2 < k 3 ∈ K. If P ′ is the k1-bad parent of P and the 

k3-bad parent of P , then P ′ is also the k2-bad parent of P .Proof. We will prove that under the specified assumptions, P ′ is also the 1.5k1 bad parent of P .This can be applied inductively until k2 = 1 .5k1 to obtain the claim. Let P = S[a..b ] and let P ′ = S[ˆ a.. ˆb]. Let the source of P be S[a′..b ′]. For every index i ∈ [a..b ]

such that J(i) ∈ [ˆ a.. ˆb], the difference rJ(i) − ri is the same. Denote this difference as δ. Since P ′ is both the k1 and the k3-bad parent of P , we have from Lemma 7.1 that b′ − k1, b ′ − k3 ∈ [ˆ a.. ˆb]. Since 

P ′ is the k1-bad parent, there is an index i1 ∈ [a..b ] with j1 = J(i1) ∈ [ˆ a.. ˆb] such that ri1 , r j1 ∈

[k1.. 1.5k1). Similarly, since P ′ is the k3-bad parent we have some i3 ∈ [a..b ] and j3 = J(i3) ∈ [ˆ a.. ˆb]

with ri3 , r j3 ∈ [k3.. 1.5k3] In particular, since rj1 = ri1 − δ, we must have δ < 0.5k1 < 0.5k2. Now, consider the index i2 = b − k2 − δ.We claim that i2 ∈ [i3..i 1]. It follows from i3 = b − ri3 ≤ b − k3 ≤ b − 1.5k2 ≤ b − k2 − δ = i2,and from i1 = b − r1 ≥ b − 1.5k1 ≥ b − k2 ≥ b − k2 − δ = i2. Therefore, i2 ∈ [a..b ] is an index 38 within P , and due to the same reasoning, we have that j2 = J(i2) ∈ [ˆ a.. ˆb]. Now notice that 

ri2 = k2 + δ ∈ [k2.. 1.5k2) and that rj2 = ri2 − δ = k2. We have shown that i2 is an index in P with 

j2 in P ′ such that ri2 , r j2 ∈ [k2.. 1.5k2], which means that P ′ is the k2 bad parent of P .We are ready to present our data structure for efficiently finding the k-bad parent. For every phrase P = S[a..b ] ∈ Z we store the following information. 1. k(P ) ⊆ K: the at most two unique values k ∈ K such that |P | ∈ [k.. 2k)

2. P ′(P ) ⊆ Z : for every k ∈ k(P ), the k-bad phrase of P , if it exists. 3. C(P ): a bit vector of size log 1.5 |P | ∈ O(log n). If k is a critical bad value of P , then 

C(P )[ k] = 1 , otherwise C(P )[ k] = 0 .4. K(P ) ∈ K: the unique power of 1.5 satisfying |P | ∈ [K(P ).. 1.5K(P )) .5. h(P ) ∈ [1 ..n ]: the minimal integer multiple of K(P ) contained in P . Formally, hP = min( {i ·

K(P ) | i ∈ Z} ∩ [a..b ]) 

Additionally, we build the hash table H in which for every P , we have H(h(P )) = P (P is represented as its index in Z). The construction of H requires O(z(log log z)2) time with Lemma 2.2. The stored information for each phrase can be computed in O(log 2 nz ) time for every phrase as follows. The values k(P ), K(P ) and h(P ) can be obtained in O(1) time via simple arithmetic operations. P ′(P ) can be obtained in O(log nk ) time using the data structure of Lemma 7.3. To construct C(P ), we iterate all values in K. For each such value, we check if it is a critical bad value directly according to the definition. To do that, we need to be able to retrieve the k-bad parent of 

P for a given k ∈ K, which we do in O(log nk ) time using Lemma 7.3. The total running time for finding C(P ) is O(|K| · log nz ) = O(log 2 nz ).Let us prove that the above information is sufficient to retrieve the k-bad parent of P in O(1) 

time. 

Lemma 7.7. Given a phrase P = S[a..b ] of Z with its source S[a′..b ′] and the values k(P ), P ′(P ), C (P ).Given the hash H, we can find for a given k the k-bad parent of P (or report that it does not exist) in O(1) time. Proof. We describe an algorithm for finding the k-bad parent of P or reporting that it does not exist. The algorithm constructs a set of candidate phrases P for being the k-bad parent of P . Then, the algorithm tests each candidate for being the k-bad parent of P using Corollary 7.2. The set of candidates is constructed as follows. First, we add the phrases in P ′(P ) to P. Then, we find the smallest critical bad value k′ of P that is at least k using C(P ). If there is indeed such value k′, we iterate each integer x that is an integer multiple of k′ contained in [b′ − 2.5k′..b ′ + 0 .5k′].For each such x, we add Px = H(x) to P, if such Px it exists (i.e. if x is a key in H). This concludes the construction of P. Clearly, |P| ∈ O(1) . This follows from |P ′(P )| ≤ 2 and from the fact that there are at most 4 integer multiples of k′ in [b′ − 2.5k′..b ′ + 0 .5k′]. It should be clear that each element in P is retrieved in O(1) time. In order to obtain k′, we need to find the minimal bit that is at least k and is equal to 1 in C(B). This can be implemented by first computing the bitwise AND of C(B) and its negation, which leaves only the rightmost bit set to 1. Then, we can lookup the position of the unique bit set to 1 by using a preprocessed perfect hash table that can be constructed in O(log n · (log log n)2) = O(z log( n/z )) time with Lemma 2.2. Then, we verify each of the candidates in O(1) time using Corollary 7.2. It should be clear that the total running time is O(1) .39 To prove the correctness of the algorithm, we claim that if there is a k-bad parent P ′ for P ,we indeed include P in P. Assume that there is a k-bad parent P ′. Let k′ ≥ k be the maximal value in K such that P ′ is a k′-bad parent of P . If k′ ∈ k(P ), then P ′ ∈ C(P ) ⊆ P , as required. If 

k′ /∈ k(P ), it follows that |P | ≥ 2k′. We claim that in this case, k′ is exactly the minimal critical bad value of P that is at least k.

Claim 7.8. Let P ′ be the k-bad parent of P and let k′ be the maximal k such that P ′ is the k′-bad parent of P . Let c be the minimal critical bad value of P that is at least k. If |P | ≥ 2k′, then k′ = c.Proof. First, we prove that c ≤ k′ by showing that P ′ is a c-bad parent of P . Assume to the contrary that P ′ is not the c-bad parent of P . Let c′ be the minimal value in K such that P ′ is not the c′-bad parent of P . Notice that c′ ∈ [1 .5k..k ′] ∩ K. For this values, we have that P ′ is the  

> c′
> 1.5

-bad parent of P but not the c′-bad parent of P . We also have 2 c′ 

> 1.5

≤ 2c′ ≤ 2k′ ≤ 2|P |, which together indicates that c′ is a critical bad value of P that is at least k and is smaller than k′ , a contradiction. Now, let us show that for any c ≥ k′. Assume to the contrary that k′ > c . We have already shown that P ′ is the c-bad parent of P . Since k′, c ∈ K, we have k′ ≥ 1.5c. From P ′ being both the 

k′-bad parent and the c-bad parent of P , Lemma 7.6 implies that P ′ is also the 1.5c-bad parent of 

P . In particular, P ′ is both the c and the 1.5c bad parent of P with 2c ≤ 2k′ ≤ | P |, a contradiction to c being a critical bad value. It follows from claim 7.8 that P ′ is indeed the k′-bad parent of P for the value k′ we find using 

C(P ). According to Lemma 7.5, we have that K(P ) = k′, and according to Lemma 7.1 we have that P ′ contains the index b′ − k′. Therefore, since |P ′| ≤ 1.5k′ we have that P ′ is contained in 

[b′ − 2.5k′..b ′ + 0 .5k′], and in particular h(P ′) ∈ [b′ − 2.5k′..b ′ + 0 .5k′]. Since h(P ′) is a multiple of 

k′, and we check every multiple of k′ in this range, we have found H(h(P ′)) = P ′ and added it to 

P, as required. 

Substituting (log log z)2 with log nz . We notice that the (log log z)2 factor arises from computing the deterministic hash of [Ruz08] over z elements from domain of size n. We can instead partition the domain n into z uniform intervals U1, U 2, . . . U z of size nz . Then, we construct a separate hash Hi

for every Ui, only mapping the values in Ui to their respective elements in [z]. When constructing the hash of Ui = ( i · nz .. (i + 1) · nz ], we use relative values instead of absolute values (i.e., x ∈ Ui is encoded as x − i · nz ). Therefore, the hash is over a domain of size nz . Now, when we wish to find 

H(x), we first find Ui containing x in constant time, and query Hi[x′] with x′ −i· nz in constant time. The time for constructing Hi is zi(log log zi)2 = O(zi log nz ), where zi is the number of hashed values within Ui. Since Pzi=1 (zi) = z, the total running time for constructing all hashes is O(z log nz ) as required. The query time remains O(1) .

# References 

[BCG +21] Djamal Belazzougui, Manuel Cáceres, Travis Gagie, Pawel Gawrychowski, Juha Kärkkäi-nen, Gonzalo Navarro, Alberto Ordóñez Pereira, Simon J. Puglisi, and Yasuo Tabei. Block trees. J. Comput. Syst. Sci. , 117:1–22, 2021. [BEGV18] Philip Bille, Mikko Berggren Ettienne, Inge Li Gørtz, and Hjalte Wedel Vildhøj. Time-space trade-offs for lempel-ziv compressed indexing. Theor. Comput. Sci. , 713:66–77, 2018. 40 [BF04] Michael A. Bender and Martin Farach-Colton. The level ancestor problem simplified. 

Theor. Comput. Sci. , 321(1):5–12, 2004. [BFK +23] Hideo Bannai, Mitsuru Funakoshi, Kazuhiro Kurita, Yuto Nakashima, Kazuhisa Seto, and Takeaki Uno. Optimal lz-end parsing is hard. arXiv preprint arXiv:2302.02586 ,2023. [BGG +15] Djamal Belazzougui, Travis Gagie, Pawel Gawrychowski, Juha Kärkkäinen, Alberto Or-dóñez Pereira, Simon J. Puglisi, and Yasuo Tabei. Queries on lz-bounded encodings. In 

DCC , pages 83–92. IEEE, 2015. [BLR +15] Philip Bille, Gad M. Landau, Rajeev Raman, Kunihiko Sadakane, Srinivasa Rao Satti, and Oren Weimann. Random access to grammar-compressed strings and trees. SIAM J. Comput. , 44(3):513–539, 2015. [CEK +21] Anders Roy Christiansen, Mikko Berggren Ettienne, Tomasz Kociumaka, Gonzalo Navarro, and Nicola Prezza. Optimal-time dictionary-compressed indexes. ACM Trans. Algorithms , 17(1):8:1–8:39, 2021. [CLL +05] Moses Charikar, Eric Lehman, Ding Liu, Rina Panigrahy, Manoj Prabhakaran, Amit Sahai, and Abhi Shelat. The smallest grammar problem. IEEE Trans. Inf. Theory ,51(7):2554–2576, 2005. [CNP21] Francisco Claude, Gonzalo Navarro, and Alejandro Pacheco. Grammar-compressed in-dexes with logarithmic search time. J. Comput. Syst. Sci. , 118:53–74, 2021. [FT98] Martin Farach and Mikkel Thorup. String matching in lempel-ziv compressed strings. 

Algorithmica , 20(4):388–404, 1998. [GGK +14] Travis Gagie, Pawel Gawrychowski, Juha Kärkkäinen, Yakov Nekrich, and Simon J. Puglisi. Lz77-based self-indexing with faster pattern matching. In LATIN , Lecture Notes in Computer Science, pages 731–742. Springer, 2014. [GJL21] Moses Ganardi, Artur Jez, and Markus Lohrey. Balancing straight-line programs. J. ACM , 68(4):27:1–27:40, 2021. [GS78] Leonidas J. Guibas and Robert Sedgewick. A dichromatic framework for balanced trees. In FOCS , pages 8–21. IEEE Computer Society, 1978. [Hag98] Torben Hagerup. Sorting and searching on the word RAM. In STACS , Lecture Notes in Computer Science, pages 366–398. Springer, 1998. [IÖ10] John Iacono and Özgür Özkan. Mergeable dictionaries. In ICALP (1) , Lecture Notes in Computer Science, pages 164–175. Springer, 2010. [KK17a] Dominik Kempa and Dmitry Kosolobov. Lz-end parsing in compressed space. In DCC ,pages 350–359. IEEE, 2017. [KK17b] Dominik Kempa and Dmitry Kosolobov. Lz-end parsing in linear time. In ESA , LIPIcs, pages 53:1–53:14. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2017. [KN13] Sebastian Kreft and Gonzalo Navarro. On compressing and indexing repetitive se-quences. Theor. Comput. Sci. , 483:115–133, 2013. 41 [KS22] Dominik Kempa and Barna Saha. An upper bound and linear-space queries on the lz-end parsing. In Proceedings of the 2022 Annual ACM-SIAM Symposium on Discrete Algorithms (SODA) , pages 2847–2866. SIAM, 2022. [LZ76] Abraham Lempel and Jacob Ziv. On the complexity of finite sequences. IEEE Trans. Inf. Theory , 22(1):75–81, 1976. [Nav22] Gonzalo Navarro. Indexing highly repetitive string collections, part II: compressed in-dexes. ACM Comput. Surv. , 54(2):26:1–26:32, 2022. [NII +20] Takaaki Nishimoto, Tomohiro I, Shunsuke Inenaga, Hideo Bannai, and Masayuki Takeda. Dynamic index and LZ factorization in compressed space. Discret. Appl. Math. ,274:116–129, 2020. [Ruz08] Milan Ruzic. Constructing efficient dictionaries in close to sorting time. In ICALP (1) ,Lecture Notes in Computer Science, pages 84–95. Springer, 2008. [Ryt03] Wojciech Rytter. Application of lempel-ziv factorization to the approximation of grammar-based compression. Theor. Comput. Sci. , 302(1-3):211–222, 2003. [TTS15] Yoshimasa Takabatake, Yasuo Tabei, and Hiroshi Sakamoto. Online self-indexed gram-mar compression. In SPIRE , Lecture Notes in Computer Science, pages 258–269. Springer, 2015. [VY13] Elad Verbin and Wei Yu. Data structure lower bounds on random access to grammar-compressed strings. In CPM , Lecture Notes in Computer Science, pages 247–258. Springer, 2013. [ZL77] Jacob Ziv and Abraham Lempel. A universal algorithm for sequential data compression. 

IEEE Trans. Inf. Theory , 23(3):337–343, 1977. 

# A Shift-Split-Merge Structure 

This section is dedicated to proving the following lemma. As mentioned earlier, it has been already proven by Farach and Thorup [FT98], and we only present our proof for completeness. Also, we make it more clear that the time complexity depends on n, the maximum number of distinct elements in a tree. We briefly comment that Iacono and Özkan [IÖ10] claimed a faster data structure, but a complete description of the shift operation isn’t present in the published version, and it is not immediately clear if their complexity could be made dependant on the number of distinct elements. 

Lemma A.1. There is a data structure storing a set X of balanced search trees where each tree in 

X has elements in [1 ..n ]. The data structure supports the following updates: 1. Initialize X as a single empty balanced search tree. 2. Apply an insertion/deletion operation to a tree in X.3. Split (T, a ) : Remove T1 from X, and add T1 over the set T ∩ [1 ..a ] and T2 over the set 

T ∩ [a + 1 ..n ] to X.4. Merge (T1, T 2): Remove T1 and T2 from X, and add a tree T to X over the set T1 ∪ T2.5. Shift (T, δ ): Add δ to every item in the tree T .

42 The initialization takes O(1) time. Each update is implemented in amortized time O(log n log N )

where N is an upper bound on the size of each tree in X.

We implement each balanced search tree with e.g. red-black trees [GS78]. Recall that a red-black tree T has the property that, given a node u ∈ T , we can split T into a tree containing all elements strictly smaller or equal to u and a tree containing all elements larger than u in O(log N )

time. Similarly, given two trees T1 and T2 such that every element stored in T1 is smaller or equal to every element stored in T2, we can obtain a tree storing all elements in T1 and T2 in O(log N )

time. We call the former operation splitting and the latter joining (to distinguish from merging, which is defined as in the statement of the lemma). Of course, because the tree is balanced, we are able to find the (strict) successor/predecessor of any element in O(log N ) time, and insert/delete new elements in the same time complexity. Each node v of the tree is decorated with a shift δv, and the invariant is that the, a node u

physically storing an element x actually corresponds to x increased by the sum of δv, over every v

that is an ancestor of u. It i straightforward to maintain the invariant during a rotation. Initialize, insertion/deletion, split, and shift are immediate to implement, with the first working in worst-case constant time, and the remaining operations working in O(log N ) worst-case time. The non-trivial step is implementing a merge. For a tree T , let distinct (T ) denote the number of distinct elements in T . We first observe that, given two trees T1 and T2, we can compute their merge in O(min {distinct (T1), distinct (T2)} log N )

time. This is done as follows. We first retrieve the smallest elements of T1 and T2, denoted x1

and y1, respectively. By symmetry, let us assume that x1 ≤ y1. We split T1 at y1 to obtain T ′

> 1

and T ′ 

> 1

and we similarly split T2 at y1 to obtain T ′ 

> 2

(containing only elements equal to y1) and T ′′  

> 2

.We join T ′

> 1

, T ′

> 2

, and the result of repeating the procedure on T ′′  

> 1

and T ′′  

> 2

. The time complexity is 

O(min {distinct (T1), distinct (T2)} log N ), and more precisely it is upper bounded by the interleave between the distinct elements of T1 and T2 times O(log N ). However, we will establish that the amortized complexity is actually much better. To analyze the amortized complexity of a merge, we use the following potential function, where to avoid clutter each unit of potential suffices to pay for O(log N ) time. Consider a tree T , and let its elements (ignoring the duplicates) be x1 < x 2 < . . . < x k. We define potential of T to be P 

> i

1 + log( xi+1 − xi), and then the potential of the whole structure is the sum of the potentials of its constituent trees. It is immediate that the amortized cost of initialization is O(1) , the amortized complexity of insertion is O(log n log N ) as we must account for the increase in the potential, while the amortized complexity of deletion, split and shift is O(log n log N ) as the potential cannot increase. To analyze the change in the potential after a merge operation, we first observe that the additional 1 in the definition allows us to absorb the cost for the elements that occur in both trees. Thus, we will assume that the elements stored in both trees are distinct. To analyze the change in the potential, it is enough to analyze the situation after inserting a range of k elements 

xi < x i+1 < . . . x i+k−1 from one tree between two consecutive elements yj < y j+1 of the other tree, which corresponds to a single step of the merging procedure. Then, the distance of yj to its successor is decreased by at least half while the distance of every other element to its successor does not increase, or the distance of yj to its predecessor is decreased by at least half while the distance of every other element to its predecessor does not increase. The potential can be equivalently defined by considering the distance of each element to its successor or the distance of each element to its predecessor, so we obtain that for at least half steps of the merging procedure the potential decreases by at least 1, which allows us to pay for all the steps. 43 B Eliminating Large Phrases 

This section is dedicated to proving the following lemma. 

Lemma 2.1. There is an algorithm that given Z, an LZ-End partition of a string S, returns Z′,an LZ-End partition of S with size at most 4z such that every phrase P ′ ∈ Z ′ has |P ′| ≤ nz . Here, 

n = |S| and z = |Z| . The algorithm runs in time O(z).

We start by showing that a large phrase can be replaced by a small number of smaller phrases. We then show how to apply this replacement iteratively and efficiently. 

Claim B.1. Let Z be an LZ-End partition with z phrases of a string S such that all the phrases have length at most nz except for the last phrase Pi. We can partition Pi into at most 2z|Pi| 

> n

+ 1 

substrings Q1, Q 2, . . . such that for every Qj = S[c1..d 1], there is a phrase Pij = S[aij ..b ij ] for some 

ij < i such that Qj = S[bij − | Qj | + 1 ..b ij ]. Equivalently, Pi can be replaced with Q1, Q 2, . . . in Z

with respective sources Pi1 , P i2 , . . . , resulting in a valid LZ-End partition of S.Proof. For i ∈ [z], denote cut (i) = j as the index of the unique phrase Pj containing bi − nz . We will show how to replace Pi with (at most) three substrings Q1, Q 2, Q 3 such that |Q2|, |Q3| ≤ nz and 

|Q2| + |Q3| ≥ nz . As a consequence, we have that |Q1| ≤ | Pi| − nz . This routine may partition Pi

with just Q2 and Q3 satisfying the above conditions, omitting Q1. To simplify presentation, we will denote that |Q1| = 0 if the phrase Q1 was not created. If |Q1| > nz , we will apply the same splitting routine on Q1, replacing it with three new phrases and reducing the length of the leftmost remaining phrase by nz again. After z|Pi| 

> n

iterations of the above approach, we must have that |Q1| ≤ nz , and we have replaced Pi with a total of 2z|Pi| 

> n

+ 1 

phrases. We now describe the construction of Q1,Q2, and Q3. Let Pj = S[aj ..b j ] be the source of Pi. Let 

j′ = cut (j), so the phrase Pj′ = [ aj′ ..b j′ ] contains bj − nz . Let c1 = bi −(bj −aj′ ) and c2 = bi −(bj −bj′ )

We define Q3 = S[c2..b i] with source Pj . Notice that |Q3| = bi − c2 + 1 = bj − bj′ + 1 ≤ nz . And in particular, |Q3| ≤ | Pi|, so Q3 is always a substring of Pi.For Q2 and Q1, we have two cases. If c1 ≥ ai, we define Q1 = S[ai..c 1 − 1] with source Pj′ , and 

Q2 = S[c1..c 2 − 1] with source Pj′−1. Otherwise, if c1 > a i we only define Q2 = S[ai..c 2 − 1] with source Pj′ .It can be easily verified that the sources we define for Q1,Q2, and Q3 indeed satisfy the required equality, as they correspond to the correctly aligned substrings of Pi = S[bj − | Pi| + 1 ..b j ]. Since the phrase Pj′ contains the index bj − nz , we have that |Q3| = bj − b′ 

> j

− 1 ≤ nz . Since Pj′ is a phrase with j′ < i , we have that |Pj′ | ≤ nz . Notice that in both cases we have |Q2| ≤ | Pj′ | ≤ nz , as required. On the other hand, notice that |Q2| + |Q3| = min( bi − c1, |Pi|). We know that |Pi| ≥ nz . Since 

Pj′ = S[aj′ ..b j′ ] contains bj − nz , we have that aj′ ≤ bj − nz which leads to bi − c1 = bj − aj′ ≥ nz .We have shown that both components in min( bi − c1, |Pi|) are at least nz , so |Q2| + |Q3| ≥ nz , as required. We are now ready to prove Lemma 2.1. 

Proof. We process Z from left to right. As long as we encounter phrases smaller than nz , we simply append them to Z′ (which is initially empty). When we reach a phrase Pi with |Pi| ≥ nz with 

O( 2z|Pi| 

> n

+ 1) phrases by applying claim B.1 on Z′, P i (notice that Z′, P i is an LZ-End partition of 

S[1 ..b i] with only the last phrase larger than nz ), and add the obtained pieces Q1, Q 2, . . . to Z′ from left to right (instead of adding Pi). 44 In order to apply the decomposition of Pi into smaller phrases described in claim B.1, we need to be able to access cut (j), the phrase containing bj − nz in constant time. To support this, we compute and store cut (j) for every phrase P ′ 

> i

that is added to Z′ as we process Z from left to right. With access to cut (j), a straightforward implementation of Lemma 4.18 will takes O(ni) where ni

is the number of newly created phrases. When we append P ′ 

> x

= [ a′

> x

..b ′

> x

] to Z′, we first check if P ′ 

> j′

with j′ = cut (x′ − 1) contains b′ 

> x

− nz .If it does, we set cut (x) = j′. Otherwise, it must be the case that b′ 

> x

− nz is to the right of Pj′ . We check Pj′+1 , P j′+2 , . . . until finally reaching the phrase P ′ 

> c

containing b′ 

> x

− nz and set cut (x) = c. Since every phrase is only eliminated once as cut (x), the overall evaluation of all cut (x) values requires 

O(z′) = O(z) time (we justify z′ = O(z) below). The number of new pieces created is bounded by P 

> P

( 2z·| P | 

> n

+ 1) where the sum is taken over the phrases we split. Since we only split phrases with length at least nz and the sum of the lengths of all phrases is n, the number of components in this sum is O(z). We therefore have that the total number of added phrases is bounded by 2zn n + z = 3 z. It follows that the newly created partition has at most 4z phrases. Since we create every new phrase in constant time, the time complexity is proportional to the number of phrases, which is O(z).

# C Data Structure for Substring Extraction 

In this section, we present an efficient compressed data structure for substring extraction, proving Theorem 1.2. That is, given [i..j ], return S[i..j ]. We will present a data structure that uses O(z)

space and supports queries in O(log 2 nz + j − i) time. That is, the best conceivable query time without improving upon the query time for random access presented in Theorem 1.1. We will assume that j − i ≤ log 2 nz , and show an algorithm with running time O(log 2 nz ). In the general case where j − i is large, we can break S[i..j ] into pieces of size log 2 nz and extract each piece separately for a total running time of O(j − i).

Intuition and Overview. As a primary tool, we will first present an optimal data structure with O(z) space and O(j − i) query time for the special case where j is the ending index of a phrase 

P (also given at query time) 1. We call this data structure a suffix extraction data structure, and we will make extensive use of this data structure in our general substring extraction data structure. The existence of the suffix extraction data structure naturally defined an ’advantageous’ setting where general substring extraction can be done efficiently. For instance, if S[i..j ] contains a phrase boundary b, we can extract S[i..b ] in O(b − i + log nz ) time (the O(log nz ) factor is required to find b

via Lemma 4.7). This allows us to assume that our input does not contain a phrase boundary With this assumption in place, another convenient case for extraction arises. If ri ∈ O(j − i) we can use suffix extraction to extract S[i..i + ri] in O(j − i) time. Due to our assumption that [i..j ]

has no phrase boundaries, we have i + ri > j and we therefore have S[i..j ] at hand as a prefix of 

S[i..i + ri].Our goal now shifts to reducing a general input i, j into an input with ri ∈ O(j − i). To this end, we will employ the epoch approach of the random access algorithm. Intuitively, we would like to say that this goal is obtainable by simply running the random access algorithm on i, and halting the algorithm prematurely when a sufficiently small r-value is obtained. Recall that the random access algorithm consists of epochs, each reducing the r-value to the next exponential level compared to the value seen at the start of the epoch. Therefore, one may expect that when running a sequence of 

> 1Kreft and Navarro [KN13] also introduced an algorithm for this case, but their definition of LZ-End is slightly different from the one we use, which is the common definition in recent works on LZ-End.

45 epochs on index i, we will encounter a sequence of indices i = i1, i 2, . . . i x such that if rix ∈ [k.. 1.5k),then rix+1 ∈ [k/ 1.5..k ) (i.e., in the next smallest exponential level). Combined with the observation that as long as rix > j − i, it holds that S[ix..i x + j − i] = S[i..j ], this structure immediately leads to an extraction algorithm. We can run the random access algorithm on i, and instead of halting upon reaching i′ with ri′ = 0 , we halt upon reaching ri′ ∈ [j − i.. 1.5( j − i)] This should occur within 

O(log nz ) epochs, leading to O(log 2 nz ) running time, with an additional O(j − i) running time to apply suffix extraction on the reduced instance. Unfortunately, the above structure does not necessarily occur in our random access algorithm. It may be the case that the r-value of ix is arbitrarily smaller than rix−1 , leading to an r-value smaller than j − i. This means that following the source of the phrase containing ix will not necessarily lead to an occurrence of S[i..j ], making any further step following ix potentially lose some of the information we are required to extract. To bypass this problem, we define a modified variant of the epoch procedure. Our epoch pro-cedure takes indices i, j as input with ri ∈ [max(10( j − i), k ).. 1.5k) for some k ∈ K. The implied assumption that ri > 10( j − i) can be enforced as we established in the previous discussion. The epoch procedure has two outputs. First, it outputs the prefix S[i..i ′) for some index i′ ∈

[i..j + 1] (notice that if i′ = i, this component of the output is trivial). Additionally, the epoch outputs an index i∗ on which we can recourse, and that makes progress towards a termination condition. That is, the index i∗ has S[i∗..j ∗] = S[i′..j ] for j∗ = i∗ − i′ + j, which means that the remaining suffix to be extracted from S[i..j ] occurs at i∗. We also demand that the index i∗ has 

ri∗ ∈ [j − i..k ), bringing us closer towards the termination condition of the algorithm. The running time is O(i′ − i + log nz ).For the special case where i′ = j + 1 , we simply have that the epoch procedure extracted S[i..j ]

in its entirety, and completing the execution of the extraction query. In this special case, we allow the epoch to consume O(j − i + log 2 nz ) time. For the formal presentation of the epoch procedure, see Lemma C.3. We implement the epoch by essentially running the epoch of the extraction procedure on i. If it happen to result in a gradual decrease in r-value, we can simply return its output. Otherwise, we carefully analyze the cases that arise from an r-value below j − i, and exploit the structure that arises in each of these cases to extract some prefix of S[i..j ] while locating an alternative i∗ with a sufficiently small r-value. 

C.1 Suffix Extraction 

. We start by solving the special case where j is an ending index of a phrase. We assume that the query specifies the phrase P that ends in index j. We will show a data structure with O(z) space, 

O(z log 2 nz ) construction time, and O(j − i) query time. 

Preprocessing and tree structure In preprocessing time, we compute δ-pre (a, b ) for every phrase P = S[a..b ] in O(z log 2 nz ) using Lemma 4.10. We defined for each P = S[a..b ] a parent phrase P ′ as follows. Let b′ = b − δ-pre (a, b ). The phrase containing b′ is P ′. This defines a forest structure over the phrases of Z, we compute and store this forest T . Finally, we construct an unweighted ancestor data structure for T .

Query Our query procedure is recursive. Given a query S[i..j ] with the phrase ending at index 

j, we simply return S[j] if i = j.Otherwise, we recurse on the LZ-End phrases strictly contained within S[i..j ]. We find the phrases intersecting S[i..j ] in time proportional to their number by simply starting with the phrase 46 P = Py ending in j and traversing Py−1, P y−2 . . . until we reach some Px = [ ax..b x] such that ax ≤ i.For every p ∈ [x..y ] seen in this process, we recursively extract S[ap..b p] by recursing on S[a′

> p

..b ′

> p

]

where a′ 

> p

= ap − δ-pre (ap, b p) and b′ 

> p

= bp − δ-pre (ap, b p).The leftmost phrase Px = S[ax..b x] intersecting S[i..j ] requires special care We use an ancestor query to find the root of the tree containing Px in the forest T . Notice that this must be a phrase of length 1, so it is equal to a suffix of S[ax..b x] (of length 1) contained within S[i..j ]. Further notice that every ancestor of Px is an occurrence of some suffix S[bx − ℓ..b x] of Px. Let P 1 be the root of the tree in T containing Px. We generate the sequence P 1, P 2, . . . P x′

of phrases as follows. For 

k ≤ x, if |Pk| < b x − i we set P k+1 to be the child of P k in T towards Px. Otherwise, if |P k| ≥ bx − i,we set P k−1 = P x′

as the last element of the sequence and terminate. In words, this is the prefix of the path from the root to Px that is truncated before the first phrase with length at least bx − i.Notice that the lengths |P 1|, |P 2|, . . . form an increasing sequence. Denote for every k ∈ [1 ..x ′] the endpoints P k = S[ak..b k]. The algorithm recurses on S[bk −|P k+1 |..b k − | P k|]. Finally for P x′

the algorithm recurses on S[bx′

− (bx − i)..b x′

− | P x′

|]

Correctness. Clearly, Px+1 , P x+2 , . . . , P y form a suffix of P [i..j ], so extracting each of them leads to the extraction of this suffix. As for the prefix covered by Px, we claim that the recursive calls induced by P 1, P 2, . . . P x′

are for strings that form a partition of S[i..b x]. More precisely, we claim that for every k ∈ [1 ..x ′)

it holds that S[bx − | P k+1 |..b x − | P k|] = S[bk − | P k+1 |..b k − | P k|], and that S[i..b x − | P x′

] = 

S[bx′

− (j − bx)..b x′

− | P x′

|].This follows immediately from the following stronger property 

Lemma C.1. Let P = S[a..b ] be a phrase in Z. Let P ′′ be an ancestor of P in T and let P ′′ =

S[a′′ ..b ′′ ] be the parent of P ′ in T . It holds that S[b − (b′ − a′)..b ] = S[b′′ − (b′ − a′)..b ′′ ].Proof. We prove by induction on the distance from P to P ′. For the case of distance 0, we have that P = P ′ = S[a..b ] and P ′′ = S[a′′ ..b ′′ ] such that b′′ = b − δ-pre (a, b ). It is immediately implies by the definition of δ-pre that S[a..b ] = S[a′′ ..b ′′ ], as required. For the inductive vase, we have that P ′ is at distance d > 0 from P . Let ˆP = S[ˆ a.. ˆb] be the child of P ′ towards P . From the induction hypothesis, we have that S[b − (ˆb − ˆa)..b ] = S[b′ − (ˆb − ˆa)..b ′].Since ˆb − ˆa > b ′ − a′, we have in particular S[a′..b ′] = S[b′ − (b′ − a′)..b ′] = S[b − (b′ − a′)..b ]. By definition, P ′′ = S[a′′ ..b ′′ ] such that b′′ = b′ − δ-pre (a′, b ′). It is immediately implies by the definition of δ-pre that S[b′′ − (b′ − a′)..b ′′ ] = S[a′..b ′] = S[b − (b′ − a′)..b ], as required. Lemma C.1 shows that the equality S[bx − | P k+1 |..b x − | P k|] = S[bk − | P k+1 |..b k − | P k|] indeed holds. Further notice that the substring S[bk − | P k+1 |..b k − | P k|] ends in a phrase boundary, as 

bk − | P k| + 1 is the first index of P k, so .b k − | P k| is the last index of the phrase preceding P k.In conclusion, we have shown that the recursive calls we made are for strings that together form a partition of S[i..j ], and that each of them is smaller than S[i..j ]. It follows that the algorithm returns the correct answer, and that it terminates. 

Complexity. During the running time of the query, we are either in a termination case of the recursion - and return S[j] in constant time, or we make R recursive calls, and work O(R) time in total to make these calls for some integer R > 1.This process can be described as a tree, where the children of each recursive call are the recursive calls made by it. As every recursive call partition the substring it is responsible for, this tree has 

j − i leaves, and each leaf correspond to O(1) time of the algorithm. Every external node of the tree with degree R corresponds to a recursive calls which is executed in O(R) time. 47 It follows that the total running time is bounded by the number of edges in a tree with j − i

leafs such that all internal degrees are at least 2, which is O(j − i) as required. We conclude with the following. 

Lemma C.2. Given an LZ-End factorization Z with z phrases of a string S with length n, we can construct in O(z) space and O(z log 2 nz ) time a data structure that given i, j , and a phrase P that ends in index j, returns S[i..j ] in O(j − i) time. 

C.2 The General Case 

As in the random access algorithm of Theorem 1.1, our algorithm is partition into epochs, where each epoch may extract some prefix S[i..i ′) of S[i..j ], and provide an index i∗ that is an occurrence of S[i′..j ], and i∗ has a significantly lower r-value than i. An epoch is described as the following procedure. 

Lemma C.3. For a string S with LZ-End factorization Z of size z, there is a data structure supporting the following query. 

Input: i ≤ j ∈ [n] such that ri ∈ [k.. 1.5k) for k ∈ K, and ri > 10( j − i).

Output: For some i′ ∈ [i..j ], return S[i..i ′) and an index i∗ ∈ [n] such that S[i∗..i ∗ − i′ + j] = 

S[i′..j ] and ri∗ ∈ [j − i′..k ).The data structure can be constructed from Z in O(z log 2( nz )) . The query time is O(j −i+log 2 nz )

if the output is S[i..j ]. Otherwise, the query time is O(i′ − i + log nz ).

Given Lemmas C.3 and C.7, we can prove Theorem 1.2. 

Proof of Theorem 1.2. For the preprocessing, we construct the data structure of Lemma C.3. For a query i, j , we repeatedly apply the following. First, we would like to enforce ri > j − i. To this end, we find the starting index i′ of the phrase containing j using Lemma 4.7. If i′ ≤ i, we already have ri ≥ j − i. Otherwise, the prefix S[i..i ′]

can be retrieved in O(i′ − i) using Lemma C.2, and the remaining task is to retrieve S[i′..j ], where 

ri′ ≥ j − i′. To avoid clutter, let us simply assume that ri ≥ j − i (implicitly using i in reference to 

i′ if the above reduction was required). If ri ≤ 10( j −i) we can use Lemma C.2 to retrieve S[i..i +ri] in O(ri) = O(j −i) time. Otherwise, we have that ri > 10( j − i) and therefore we can apply Lemma C.3. If Lemma C.3 outputs S[i..j ],we are done. Otherwise, let k ∈ K such that ri ∈ [k.. 1.5k). The output of Lemma C.3 is S[i..i ′)

and an index i∗ such that S[i′..j ] = S[i∗..i ∗ − i + j], and ri∗ < k . Our task is therefore reduced to extracting S[i∗..i ∗ − i + j]. We obtain this reduced instance in total O(i′ − i + log nz ) time. When iteratively applying the above, every call for Lemma C.3 results in i′ with ri′ ∈ [k.. 1.5k)

for a k ∈ K strictly smaller than the one seen in the previous call. Therefore, we will have a total of at most O(log nz ) calls before we finally reach ri ∈ [j − i.. 10( j − i)] . At this point, we can extract 

S[i..j ] as previously described. Since every call to Lemma C.3 retrieves a disjoint fragment of the initial S[i..j ], the total time contributed by the O(i′ − i) component of all calls to Lemma C.3 is O(j − i). Since we make a total of O(log nz ) calls, the total time contributed by the O(log nz ) components of the calls to Lemma C.3 is O(log 2 nz ). In total, the running time is O(j − i + log 2 nz ), as required. It may also be the case that the last call we make for Lemma C.3 returned the remaining suffix. In this case, the running time of this call has an O(log 2 nz ) factor instead of O(log nz ). Since we immediately terminate when this happens, the total running time would not exceed (j − i +log 2 nz )

48 For the rest of this section, we prove Lemma C.3. As in the random access algorithm, we split the epoch into a naive part and a stable part. We will reuse a lot of concepts and terminology from the random access algorithm. Let us prove some helpful properties of the pre function. 

Lemma C.4. Let i ∈ [a..b ] with ri ≥ b − i, and let i′ = i − δ-pre (a, b ). There is a phrase boundary in [i′ + b − i..i ′ + ri].Proof. Let us define the pre -depth of a pair (a, b ), denoted as d(a, b ). If pre (a, b ) = a then the 

d(a, b ) = 0 . Otherwise, the d(a, b ) = d(J(a), J (b)) + 1 . In words, the pre -depth of (a, b ) is the number of recursive calls of pre () before the value of pre (a, b ) is reached. We prove the claim by induction on the pre -depth of (a, b ). If the d(a, b ) = 0 , then i = i′. If 

ri < b − a then a and b are not in the same phrase, and therefore δ-pre (a, b ) = 0 , we have i′ = i and the claim holds since i′ + ri = i + ri is a phrase boundary. If d = d(a, b ) > 0, we have that a and b are contained in the same phrase P , and pre (a, b ) = 

pre (a∗, b ∗) such that the pre -depth of (a∗, b ∗) = ( J(a), J (b)) is d−1. Let i∗ = J(i) = i−δP ∈ [a∗..b ∗].It holds due the induction hypothesis that if ri∗ ≥ b∗ − i∗, then there is a phrase boundary in 

[i′..b ∗ − i∗..i ′ + ri∗ ]. If it indeed hold that ri∗ ≥ b∗ − i∗, the induction hypothesis implies that there is a phrase boundary in [i′..b ∗ − i∗..i ′ + ri∗ ]. The r-monotonicity of J() implies ri∗ < r i, and it should be clear that b − i = b∗ − i∗. Together, the three imply that there is a phrase boundary in 

[i′..b − i..i ′ + ri]

In the other case, if ri∗ < b ∗ − i∗, there is a phrase boundary in [a∗..b ∗], so pre (a, b ) = a∗ and 

i∗ = i′. Since i′ is obtained by following the source link of the phrase ending in i + ri, we have that there is a phrase ending in i′ + ri, which is in the required interval [i′ + b − i..i ′ + ri] if ri ≥ b − i.The following two are immediate corollaries of Lemma C.4 

Corollary C.5. Let P ∈ M be a marked phrase. Let I = [ a..b ] be an interval in the halved canonical partition of P , and let i ≤ j be two indices in [a..b ] and let M (i) = i′ and M (j) = j′. If ri′ < j − i,then the phrase containing j′ is of length at most ri.Proof. Since i, j are contained in the same phrase, we have ri = j −i+rj . Clearly, we have rj ≥ b−j

since j + rj is the right boundary of P , while [a..b ] is an interval contained in P . By definition, 

j′ = j − δ-pre (a, b ), so it follows from Lemma C.4 that there is a phrase boundary in [j′..j ′ + rj ].Since i′ = i − δ-pre (a, b ), we also have i − j = i′ − j′.Due to our assumption that ri′ < j − i, we have a phrase boundary in [i′..i ′ + ( j − i)) = [ i′..j ′).We have shown that there is a phrase boundary at most (j − i) indices to the left of j′, and a phrase boundary at most rj indices to the right of j′. Therefore, the length of the phrase containing j′ is at most rj + j − i = ri, as required. 

Corollary C.6. Let P = S[a..c ] ∈ Z be phrase and let L(P ) = S[a..b ]. i ≤ j be two indices in [a..b ]

and let sJ (i) = i′ and sJ (j) = j′. If ri′ < j − i, then the phrase containing j′ is of length at most 

ri.Proof. Since i, j are contained in the same phrase, we have ri = j −i+rj . Clearly, we have rj ≥ b−j

since j + rj is the right boundary of P , while S[a..b ] is prefix of P . By definition, j′ = j − δ-pre (a, b ),so it follows from Lemma C.4 that there is a phrase boundary in [j′..j ′ +rj ]. Since i′ = i−δ-pre (a, b ),we also have i − j = i′ − j′.Due to our assumption that ri′ < j − i, we have a phrase boundary in [i′..i ′ + ( j − i)) = [ i′..j ′).We have shown that there is a phrase boundary at most (j − i) indices to the left of j′, and a phrase 49 boundary at most rj indices to the right of j′. Therefore, the length of the phrase containing j′ is at most rj + j − i = ri, as required. We now present an adjusted version of the naive part of the epoch from the random access algorithm (Lemma 3.1). 

Lemma C.7. Given an LZ-End factorization Z with z phrases of a string S with length n, we can construct in O(z log 2 nz ) time a data structure taking O(z) space that supports the following query. Given i, j ∈ [n]2 with ri ∈ [max(10( j − i), k ).. 1.5k) for some k ∈ K, return an occurrence S[i′..j ′]

of S[i..j ] (specified by its endpoints) satisfying one of the following. 1. ri′ ∈ [j − i..k ),2. There is a phrase boundary in S[i′..j ′], and ℓj′ + rj′ + 1 < 1.5k, or 3. S[i′..j ′] is contained some a marked phrase and ri′ < 1.5k.The running time of the query is O(log nz ).Proof. In preprocessing time, we construct the data structure of Lemma 3.1. In query time,we run the query algorithm of Lemma 3.1 with the following modification. Recall that the query of Lemma 3.1 applies steps of the form J(i), Jlog nz (i) until the first time an index i′

is reached that either has ri < k or is contained within a marked phrase. We apply exactly the same steps, but we stop when we either reach ri′ < max( k, j − i) or then we reach an i′ contained in a marked phrase. Since our stopping condition is strictly stronger than that of Lemma 3.1, the running time until one of our conditions is met is bounded by the query time of Lemma 3.1, which is O(log nz ) (notice that checking that these conditions are satisfied is done in constant time, as we have access to ri′ for every index i′ visited throughout the query). Let i′ be the first index seen throughout the query satisfying ri′ < max( k, j − i) or i′ is in a marked phrase. Firstly, we wish to ensure that i′ is an occurrence of S[i..j ]. Let i′′ be the last index seen before i′ in the execution of the query. It must hold that ri′′ > j − i, and since r-values are monotonic, it holds that the r-values of all seen indices preceding i′′ is also at least j − i.This implies that S[i′′ ..i ′′ + j − i] = S[i..j ]. If i′ = J(i′′ ), it follows from the definition of J that 

S[i′..i ′ + j − i] = S[i′′ ..i ′′ + j − i] = S[i..j ].Otherwise, if i′ = Jlog nz (i′′ ), it is not necessarily the case that S[i′..i ′ + j − i] = S[i..j ]. However, if we iteratively apply J(i′′ ), J 2(i′′ ), . . . until we finally reach an index i∗ with r-value less than 

max( k, j − i), then i∗ will be an occurrence of S[i..j ] due to the same argument as before. Notice that this can be implemented in O(log nz ) time, since we are guaranteed to meet this condition at 

Jlog nz (i′′ ) (and all of those steps correspond to an upward path in Tbad , since i′ was in the (log nz )-bad interval of its containing phrase). So far, we have shown a procedure that finds an occurrence S[i′..j ′] of S[i..j ] with either ri′ <

max( j − i, k ) or i′ in a marked phrase. Let us show that this S[i′..j ′] is a valid output, satisfying one of the three conditions specified in the statement of the lemma. If ri′ ∈ [j − i..k ), then it is a valid output (satisfying the first condition in the statement of the lemma). If ri′ < j − i, then [i′..j ′] contains a phrase boundary. Furthermore, recall that 

ri′ ≤ ri < 1.5k. Therefore, there is a phrase boundary at most 1.5k indices to the right of i′, and since j′ is trapped between the phrase boundary in [i′..j ′] and the phrase boundary in [i′..i ′ + 1 .5k),the phrase containing j′ is of size less than 1.5k, and we have that S[i′..j ′] satisfies the second condition statement of the lemma. If i′ is contained in a marked phrase, we have two cases. If [i′..j ′] is fully contained in the marked phrase, we have that the last condition of the lemma is satisfied. Otherwise, we have 50 a phrase boundary in [i′..j ′], and due to the same argument as before, we have that the phrase containing j′ is of size less than 1.5k. Therefore, S[i′..j ′] satisfies the second condition of the lemma in this case. The running time consists of the O(log nz ) running time of the query of Lemma 3.1, plus possibly 

O(log nz ) time to compute Jlog nz (i′′ ) (or some lower number of J applications) step-by-step. The total running time is O(log nz ), as required. Similarly to the random access algorithm, we have that the naive part, specified above as Lemma C.7, may already return a satisfactory output as S[i′..j ′] = S[i..j ] with ri ∈ [j − i..k ).We prove the following useful properties of sJ () .

Lemma C.8. Let P = S[a..b ] be a phrase with bad parent P ′ = S[a′..b ′]. Let i ∈ [a..b ] such that i′ = sJ (i) is well-defined. It holds that |P ′| ≤ | P |. Additionally, if i′ /∈ [a′..b ′], it holds that 

ri′ < 2/3|P |, and if i′ ∈ [a′..b ′], then ℓi′ < ℓ i.Proof. Denote ap = sJ (a) and bp = sJ (a +  23 |P | − 1) . Recall that a′ is the rightmost phrase boundary in [ap..b p]. Also recall that applying sJ never increases the r-value, so rbp ≤ ra+⌊ 23 |P |⌋−1 =

⌈| P |/3⌉. We have shown that there is a phrase boundary in [bp..b p +⌈| P |/3⌉] = [ ap + 23 |P |−1..a p +

|P |], and that a′ ∈ [ap..b p]. Since b′ is the first phrase boundary to the right of a′, this necessarily means b′ < a p + |P | which in turn leads to b′ < a ′ + |P |. It follows that |P ′| ≤ | P |. Furthermore, notice that a′ > a p, as ap and bp are not in the same phrase (by the definition of pre ). Therefore, we have ℓi′ < ℓ i if i′ ∈ [a′..b ′].If i′ is not in P ′, than the r-value of i′ is decreased by at least ⌈| P |/3⌉ compared to i which had 

ri ≤ | P |. Therefore, ri′ ≤ ri − | P |/3 ≤ 23 |P |.We proceed to show that in each of the second and the third cases for the output of Lemma C.7, we can apply an additional procedure to obtain a valid output for Lemma C.3. We start by providing the following subroutine. Intuitively, we show that if [i..j ] contains a phrase boundary, and j is contained in phrase of length less than 1.5k, we can efficiently return a valid output for the epoch. 

Lemma C.9. There is a data structure with O(z) space for the following query. Given i, j satisfying: 1. There is a phrase boundary in S[i..j ], and 2. ℓj + rj − 1 < 1.5k for k ∈ K, and k > 2( j − i),return for some i′ ∈ [i..j + 1] the string S[i..i ′) and i∗, j ∗ such that S[i′..j ] = S[i∗..j ∗] and ri∗ < k .The query time is O(i′ − i + log nz )

Proof. At preprocessing, we apply exactly the preprocessing of Lemma 3.2 to obtain the tree Tbad 

such that the parent of each P is its bad parent P ′, and the pre () values of L(P ) for every phrase 

P ∈ Z .Given a query i, j with k ∈ K satisfying the required properties, we first find the phrase P =

S[a..b ] containing j using Lemma 4.7, and extract S[i..a − 1] using Lemma C.2 in O(a − i) time. Notice that |P | = ℓj + rj − 1 < 1.5k. If |P | ≤ 3( j − i), we can extract P = S[a..b ] in O(j − i)

time using Lemma C.2 and obtain S[a..j ] as a prefix of S[a..b ], completing the extraction of all 

S[i..j ] (formally, we also return an arbitrary phrase boundary as i∗ and j∗ = i∗ − 1. This is valid since i′ = j + 1 and S[i′..j ] = ε). Otherwise, we have rj = b − j = ( b − a) − (j − a) ≥ 3( j − i) − (j − i) = 2( j − i), and 

ℓj = j − a ≤ j − i ≤ 2rj . It follows from observation 5.2 that sJ (j) is defined. We repeatedly apply 

sJ () to obtain the sequence j0 = j = sJ 0(j), j 1 = sJ (j), j 2 = sJ 2(j), . . . j x = sJ X (j) until we reach 51 some jX that is not in the bad suffix of the phrase containing it. For every x ∈ [0 ..X ], denote as 

Px the phrase containing jx. Notice that we have access to the bad parent Px+1 of Px via the tree 

Tbad , so each of those steps is carried in O(1) time. As we compute the sequence sequence j0, j 1, . . . ,we will show that the following invariants are preserved. 1. ℓx+1 < ℓ x and |Px+1 | ≤ | Px| for every x ∈ [0 ..X − 1] .2. S[jx − ℓjx ..j x] = S[jx+1 − ℓjx ..j x+1 ] for every x ∈ [0 ..X − 1] .3. After reaching jx, we have already extracted S[i..j − ℓjx ) for every x ∈ [0 ..X − 1] .Initially, the invariant is satisfied because j0 = j, and j − ℓj = a − 1, and we already extracted 

S[i..a − 1] . It follows directly from Lemma C.8 that |Px+1 | ≤ | Px| and ℓx+1 < ℓ x for every x ∈

[0 ..X − 1] . The second invariant follows from the definition of sJ (jx). For every x ∈ [0 ..X − 1] ,the value sJ (jx) is defined which implies that jx is in L(PX ), the leftmost 2/3 of Px. Denote 

L(Px) = S[ax..b x], and notice that ax = jx − ℓjx .The function sJ (jx) = jx −δ-pre (Px) maps jx to an occurrence of L(Px) such that jx+1 is aligned with jx within this occurrence. It directly follows that S[jx − ℓjx ..j x] = S[jx+1 − ℓjx ..j x+1 ]. Notice that the second invariant combined with the first yield S[jx+1 − ℓx..j x+1 ] = S[j − ℓx..j ], since the ℓ

values are decreasing. It remains to show how we maintain the extraction invariant. When we traverse from jx to jx+1 ,we notice that S[jx+1 − ℓjx ..j x+1 − ℓjx+1 ] is a suffix of the phrase ending right before Px+1 . We extract S[jx+1 − ℓjx ..j x+1 − ℓjx+1 ] = S[j − ℓjx ..j − ℓjx+1 ] in O(ℓx − ℓx+1 ) time using Lemma C.2. This concludes the maintenance of all three invariants. When we finally reach PX , we have that jX is not contained within the bad suffix of PX , and that 

|PX | ≤ PX−1 ≤ . . . ≤ P0 = |P | < 1.5k. If jX is not in L(PX ), then rjX < |PX |/3 < 1.5k/ 3 = 0 .5k,and it also holds that rjX −ℓjX < 0.5k + ℓjX ≤ 0.5k + ℓ0 ≤ 0.5k + ( j − i) ≤ k. It is therefore valid to output jX − ℓX as i∗ and jX as j∗.Otherwise, we have that jX is to the left of the bad suffix, and therefore jX+1 = sJ (jX ) is well defined. Let iX = jX − min( ℓjX , ℓ jX+1 ). It holds that iX is also not in the bad suffix of PX (as it is still within PX = S[jX − ℓjX ..j X + rjX ], and to the left of jX ). It follows that sJ (iX ) = i′

is well defined, and due to Lemma C.8 we have ri′ ≤ 23 |Px| < k . In particular it holds that 

i′ = jX+1 − min( ℓX , ℓ X+1 ). If i′ = jX+1 − ℓjX , we have already extracted S[i..j − ℓX ), and we have 

S[i′..j X+1 ] = S[j − ℓX ..j ] and ri′ < k , so i′ and jX+1 are valid outputs for i∗ and j∗, respectively. Otherwise, if i′ = jX+1 − ℓjX+1 , we can extract S[j − ℓjX ..j − ℓjX+1 ] as in the previous cases, and then i′ and jX+1 are valid outputs. 

Query Time. Initially, we spend O(log nz ) time to find the phrase P containing j and extract the prefix of S[i..j ] to the left of P . When processing jx, we retrieve a non empty substring of S[i..i ′)

disjoint from all previously extracted substrings in time proportional to the length of the substring, so the total time spent on extractions is O(i′ − i) where S[i..i ′) is the prefix we end up extracting. We may also spend O(log nz ) time when the last jX is reached to find the phrase containing jX+1 ,so we can compute ℓX+1 , iX , and i′ (for the case in which this is required). The total running time is O(i′ − i + log nz ), as required. We now show another case where we can efficiently find a valid output for the epoch. Namely, we show that if we have an occurrence of S[i..j ] such that i ∈ L(P ), i.e. in the leftmost 2/3 indices of the phrase P containing i, then we can efficiently find a valid output for the epoch. More precisely: we either find a valid output for the epoch in O(i′ − i + log nz ), or we completely extract all of S[i..j ]

52 in time O(log 2 nz ). The latter case correspond to the looser bound on the running time allowed in the statement of Lemma C.3 for the case in which the epoch extracts all of S[i..j ].

Lemma C.10. Given an LZ-End factorization Z with z phrases of a string S with length n, we can construct in O(z log 2 nz ) time a data structure with O(z) space supporting the following query. Given i, j such that i and j are in the same phrase P , j − i ∈ [0 .. log 2 nz ), i is in L(P ), and 

ri ∈ [max( k, 10( j − i)) .. 1.5k) for some k ∈ K, output one of the following. 1. S[i..j ]

2. S[i..i ′) and indices i∗, j ∗ such that S[i∗..j ∗] = S[i′..j ] and ri∗ < k .If the first is returned, the running time is (j − i + log 2 nz ). If the latter is returned, the running time is O(i′ − i + log nz ).Proof. As preprocessing, we construct the data structure of Lemma 3.2. Additionally, for every phrase P = S[a..b ] ∈ Z , we compute δ-pre (a, a + 23 |P |+log 2 nz ). This can be achieved in O(z log 2 nz )

time using Lemma 4.10. At query time, we apply the query of Lemma 3.2 on i with ri ∈ [max( k, 10( j − i)) .. 1.5k). Notice that since [max( k, 10( j − i)) .. 1.5k) is not empty, it must be the case that 1.5k > 10( j − i) which implies k > j − i. Let us recall the output of the query of Lemma 3.2. Consider the sequence 

i0, i 1, . . . of indices with i0 = i and ix = sJ (ix), which is terminated when we reach iX with riX < k .The query retrieves this iX and also iX−1. Denote jX−1 = iX−1 − i + j and jX = iX − i + j.Since riX−1 ≥ k > (j − i), we have that S[iX−1..j X−1] = S[i..j ]. Furthermore, since iX = sJ (iX−1)

is well defined, we have that iX−1 is in L(P ′) where P ′ is the phrase containing iX−1, and that 

iX is within an occurrence of L(P ′). Denote L(P ) = S[a..b ] and let S[a′..b ′] be the occurrence of 

S[a..b ] containing iX . We consider three cases depending on the r-value of iX and on whether or not jX ∈ L(P ′).

Case 1: riX ∈ [j −i..k ) In this case, it holds that S[iX ..i X +j −i] = S[iX−1..i X−1 +j −i] = S[i..j ].We can therefore return iX as a valid i∗ output and extract nothing. 

Case 2.A: riX < j − i, and jX−1 < b In this case, jX−1 is also contained in L(P ′) = S[a′..b ′],then it holds that S[iX ..j X ] = S[iX−1..j X−1] = S[i..j ]. Since riX < j − i, there is a phrase boundary 

ˆa = iX + riX such that ˆa ∈ S[iX ..j X ). It follows from Corollary C.6 that the phrase containing jX is of length at most ri which is less than 1.5k, and we can apply Lemma C.9. This yields some prefix 

S[iX ..i ′ 

> X

) = S[i..i ′) and indices i∗, j ∗ such that S[i∗..j ∗] = S[i′ 

> X

..j X ] = S[i′..j ] in O(i′ − i + log nz )

time, which is a valid output. 

Case 2.B: riX < j − i and jX−1 > b Recall our assumption that j − i ≤ log 2 nz . Notice that 

jX−1 − iX−1 = j − i, so we have jX−1 − iX−1 ≤ log 2 nz . It follows from ix−1 ≤ b that we have that 

jX−1 < b + log 2 nz . Recall that pre (a, b ) is defined by a sequence (a, b ) = ( a0, b 0), (a1, b 1), . . . , (ad, b d)

such that if ay and by are not in the same phrase, then (ay, b y) = ( ad, b d) and otherwise ay+1 , b y+1 =(J(ay), J (by)) . It should be clear that pre (a, b + log 2 nz ) will be defined by a prefix of this sequence, where it would only be a proper prefix if for some y ∈ [0 ..d ), it holds that by and by + log 2 nz are in different phrases. If it happens to be the same sequence, then S[iX ..j X ] = S[iX−1..j X−1] = S[i..j ]

and the same argument as before follows. Otherwise, we have some i′ 

> X

= iX−1 − δ-pre (a, b + log 2 nz )

such that S[i′ 

> X

..j ′ 

> X

] = S[i..j ], and there is a phrase boundary at most log 2 nz to the right of j′ 

> X

. If this phrase boundary is to the left of j′ 

> X

, we are in the same situation as in Case 2.A. Otherwise, we can retrieve S[i..j ] in O(j − i + log 2 nz ) time using Lemma C.2. 53 We are finally ready to put it all together and prove Lemma C.3. 

Proof of Lemma C.3. In preprocessing time, we construct the data structures of Lemmas C.7 and C.9. Given a query i, j with ri ∈ [max(10( j − i), k ).. 1.5k), we apply a query of Lemma C.7 to obtain an occurrence S[i′..j ′] of S[i..j ] satisfying one of the three conditions specified in Lemma C.7. We proceed as follows based on the satisfied condition. 

Case 1: ri′ ∈ [j − i..k ). In this case, we can simply output i′.

Case 2: There is a phrase boundary in S[i′..j ′], and ℓj′ + rj′ + 1 < 1.5k. In this case, we apply Lemma C.9 to obtain S[i′..i ′′ ) for some i′′ ∈ [i′..j ′ +1] and an index i∗ with S[i∗..j ∗] = S[i′′ ..j ′]

in O(i′′ − i′ + log nz ) time. Notice that this is a valid call, since ri ∈ [10( j − i).. 1.5k) and therefore 

1.5k > 10( j − i) which in particular implies k > 2( j − i). The extracted prefix and the index i∗ are a valid output, and the running time is as required. 

Case 3: S[i′..j ′] is contained in a marked phrase and ri′ < 1.5k. Let S[x..y ] be the canonical interval containing i′, and let M (i′) = ˆ i. Notice that S[ˆ i.. ˆi + rˆi] = S[i..i + rˆi]. We consider three sub-cases. 1. Case 3.A : rˆi ∈ [k.. 1.5k). In particular, we have that S[ˆ i.. ˆj] = S[i..j ] for ˆj = ˆ i + j − i

(since k > j − i). It follows from rˆi ≥ k and from claim 4.14 that ℓj < k , which leads to 

ˆi ∈ L( ˆP ) where ˆP is the phrase containing ˆi. We can apply Lemma C.10 to ˆi, ˆj to obtain 

S[ˆ i.. ˆj] = S[i..j ] in O(j − i + log 2 nz ) time or to obtain S[i..i ′) and i∗, j ∗ that are valid outputs in O(i′ − i + log nz ) time. 2. Case 3.B : rˆi ∈ [j − i..k ). In this case we can simply return ˆi and extract nothing. 3. Case 3.C.I : rˆi < j − i, and j′ ∈ [x..y ]. In this case, we have S[ˆ i.. ˆj] = S[i..j ] and M (j) = ˆ j.This follows from the fact that M () maps i′ to an occurrence S[ˆ x.. ˆy] of S[x..y ], which contains an occurrence of S[i..j ]. Since M (i′) = ˆ i and since ri′ < 1.5k, it follows from Corollary C.5 that the phrase containing ˆj has size less than 1.5k and we can apply Lemma C.9 to obtain a valid output. 4. Case 3.C.II : rˆi < j − i, and j′ /∈ [x..y ]. Here we find the rightmost phrase boundary a′ in 

[x′..y ′], and check what is ra′ . If a′ +ra′ −ˆi < j −i, we extract S[ˆ i..a ′ +ra′ ] = S[i..i +ra′ +a′ −ˆi]

in time linear to the length of this prefix using Lemma C.2. After that, we proceed to extract the remaining suffix using the next interval [x∗..y ∗] that contains non-extracted indices. If 

a′ + ra′ − ˆi > j − i, we are in the same case as 3.C.I as the phrase containing a′ must have length less than 1.5k due Corollary C.5. In each of the cases above, we either return a valid output directly, or delegate to Lemmas C.9 and C.10 to return a valid output in O(log nz ) plus time proportional to the length of an extracted prefix (or possibly O(log 2 nz ) if the entire string is extracted). The only exception is Case 3.C.II where we extract some prefix in time proportional to its length and then proceed to extract the remaining suffix. In this case, notice that we will remain in Case 3.C.II, and proceed to extract non-empty prefixes in time proportional to their length, or eventually land in another one of the subbases of Case 3. As long as we remain in Case 3.C.II, the running time can be charged on the lengths of the extracted prefixes. 54