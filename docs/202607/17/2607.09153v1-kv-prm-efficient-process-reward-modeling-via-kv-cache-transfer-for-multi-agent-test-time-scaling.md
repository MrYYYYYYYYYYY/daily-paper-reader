---
title: "KV-PRM: Efficient Process Reward Modeling via KV-Cache Transfer for Multi-Agent Test-Time Scaling"
title_zh: KV-PRM：通过KV缓存传输实现高效过程奖励建模用于多智能体测试时扩展
authors: "Peng Kuang, Haibo Jin, Xiaoyu Han, Yanli Wang, Xiaopeng Yuan, Ye Yu, Kaidi Xu, Haohan Wang"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09153v1"
tags: ["query:lpt"]
score: 8.0
evidence: 直接提出高效过程奖励模型用于类似RLHF的测试时扩展
tldr: 过程奖励模型（PRM）在测试时扩展中有效，但现有文本基PRM需重新编码整个轨迹，计算成本随长度二次增长，成为长上下文瓶颈。本文提出KV-PRM，通过直接利用LLM生成阶段的KV缓存，仅处理一个验证token，将成本降至线性。在多个数学推理基准上，KV-PRM匹配或超越文本PRM，计算FLOPs减少5000倍，延迟减少37倍，内存减少34倍。该工作大幅提升了PRM的效率，使其可扩展至长时多智能体场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1367, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 685, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 740, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 753, \"height\": 334, \"label\": \"Table\"}]"
motivation: 文本基PRM的计算成本随轨迹长度二次增长，严重限制其在长上下文多智能体系统中的应用。
method: KV-PRM直接读取LLM生成阶段产生的KV缓存，仅处理一个验证token进行奖励建模，将成本从O(L²)降至O(L)。
result: 在MATH、GSM8K、AIME上，KV-PRM在多种TTS方法下匹配或超越文本PRM，且FLOPs减少5000倍、延迟减少37倍、内存减少34倍。
conclusion: KV-PRM实现高效过程奖励建模，通过KV缓存传输大幅降低计算开销，为多智能体测试时扩展提供实用方案。
---

## 摘要
过程奖励模型（PRMs）已被证明在指导测试时扩展（TTS）方法方面非常有效，显著提升了基于LLM的多智能体系统的能力。然而，现有的PRMs是基于文本的：它们从头开始重新编码整个轨迹文本。在长时间的多智能体展开中，评分成本随序列长度L呈二次增长，造成严重的计算瓶颈，极大限制了PRMs在长上下文场景中的应用。为了解决这个问题，我们引入了KV-PRM，一种高效的过程奖励模型，它通过直接读取LLM生成阶段自然产生的KV缓存，消除了繁重的文本重新编码。通过针对预先存在的KV缓存处理单个“验证令牌”，KV-PRM将评分成本从O(L^2)降低到O(L)。我们正式证明了KV缓存包含严格大于文本的信息容量，并且对于下游奖励建模更高效。在MATH、GSM8K和AIME基准测试中，KV-PRM在各种TTS方法（如波束搜索、MCTS和加权投票）下匹配或严格优于文本PRMs，与基于文本的PRMs相比，评分FLOPs减少高达5000倍，延迟减少37倍，每序列内存占用减少34倍。

## Abstract
Process Reward Models (PRMs) have been proven to be highly effective in guiding test-time scaling (TTS) methods, which significantly boost the capabilities of LLM-based multi-agent systems. However, existing PRMs are text-based: they re-encode the entire trajectory text from scratch. In long multi-agent rollouts, the scoring cost, growing quadratically with respect to sequence length L, creates a severe computational bottleneck, severely limiting PRMs' application in long-context scenarios. To resolve this, we introduce KV-PRM, a highly efficient process reward model that eliminates the heavy text re-encoding by directly reading the KV cache produced naturally during the LLM's generation phase. By processing a single "verify token" against the pre-existing KV cache, KV-PRM reduces the scoring cost from O(L^2) to O(L). We formally prove that the KV cache contains strictly greater information capacity than text, and is more efficient for downstream reward modeling. Empirically, across the MATH, GSM8K, and AIME benchmarks, KV-PRM matches or strictly outperforms text-PRMs under various TTS methods such as Beam Search, MCTS, and Weighted Voting, with up to a 5,000x reduction in scoring FLOPs, a 37x reduction in latency, and a 34x reduction in per-sequence memory footprint compared to text-based PRMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：过程奖励模型（PRMs）在指导多智能体系统的测试时扩展（TTS）中至关重要，但现有文本基PRM在每次评分时需要重新编码整个轨迹文本。自注意力机制导致计算成本随序列长度 \(L\) 二次增长（\(O(L^2)\)），在长轨迹（数千token）时成为严重瓶颈，极大限制了PRM在长上下文多智能体推理中的应用。
- **意义**：若能将评分成本降至线性（\(O(L)\)），则可大幅提升TTS的可扩展性，使得PRM引导的搜索算法在长序列场景下实用化。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：直接复用LLM生成过程中自然产生的KV缓存，仅通过处理一个“验证令牌”（verify token）来输出奖励分数，避免从头重新编码文本。
- **关键技术细节**：
  - 架构：使用与生成器共享的基座模型（Qwen3系列），在评分时仅激活一个轻量级LoRA适配器（\(r=256, \alpha=32\)）。在验证token前附加一个特殊标记（如“？”），通过它的输出logits（对“+”和“-”取softmax）得到正面概率作为奖励分数。
  - 训练数据生成：利用MCTS对多智能体推理过程进行搜索，得到中间步骤的Q值（区间[-1,1]）作为监督标签。使用MSE损失训练LoRA适配器。
  - 公式化：评分FLOPs从文本PRM的 \(O(dL^2)\) 降至 \(O(dL)\)，其中 \(d\) 为隐藏维度，\(L\) 为序列长度。
- **算法流程**（Algorithm 1）：
  1. 对每个训练样本\((x, y)\)，用基座模型（适配器关闭）编码轨迹，得到KV缓存（不计算梯度）。
  2. 用适配器激活的模型，以KV缓存为输入，处理验证token一次，得到正面概率P(+)，计算MSE损失。
  3. 仅更新LoRA参数。

- **理论支撑**：论文证明KV缓存的信息容量是文本的\(\Omega(d/\log|V|)\)倍（Proposition 1），且单token读入即可捕获绝大部分可提取的奖励信息（Theorem 2），近似误差随读入深度指数衰减。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：MATH、GSM8K、AIME 2024、AIME 2025（数学推理基准），使用精确匹配准确率。
- **基准与对比方法**：三种基线：
  - No PRM（随机采样/多数投票）；
  - Policy Log-prob（使用生成器自身的token级对数概率，几乎零额外成本）；
  - Text-PRM（与KV-PRM相同架构和数据训练的文本基LoRA PRM，但每次评分需完整编码轨迹，\(O(dL^2)\)）。
- **搜索算法**：步级波束搜索（SBS）、蒙特卡洛树搜索（MCTS）、加权多数投票。
- **模型规模**：Qwen3-0.6B、4B、8B（禁用thinking mode）。
- **多智能体拓扑**：Sequential（Reader->Planner->Solver->Verifier）和Hierarchical（学科Agent->总结Agent）。
- **消融与分析实验**：
  - 不同大小Text-PRM（0.6B/4B/8B）与KV-PRM-8B对比（图2左）。
  - 不同验证token数量 \(k\) 的效果（图2右，验证指数衰减）。
  - 分层MAS拓扑（表3）。
  - KV Steering概念验证（表4）。

### 4. 资源与算力

- 论文中未明确提及训练所使用的GPU数量、总训练时长等具体算力资源。
- 仅在推理延迟测试中说明使用了**NVIDIA GH200 120GB GPU**，测量了KV-PRM与Text-PRM的每序列延迟和内存占用。
- 训练超参数：LoRA rank=256, α=32, dropout=0.05, max seq length=8192, global batch=2048, bf16精度。训练标签来自MCTS（64次rollout，每步4候选）。

### 5. 实验数量与充分性

- **实验数量**：涵盖了4个数据集、3个模型规模、3种搜索算法、多种搜索配置（如波束宽度、投票数），并加入了分层拓扑和验证token消融，总计约20+个主要对比情景。额外有梯度优化实验（KV Steering）。
- **充分性与公平性**：
  - 对比基线包括无PRM和文本PRM，且文本PRM使用相同LoRA处理和训练数据，确保了对比公平。
  - 实验覆盖了从简单（GSM8K）到困难（AIME）的数学推理，以及多种搜索复杂度。
  - 消融实验验证了理论预测（指数衰减、k=1近最优性）。
  - 不足：训练数据仅来自MATH训练集拆分，可能引入领域偏差；未与其他高效PRM方法（如分级PRM、隐空间PRM）对比；未在不同模型族（如Llama）上验证。

### 6. 论文的主要结论与发现

- **主要结论**：KV-PRM在匹配或超越文本PRM准确率的同时，将每步评分FLOPs降低约 \(L\) 倍（实际达 \(10^3\)-\(10^4\) 倍），延迟降低15-37倍，内存减少约34倍。
- **理论发现**：KV缓存信息容量严格高于文本；单token读入（\(k=1\)）在理论上接近最优，边际信息增益指数衰减。
- **实际应用**：KV-PRM可直接替换任何搜索算法中的PRM，无需修改算法。其可微分性质还支持梯度优化的KV Steering，在无搜索情况下提升准确率。

### 7. 优点

- **创新性**：首次利用KV缓存作为奖励模型输入，实现近乎零额外编码成本的评分，思路新颖。
- **高效性**：速度提升三个数量级，同时保持或提升准确性，实用价值高。
- **理论坚实**：从信息论和函数近似角度证明了方法的合理性和近最优性。
- **通用性**：适用于多种MAS拓扑和搜索方法，可作为即插即用模块。
- **扩展性**：可微分性开辟了新的推理时优化方向（如KV Steering）。

### 8. 不足与局限

- **依赖共享架构**：要求生成器和验证器共享相同基础模型架构（因需复用KV缓存），限制了对齐不同规模或不同模型族的灵活性。
- **理论假设**：线性表示假设（Assumption 1）和低秩奖励结构（Assumption 2）虽合理但未完全验证，在更复杂奖励下可能不成立。
- **实验覆盖**：仅测试数学推理任务，未在科学推理、代码生成等场景验证；仅使用Qwen3模型，未跨模型族验证。
- **训练数据偏差**：训练标签来自MCTS（基于是否精确匹配答案），可能偏向于特定搜索模式，泛化性存疑。
- **KV Steering初步**：仅概念验证，未与搜索方法结合，且梯度优化对KV缓存的操作可能破坏生成连贯性。
- **未与其他高效PRM方法对比**：如先用小模型评分筛选、或使用压缩表示的PRM，缺乏横向比较。

（完）
