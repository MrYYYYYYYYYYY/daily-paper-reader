---
title: Predictive Divergence Masks for LLM RL
title_zh: 用于LLM强化学习的预测性散度掩码
authors: "Xiangxin Zhou, Jiarui Yao, Penghui Qi, Bowen Ping, Jiaqi Tang, Haonan Wang, Tianyu Pang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10848v1"
tags: ["query:lpt"]
score: 8.0
evidence: 使用发散掩码改进PPO方向标准
tldr: 针对PPO风格RL中方向准则与邻近准则不一致的问题，提出预测性散度掩码，通过预测梯度步对信任域散度的增减来修正方向判定，推导了离散softmax策略的闭式解，并针对生产环境top-K视图设计两种轻量估计器。实验表明散度方向比采样比率更符合实际散度变化，在不同模型规模和精度下显著提升训练效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10848v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1689, \"height\": 994, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1669, \"height\": 966, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1557, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1566, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1634, \"height\": 1037, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1631, \"height\": 1034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1636, \"height\": 1031, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1633, \"height\": 1039, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10848v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1542, \"height\": 540, \"label\": \"Table\"}]"
motivation: PPO方向准则基于单样本比率符号，与散度变化可能不一致，导致掩码不准确。
method: 预测下一步梯度更新对信任域散度的增减，对离散softmax策略推导闭式解，并设计top-K估计器。
result: 散度方向预测与实际散度变化一致性优于采样比率，RL训练效果在多种设置下得到提升。
conclusion: 预测性散度掩码有效统一邻近与方向准则，提升LLM强化学习稳定性与性能。
---

## 摘要
用于大型语言模型（LLM）的强化学习通常依赖信任区域掩码来稳定离策略更新。主流的 PPO 风格方法使用采样词元的重要性比率来满足两个标准：接近性标准（判断策略是否偏离行为策略过远）和方向性标准（判断更新是否使其进一步远离）。近期工作 DPPO 通过用行为策略与训练策略之间的概率散度替换 PPO 的基于比率的检验，改进了接近性标准。然而，其方向性标准仍继承自 PPO。仅当采样词元的重要性比率偏离1时，该词元才会被掩码。我们观察到，这种基于比率的方向性标准是一个单样本代理，其符号可能与定义接近性标准的散度变化不一致。因此，我们提出预测性散度掩码，用于判断下一步策略梯度是会增加还是减少信任区域所使用的同一散度。对于 LLM 强化学习中使用的离散 softmax 策略，我们推导出了该预测的闭式表达式。由于生产环境中的推理引擎仅暴露词汇表的截断（top-K）视图，我们为该预测开发了两种轻量级的 top-K 估计器。详细分析表明，基于散度的方向比采样比率更能与散度的实际变化对齐，且由此产生的掩码在多种模型规模和精度设置下均能改善强化学习训练。

## Abstract
Reinforcement learning for large language models (LLMs) typically relies on trust-region masks to stabilize off-policy updates. The dominant PPO-style approach uses the sampled-token importance ratio for two criteria: a proximity criterion, which asks whether the policy has moved too far from the behavior policy, and a direction criterion, which asks whether the update pushes it farther away. Recent work DPPO improves the proximity criterion by replacing PPO's ratio-based test with a probability divergence between the behavior and training policies. However, its direction criterion is still inherited from PPO. A token can be masked only when the sampled-token importance ratio moves away from one. We observe that this ratio-based direction criterion is a single-sample proxy that can disagree in sign with the change of the divergence that defines the proximity criterion. We therefore propose the predictive divergence mask, which asks whether the next policy-gradient step will increase or decrease the same divergence used by the trust region. For the discrete softmax policies used in LLM RL, we derive this prediction in closed form. Because production rollout engines expose only a truncated (top-K) view of the vocabulary, we develop two lightweight top-$K$ estimators for this prediction. Detailed analysis shows the divergence-based direction is better aligned with the realized change of the divergence than the sampled ratio, and the resulting masks improve RL training across model scales and precision settings.

---

## 论文详细总结（自动生成）

# 论文总结：Predictive Divergence Masks for LLM RL

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：LLM 强化学习（RL）通常采用 PPO 风格的信任区域掩码来稳定离策略更新。掩码包含两个标准：**接近性准则**（判断当前策略是否偏离行为策略过远）和**方向性准则**（判断更新是否使策略进一步偏离）。  
- **已有方法的局限**：DPPO 通过用分布散度（如 KL 散度）替代 PPO 的基于单样本比率（|r-1|）的接近性准则，改善了信任区域的度量。但其方向性准则仍沿用 PPO 的 `sign(Â·(r-1))`——仅通过采样 token 的重要性比率判断更新是否“向外”。这种做法与定义接近性准则的全分布散度不一致，因为单样本比率无法反映整个 softmax 分布的耦合变化。  
- **核心问题**：方向性准则与接近性准则存在不匹配，导致掩码决策可能错误地保留或屏蔽更新，影响训练稳定性与效果。  
- **整体含义**：本文提出**预测性散度掩码**（Predictive Divergence Mask），使方向性准则与用于定义信任区域的散度保持一致，从而提升 LLM RL 的稳定性和性能。

## 2. 方法论

### 核心思想
- 不再依赖采样 token 的重要性比率符号，而是**预测下一步梯度更新是否会导致该 token 的散度（与行为策略之间的 KL 散度）增加或减少**。若更新会使散度增加（即进一步偏离信任区域），则屏蔽该 token；若会使散度减小，则保留。
- 对于离散 softmax 策略，该预测可导出**闭式解**，分解为局部项（等于比率符号）和全局项（捕捉 softmax 归一化耦合）。全局项是单样本比率无法观察到的。

### 关键技术细节
1. **方向导数的推导**：  
   令 π 为当前策略，μ 为行为策略，k 为采样词元。沿策略梯度方向（由梯度 ∇log π_k 导出）的单位步长下，DKL(μ∥π) 的方向导数为：  
   `˙D = (π_k - μ_k) + Σ_i π_i (μ_i - π_i)`  
   - 第一项是局部项，符号恰好等于 `sign(r-1)`（r=π_k/μ_k），与比率准则一致；  
   - 第二项是全局项，反映了整个分布的耦合效应。  
   最终的掩码依据 `sign(Â · ˙D)` 的正负决定：正则屏蔽，负则保留。

2. **适应生产环境 top-K 视图**：  
   由于推理引擎只暴露 top-K 概率（及尾部聚合质量），本文提出两种**轻量级估计器**计算 ˙D：
   - **聚合尾估计器**（Aggregated-tail）：将尾部视为一个聚合桶，用 `π_tail` 和 `μ_tail` 计算尾部贡献。  
   - **均匀尾估计器**（Uniform-tail）：将尾部质量均匀分配到 n-m 个未见词元上，尾部贡献被缩放 1/(n-m)，在大词表中几乎可忽略。  
   两种估计器差异很小，计算开销极低，仅需已有的 top-K 概率和尾部质量。

3. **算法流程**：  
   替代 DPPO-TopK-KL 中的方向性准则，得到新掩码 `M_pred`：  
   `M_pred = 0 if sign(Â·˙D) > 0 and D_KL(top-K) > δ, else 1`。  
   目标函数仍是掩码后的策略梯度 `∇θL = E[ Σ_t M_pred · r_t · Â_t · ∇log π(y_t|s_t) ]`。

## 3. 实验设计

- **数据集**：使用过滤后的 DAPO-Math-17k（约 13k 训练样本）。  
- **基准（Benchmark）**：AIME2024 和 AIME2025，每个问题采样 16 条响应，报告平均准确率（avg@16）。  
- **模型**：Qwen3-4B-Base、Qwen3-8B-Base、Qwen3-30B-A3B-Base（含两种低精度设置：FP8 仅 rollout、FP8 端到端）。  
- **对比方法**：  
  - **GRPO Clip-Higher**（ε_low=0.2, ε_high=0.28）作为比率裁剪基线。  
  - **DPPO-TopK-KL**（基于 top-K KL 接近性 + 原始比率方向性）作为主要基线。  
  - **DPPO-TopK-KL w/ Predictive Divergence Mask**（两种估计器：聚合尾和均匀尾）。  
- **核心实验**：在 δ=0.15 下比较四种方法在四个模型/精度配置上的训练曲线；还进行了 δ=0.05 的阈值敏感性分析。

## 4. 资源与算力

- **文中明确说明**：实验基于 VeRL 框架，使用 **4 个节点，每个节点 8 块 NVIDIA H20 GPU，共 32 块 GPU**。  
- 未提及具体训练时长或总步数对应的时长，但给出了训练步数范围（如 400 步/300 步）。FP8 设置用于加速训练并扩大数值差异以测试稳定性。

## 5. 实验数量与充分性

- **多模型尺度**：覆盖 4B、8B、30B（MoE）三种规模，包括标准 BF16 和 FP8 精度，共 4 个配置。  
- **多个阈值**：δ=0.15（主实验）和 δ=0.05（紧缩阈值）。  
- **消融分析**：在 token 级别（61 个独立种子）对比两种方向性准则的“不安全保留率”（kept-but-diverged 比例），统计了分歧 token 上的实际散度变化。  
- **实验充分性评价**：  
  - 实验设计较为全面：跨模型、精度、阈值，并配以 token 级分析验证机制合理性。  
  - 对比基线包括当前最接近的 DPPO-TopK-KL，分离了方向性准则改进的变量。  
  - 但缺乏与其他近期方法（如带有正则化的散度方法）的直接比较，也缺少在更多样化任务（如非数学推理）上的验证。总体充分但有一定局限性。

## 6. 主要结论与发现

1. **散度方向性准则优于比率方向性准则**：在 token 级分析中，预测性散度掩码的“不安全保留率”低于比率准则（34.2% vs 36.9%），且保留的更新中有更大比例是真正使散度收缩的修正性更新（65.1% vs 62.9%）。  
2. **训练稳定性与效果提升**：在四个模型/精度设置下，预测性散度掩码的准确率均优于 DPPO-TopK-KL，且训练曲线更稳定；GRPO Clip-Higher 在 30B 设置下崩溃。  
3. **两种估计器表现相近**：聚合尾和均匀尾估计器在训练中几乎无差别，说明对尾部建模不敏感。  
4. **对不同阈值鲁棒**：在严格阈值 δ=0.05 下，预测性散度掩码仍优于 DPPO-TopK-KL，显示出更好的超参数鲁棒性。

## 7. 优点

- **理论统一**：使方向性准则与接近性准则基于同一散度，消除不一致性，增强信任区域方法的自洽性。  
- **闭式可计算**：基于 softmax 策略梯度的方向导数有闭合形式，无需额外计算或近似梯度求解，易于实现。  
- **轻量适配生产环境**：仅利用 rollout 已有的 top-K 概率和尾部质量，不增加额外开销，可直接部署。  
- **实验验证扎实**：从宏观训练指标到微观 token 级行为分析，多维度证明方法有效。  
- **不引入新超参数**：直接复用现有的散度阈值 δ，无需调节额外参数。

## 8. 不足与局限

- **局部一阶近似**：预测的散度变化基于 token 级别的梯度方向，忽略批次内其他 token 梯度更新的相互影响，因此只是一个局部估计，可能存在偏差。  
- **实验任务单一**：仅在数学推理任务（AIME）上评估，未覆盖更多领域（如对话、代码、安全性），推广性有待验证。  
- **缺乏与其他先进方法对比**：未与近期提出的如 RVRPO、简单策略优化（SPO）、基于镜像下降的方法比较。  
- **对尾部建模的深入分析不足**：虽然两种估计器表现相近，但未在高散度/低 top-K 覆盖率的极端情况下测试尾部近似误差的影响。  
- **计算资源细节不够完整**：未报告每轮训练的具体耗时或 GPU 利用率，影响可复现性。

（完）
