---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 可解释的不确定性用于问答中的自适应检索与推理
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:lpt"]
score: 9.0
evidence: 利用来自内部表示的不确定性信号显式解决LLM幻觉问题
tldr: 大型语言模型在问答中易产生幻觉且缺乏透明度，现有检索增强生成（RAG）的触发策略不透明或计算成本高。本文提出基于模型内部表示的不确定性估计框架，通过单次前向传播从隐藏状态中区分知识不足与歧义/冲突两种不确定性，动态决定是否触发检索或进行额外推理。该方法提供了透明且高效的适应性问答机制，有助于构建可解释的用户工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 现有RAG决策依赖黑箱策略或多步提示，缺乏可解释性和效率，亟需一种基于显式不确定性信号的透明触发机制。
method: 利用LLM内部隐藏状态单次前向估计知识不足和知识歧义/冲突两种不确定性，据此自适应选择检索或推理路径。
result: 单次前向传播即可实现自适应决策，避免多步提示开销，提升系统透明性和效率。
conclusion: 提出一种透明、实用的适应性问答框架，通过分解不确定性信号为可解释用户工具提供支持。
---

## 摘要
大型语言模型（LLMs）在问答（QA）中表现出色，但仍容易产生幻觉且透明度有限。检索增强生成（RAG）可以提高事实性，但关于何时以及如何从外部资源检索的决策通常基于不透明的策略或计算效率低下的多步提示程序。我们提出了一种基于LLM内部表示显式信号的自适应QA不确定性感知框架。我们区分了知识不足和知识模糊或冲突，并在单次前向传递中从隐藏状态高效估计这些信号。这些信号指导系统行为：当知识不足时触发RAG，当模糊或冲突较高时应用额外推理。通过将自适应决策建立在分解且可高效估计的不确定性信号上，该方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释用户面向工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）在问答任务中表现优异，但存在幻觉、依赖不完整或过时的内部知识、以及缺乏透明度等缺陷。检索增强生成（RAG）虽能改善事实性，但关于何时及如何检索的决策往往基于不透明的策略（如隐式学习策略）或计算效率低下的多步提示流程（如Self-RAG）。现有方法无法区分不同来源的不确定性（知识不足 vs. 知识歧义/冲突），难以向用户提供可解释的决策依据。
- **研究动机**：论文主张在问答系统中将自适应决策建立在显式、可解释的不确定性信号上，而非黑箱策略。通过从LLM内部表示中高效估计两种不确定性——**知识不足**（模型缺乏所需信息）和**知识歧义/冲突**（存在多个相互竞争的候选答案），并据此触发不同的系统行为（检索或额外推理），从而实现透明且高效的适应性问答。
- **整体含义**：该框架为构建可解释的用户面向工具提供了基础，用户和系统设计者可以理解系统为何搜索或进一步推理，而非事后推断。

## 2. 论文提出的方法论

### 核心思想
- 利用LLM的**隐藏状态**（hidden states）在**单次前向传播**中同时估计两种不确定性信号，避免多轮采样或辅助模型。
- 基于阈值化的决策机制：知识不足高时触发检索增强生成（RAG）；知识歧义/冲突高时分配额外的测试时计算（如Chain-of-Thought或Self-Consistency）；两者都低时直接作答。

### 关键技术细节
1. **不确定性估计**：
   - **知识不足**：定义为事实在预训练语料中出现次数，次数越低表示知识越不足。训练一个轻量级回归探针（regression probe）从LLM隐藏状态预测该出现次数。
   - **知识歧义/冲突**：定义为不同版本事实的出现次数的熵（entropy），熵越高表示歧义/冲突越大。同样训练回归探针从隐藏状态预测该熵值。
2. **自适应决策机制**：
   - 输入问题 → LLM单次前向传播 → 提取隐藏状态 → 回归探针估计两个不确定性分数 → 与预设阈值比较 → 决定是否触发RAG、增加推理或直接回答。

### 算法流程（文字说明）
1. 接收用户问题。
2. 将问题输入LLM，获取最后一层隐藏状态。
3. 使用两个预训练的回归探针，分别从隐藏状态估计知识不足得分（预测的出现次数）和知识歧义得分（预测的熵）。
4. 若知识不足得分低于阈值（即预测出现次数少 → 知识不足高），则触发RAG：检索外部知识，并基于检索结果生成答案。
5. 若知识歧义得分高于阈值（即熵高 → 冲突大），则增加测试时计算（如链式思维推理或自我一致性）来解析候选答案。
6. 否则，直接由LLM生成答案。

## 3. 实验设计

- **数据集**：Natural Questions (NQ) 数据集（事实型问答基准）。
- **基础模型**：Llama-2-7b-chat。
- **对比方法**：
  - **LLM-only 基线**：仅使用LLM直接回答。
  - **Always-on RAG 基线**：对所有问题都进行检索增强生成。
  - **不确定性触发RAG（两种变体）**：
    - 基于语义熵（Semantic Entropy, SE）触发RAG。
    - 基于加权熵产生率（Weighted Entropy Production Rate, WEPR）触发RAG。
    - （注：论文在初步实验中使用了这些现成的不确定性指标来驱动决策，而框架本身计划通过探针从隐藏状态估计，但初步验证阶段使用了SE和WEPR。）
- **评估指标**：未明确提，但根据比较结果（准确率提升百分比）推断为答案准确率。
- **显著性检验**：McNemar's test。

## 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。
- 仅提及基础模型为Llama-2-7b-chat（7B参数），在NQ数据集上进行实验。训练回归探针和推理的开销较低（单次前向传播），但具体训练资源未报告。

## 5. 实验数量与充分性

- **实验数量**：仅在一个数据集（NQ）上进行了一组主要实验，比较了四种设置（LLM-only, Always-on RAG, SE-driven, WEPR-driven），且仅报告了总体准确率提升（+5.9%, +4.7%等）。未进行消融实验、多数据集验证、不同模型规模对比、不同探针配置对比等。
- **充分性与客观性**：
  - **不充分**：仅一个数据集、一个模型、单一任务类型（事实型问答）。未检验对知识冲突场景（如模糊/冲突问题）的单独效果，也未评估探针的估计精度。
  - **客观性**：采用了McNemar's检验，统计显著性声明是合理的，但实验设置较为初步。
  - **公平性**：对比了LLM-only和Always-on RAG两个合理基线，但未对比其他自适应检索方法（如Self-RAG、Adaptive-RAG等）。

## 6. 主要结论与发现

- 不确定性驱动的自适应问答显著优于LLM-only基线（+5.9% 基于SE，+4.7% 基于WEPR）和Always-on RAG基线（+3.3% 基于SE，+2.1% 基于WEPR），证明了该方向的潜力。
- 通过分解两种不确定性（知识不足 vs. 知识歧义/冲突），系统可以采取更精准的干预行为，提高可解释性和效率。
- 单次前向传播估计不确定性是可行的，避免了多步提示或重复采样的计算开销。

## 7. 优点

- **方法创新性**：首次系统性地提出区分两种不确定性（知识不足与知识冲突），并基于此设计自适应检索/推理机制，比单一阈值触发更细粒度。
- **可解释性**：决策直接与显式不确定性信号关联，易于向用户解释“为什么搜索”或“为什么推理”。
- **效率**：单次前向传播估计不确定性，无需额外采样或辅助模型，适合多步或智能体场景。
- **初步实证有效**：在标准数据集上验证了不确定性驱动策略优于无检索和恒定检索。
- **设计思想前瞻**：将可解释性作为设计原则，而非事后工具。

## 8. 不足与局限

- **实验覆盖不足**：仅使用NQ数据集和一个7B模型，未验证在其他数据集（如TriviaQA、WebQuestions）、其他模型（如更大规模LLM或不同架构）、其他任务类型（如多步推理、开放域生成）上的效果。
- **未验证核心探针**：论文提出了用回归探针从隐藏状态预测两种信号，但初步实验中实际使用了已有的SE和WEPR指标，而非所训练的探针。探针的有效性尚未通过实验验证。
- **阈值设定与鲁棒性**：未讨论阈值如何确定，不同阈值对性能的影响未知。
- **消融分析缺失**：没有单独分析每种不确定性信号驱动决策的贡献，也没有分析RAG和推理同时触发或顺序触发的情况。
- **计算资源未报告**：缺乏训练探针和模型推理的具体算力信息，难以评估可复现性。
- **实际部署挑战**：论文未讨论模型隐状态提取在实际API服务中的可行性（黑盒LLM可能不提供隐藏状态），以及探针的泛化能力。
- **局限性声明**：论文本身是短论文（3页），故内容较为浓缩，上述不足多源于篇幅限制。

（完）
