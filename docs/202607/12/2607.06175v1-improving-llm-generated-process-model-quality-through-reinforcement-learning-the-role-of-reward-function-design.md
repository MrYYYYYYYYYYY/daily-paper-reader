---
title: "Improving LLM-Generated Process Model Quality Through Reinforcement Learning: The Role of Reward Function Design"
title_zh: 通过强化学习提升LLM生成的过程模型质量：奖励函数设计的作用
authors: "Alexander Rombach, Chantale Lauer, Nijat Mehdiyev"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06175v1"
tags: ["query:lpt"]
score: 9.0
evidence: 针对LLM生成流程模型的RL奖励函数设计
tldr: LLM生成的BPMN流程模型受限于监督微调的数据模式。本文采用强化学习(RL)结合38项自动化质量指标设计奖励函数，系统研究了奖励函数设计对模型优化的影响。实验表明：RL显著提升语法和语用质量，保持语义保真度，输出变异性降低6倍以上；相等奖励加权优于针对性加权；设计选择与模型架构存在复杂交互。奖励组成是优化结果的关键决定因素，其影响与是否使用RL本身同等重要，可推广到多维度评估的结构化生成任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06175v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 1090, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1797, \"height\": 1071, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1793, \"height\": 792, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1791, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1790, \"height\": 629, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06175v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 657, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 621, \"height\": 499, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 795, \"height\": 696, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1800, \"height\": 650, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1804, \"height\": 833, \"label\": \"Table\"}]"
motivation: 探索如何设计奖励函数以优化LLM生成BPMN流程模型的多维质量（语法、语用、语义）。
method: 使用Group Sequence Policy Optimization，基于38个自动化质量指标设计奖励函数，训练Llama3.1 8B和Qwen2.5 14B模型。
result: RL显著提升语用和语法质量且保持语义，变异性降低6倍；相等奖励加权优于针对性加权；模型架构与设计交互复杂。
conclusion: 奖励函数组成是RL优化结果的主要决定因素，其影响与使用RL本身相当，可推广到结构化生成任务。
---

## 摘要
大语言模型（LLM）能够从自然语言描述中生成BPMN流程模型，但监督微调（SFT）将其输出质量限制在训练数据中的模式范围内。强化学习（RL）可以通过外部质量度量超越这一上限，但当质量是多维时，如何设计奖励函数尚待探索。我们针对基于RL的流程模型生成中的奖励函数设计进行了系统研究，使用基于自动评估框架（包含38个覆盖语法、语用和语义质量指标）的奖励，在48种配置下通过组序列策略优化训练了两个LLM族（Llama~3.1 8B, Qwen~2.5 14B）。主要发现有三点。首先，RL显著提升了语用和语法质量，同时保持了语义保真度，并将输出变异性降低了六倍以上。其次，等权奖励加权方式始终优于目标加权：强调特定维度不仅未能改善该维度，还可能导致模型陷入低质量模式。第三，设计选择与模型架构存在非平凡交互：无效惩罚对一个模型至关重要，但对另一个模型无关紧要；SFT初始化对一种架构不可或缺，但对另一种架构适得其反。这些结果表明，奖励组合是优化结果的主要决定因素，其影响与是否应用RL本身一样大。这些发现适用于任何基于多自动维度评估质量的结构化生成任务。我们在https://github.com/chlauer99/RL_for_process_modeling上发布实现和实验代码。

## Abstract
Large language models (LLMs) can generate BPMN process models from natural-language descriptions, yet supervised fine-tuning (SFT) limits their output quality to the patterns present in the training data. Reinforcement learning (RL) can optimize beyond this ceiling using external quality measures, but how the reward function should be designed when quality is multi-dimensional remains unexplored. We present a systematic investigation of reward function design for RL-based process model generation, training two LLM families (Llama~3.1 8B, Qwen~2.5 14B) under 48 configurations using Group Sequence Policy Optimization with rewards derived from an automated evaluation framework comprising 38 metrics across syntactic, pragmatic, and semantic quality. Three findings emerge. First, RL significantly improves pragmatic and syntactic quality while preserving semantic fidelity, reducing output variability by more than sixfold. Second, equal reward weighting consistently outperforms targeted weighting: emphasizing a specific dimension fails to improve it and can collapse the model into a low-quality mode. Third, design choices interact with model architecture in non-trivial ways: the invalidity penalty is essential for one model but irrelevant for the other, and SFT initialization is indispensable for one architecture but counterproductive for another. These results demonstrate that reward composition is a primary determinant of optimization outcomes, with effects as large as the decision to apply RL itself. The findings generalize to any structured generation task where quality is assessed along multiple automated dimensions. We release our implementation and experimental code at https://github.com/chlauer99/RL_for_process_modeling.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）可以生成业务流程模型（BPMN），但传统的监督微调（SFT）方法将模型输出质量限制在了训练数据模式的上限。强化学习（RL）虽然能利用外部质量指标突破此上限，但在“质量是多维度”这一前提下，**如何设计奖励函数**（尤其是如何组合和权衡不同维度的质量指标）这一关键问题尚未被系统性研究。

- **研究动机与背景**：
    - **业务背景**：企业需要大量BPMN模型来文档化、分析和自动化工作流，但专业建模人员不足，LLM提供了一种自动从自然语言生成模型的途径。
    - **技术背景**：SFT本质上是模仿训练数据，无法超越数据质量。而LLM生成的结构化输出（如BPMN模型）可以自动、客观地进行评估，非常适合使用RL进行优化。
    - **空白点**：现有的RL应用（尤其在数学推理和代码生成领域）通常使用单一维度的奖励（正确/错误），但流程模型的质量是内在多维的（语法、语用、语义），这三个目标可能相互冲突。如何平衡这些目标，以及奖励函数的设计选择如何与模型架构交互，是待解答的问题。

- **整体含义**：本文通过系统性实验证明，**奖励函数的设计（如何组成、加权、惩罚）是RL优化结果的首要决定因素**，其影响甚至与“是否使用RL”这个决策本身同等重要。这一发现对于任何依赖多维自动评估的结构化生成任务都具有普适性意义。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用**组序列策略优化（GSPO）**，将流程模型的多维质量评分直接作为RL的奖励信号，通过比较同一提示下多个候选输出的相对质量来更新策略，从而避免了对人类偏好数据或单独的价值网络的依赖。

- **核心技术细节**：
    1.  **任务定义**：文本到模型的生成任务，输入为系统指令+流程描述，输出为结构化的BPMN模型。定义了一个从原始输出序列到解析模型再到BPMN XML的完整链路，包含潜在的失败点（解析失败、翻译失败等）。
    2.  **训练管道**：
        - **第一阶段：监督微调（SFT）**：在配对数据上使用LoRA进行参数高效的微调，学习从文本到结构化模型的基本映射。**（注意：SFT在此处是实验变量，而非固定前提）**。
        - **第二阶段：强化学习（GSPO）**：在相同的训练数据上应用GSPO。
    3.  **RL算法（GSPO）**：
        - **候选生成**：对每个提示，从当前策略中采样K个候选输出。
        - **奖励计算**：使用奖励函数对每个候选输出进行评分。
        - **优势计算**：在组内对候选奖励进行归一化，得到相对优势（`A = (r - μ) / (σ + ε)`）。这降低了绝对奖励尺度的影响。
        - **策略更新**：使用裁剪后的替代目标（clipped surrogate objective）进行更新，确保稳定性。核心思想是提高相对于组内其他候选得分更高的输出序列的概率。
    4.  **奖励函数设计**：
        - 基于**BEF4LLM评估框架**，该框架提供38个自动化指标，覆盖三个质量维度：
            - **语法质量（Syntactic）**：模型是否符合BPMN语言结构规则。
            - **语用质量（Pragmatic）**：模型是否清晰、易于理解（通过复杂性度量）。
            - **语义质量（Semantic）**：模型是否忠实于原始文本描述（通过与参考模型的相似度度量）。
        - 奖励函数定义为：`R(w, p; ̃y)`。对于有效输出，`R = w_syn * r_syn + w_pra * r_pra + w_sem * r_sem`；对于无效输出，`R = p`（惩罚项）。
        - 实验设计了**六种奖励函数配置**，主要变化在于：
            - **权重策略**：`R1`（等权），`R3,R4,R5`（分别强调语法、语用、语义，比例为3:1:1）。
            - **无效惩罚**：`R1`（惩罚-1） vs. `R2`（无惩罚0）。
            - 还包括一个等权但实现路径不同的`R_avg`作为对照。

### 3. 实验设计

- **数据集**：
    - **训练集**：1,552个样本，每个样本包含系统提示、流程描述和对应的BPMN目标模型。数据来自公开数据集和合成数据，涵盖德语和英语。
    - **评估集**：105个样本的独立测试集，用于所有实验评估。所有样本都经过人工验证，确保了参考模型的质量。

- **Benchmark/基线**：**SFT-only模型**（即只进行第一阶段微调的模型）作为无RL的基线。

- **对比方法**：主要对比的是不同**奖励函数配置**（R1~R5，R_avg）、不同**模型初始化策略**（SFT后初始化 vs. 原始指令微调模型直接进行RL），以及是否使用**语法约束解码**。

### 4. 资源与算力

- **未明确说明**：论文没有提供具体的GPU型号、数量或训练总时长等硬性算力指标。仅提到SFT和GSPO阶段均在bfloat16精度下进行，并使用了LoRA（秩8和秩16）来降低资源消耗。模型规模分别为8B（Llama）和14B（Qwen），是中小型模型。

### 5. 实验数量与充分性

- **实验数量**：非常充分。论文共进行了**48种**实验配置（2个模型 × 6种奖励函数 × 2种初始化策略 × 2种解码约束），并对每种配置在105个样本上评估。此外，还包含SFT基线和其他补充分析。

- **充分性与公平性**：
    - **充分性**：实验覆盖了核心研究问题（RQ1-RQ3），系统地变化了奖励函数设计、初始化和解码的关键维度。统计分析严谨，使用了**配对置换检验**和**Bonferroni校正**，确保了统计结论的可靠性。
    - **客观公平**：所有的评估都基于自动化的BEF4LLM框架，避免了主观偏差。SFT和GSPO使用**完全相同的数据集**，确保性能差异源于训练方法和奖励信号。基线（SFT-only）是合理的。实验公平性总体很高。

### 6. 论文的主要结论与发现

- **RQ1：RL是否优于SFT？**
    - **是**。GSPO显著且一致地提升了两个模型的**语用质量**（提升>0.11），并显著提升了**语法质量**（对Llama提升大，对Qwen提升小）。
    - **语义质量**基本保持不变（Llama略有提升，Qwen无显著变化）。
    - 最显著的效果是**输出变异性大幅降低**（语用质量的标准差降低了6倍以上），模型生成更加稳定、可靠。

- **RQ2：奖励函数如何影响质量权衡？**
    - **等权加权（R1）是最稳健的默认选择**。有针对性的加权（R3-R5, 3:1:1比例）**不仅未能改善目标维度，反而可能导致模型“策略崩溃”**（如Llama在强调语法时，语法得分反而从0.926骤降至0.662）。
    - **无效惩罚的作用是模型依赖的**。对于Qwen，移除惩罚（R2）会导致模型收敛到一个单一、低质量的模板（模板坍塌），产生所有输入都雷同的输出。对于Llama，SFT后模型已经很少输出无效结果，惩罚作用不大。惩罚的“隐性正则化”作用对维护输出多样性至关重要。

- **RQ3：SFT初始化是否必要？**
    - **是架构依赖的**。对于8B的Llama，**SFT初始化是不可或缺的**，没有它RL会陷入低质量平衡。对于14B的Qwen，**SFT是多余的**，甚至会降低最终的**语义质量**，因为它限制了模型的探索空间。这表明SFT并非总是有益的。

- **核心发现总结**：奖励函数的组成（权重、惩罚）是RL优化结果的首要决定因素，其影响与是否应用RL本身一样大。设计选择（权重、惩罚、初始化）与模型架构之间存在复杂、非平凡的交互，无法直接迁移。

### 7. 优点

- **系统性**：这是首个对RL奖励函数在多维结构化生成任务中的设计进行系统性实证研究的工作。实验设计极其全面，覆盖了核心设计变量。
- **实践指导性**：论文给出了非常具体、可操作的实践指南：如优先选择等权加权、根据模型的行为调整无效惩罚、根据模型容量判断是否需要SFT初始化。
- **方法论严谨**：使用GSPO而非传统的PPO，更符合序列级评估的特性。统计检验方法（配对置换检验+Bonferroni校正）科学严谨。
- **揭示深层次机制**：不仅报告结果，还深入分析了“优化可达性”、“策略坍塌”、“模板坍塌”等背后的动力学机制，并提出了“无效惩罚的隐性正则化作用”这一深刻的洞察。

### 8. 不足与局限

- **实验覆盖有限**：只使用了105个测试样本，且每个配置只生成单次输出，难以评估内部方差和对微小效果的敏感性。
- **模型规模有限**：只研究了8B和14B的模型，结论向更大模型（如70B+）或不同架构的泛化性有待验证。
- **权重设计简单**：加权方案仅使用了固定的“3:1:1”比例。对于某些模型（如Llama）导致完全失效，但更温和的加权效果未知。
- **语义质量评估局限**：语义质量通过与一个“标准答案”的参考模型比较来评估，这可能会低估那些有效但结构不同的新颖设计的语义保真度。
- **超参数探索有限**：RL训练只进行了一个epoch，使用了固定的LoRA参数。更长时间的RL训练以及与其他正则化技术的结合效果未知。
- **研究问题深度**：如论文自身所承认，对该领域更细致的机制（如“优化可达性差异的来源”、“模板坍塌的量化”等）的探讨仍然有限。

（完）
