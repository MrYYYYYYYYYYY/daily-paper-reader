---
title: Which Pairs to Compare for LLM Post-Training?
title_zh: LLM后训练中应比较哪些配对？
authors: "Jiangze Han, Vineet Goyal, Will Ma"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19607v1"
tags: ["query:lpt"]
score: 9.0
evidence: 从人类反馈中学习的奖励建模
tldr: 偏好后训练中，人类标注成本远高于生成回答，因此需从大量候选比较对中选取最具信息量的进行标注。本文将比较选择形式化为采样设计问题，针对DPO训练推导出策略最优性差距的上下界，揭示比较选择通过信息矩阵影响参数估计误差和策略次优性。基于这一理论提出优化准则并设计实用采样方法，实验表明其在样本效率上显著优于常见启发式策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19607v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19607v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1436, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19607v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 552, \"label\": \"Figure\"}]"
motivation: 偏好标注成本高昂，如何用有限预算选择最有价值的比较对以提升后训练效果。
method: 将比较选择建模为采样设计问题，推导DPO最优性差距的界，得到依赖信息矩阵的优化准则，并设计实用采样策略。
result: 提出比常见启发式更高效的采样设计，在合成数据和语言模型基准上提升样本效率。
conclusion: 分析表明比较对选择通过信息矩阵影响DPO训练效果，为预算受限下的偏好数据收集提供理论指导和实用方法。
---

## 摘要
基于偏好的后训练已成为对齐语言模型的核心范式。常见的数据收集策略是为每个提示生成少量补全，并标注由此产生的比较配对。然而，人工偏好标签往往比生成额外补全昂贵得多，这暗示了同一标注预算的不同使用方式：生成更大的补全集，但只标注最具信息量的比较配对。本文研究了在基于偏好的后训练中应比较哪些配对。我们将比较策划形式化为一个抽样设计问题，并根据基于偏好的后训练目标下的最终策略质量来评估设计。我们针对直接偏好优化（DPO）实例化了该框架，分析了所选标注配对如何通过DPO训练传播到下游策略性能。我们的主要结果为DPO训练策略的后训练最优性差距提供了匹配的上界和下界。这些界限表明，比较选择通过一个依赖于设计的信息矩阵影响下游性能，该矩阵将标签分配与参数估计误差和策略次优性联系起来。这为预算有限的比较策划提供了明确的优化准则，并激发了从大型生成补全池中选择信息量大的配对的实际抽样设计。在合成设置和语言模型后训练基准上的实验表明，所提出的设计在样本效率上始终优于常见的比较选择启发式方法。

## Abstract
Preference-based post-training has become a central paradigm for aligning language models. A common data-collection strategy is to generate a small set of completions for each prompt and label the resulting comparison pairs. However, human preference labels are often much more expensive than generating additional completions, suggesting a different use of the same labeling budget: generate a larger pool of completions, but label only the most informative comparison pairs. This paper studies which pairs should be compared in preference-based post-training. We formulate comparison curation as a sampling-design problem and evaluate designs by the quality of the final policy under the preference-based post-training objective. We instantiate this framework for Direct Preference Optimization (DPO), analyzing how the choice of labeled pairs propagates through DPO training to downstream policy performance. Our main results provide matching upper and lower bounds on the post-training optimality gap of the DPO-trained policy. The bounds show that comparison selection affects downstream performance through a single design-dependent information matrix, which links label allocation to parameter estimation error and policy suboptimality. This yields an explicit optimization criterion for budgeted comparison curation and motivates practical sampling designs for selecting informative pairs from large generated completion pools. Experiments on synthetic settings and language-model post-training benchmarks show that the proposed designs consistently improve sample efficiency over common comparison-selection heuristics.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在基于偏好的语言模型后训练（如RLHF/DPO）中，人类偏好标注成本远高于模型自动生成补全。传统策略是为每个提示生成少量补全并标注所有或随机配对，浪费了昂贵标签。
- **核心问题**：在固定标注预算下，如何从大量候选比较对中筛选出最具信息量的配对进行标注，以最大化提升最终后训练策略（DPO策略）在KL正则化RLHF目标下的性能。
- **整体含义**：将比较选择建模为抽样设计问题，通过理论推导确定关键优化准则，为预算受限下的偏好数据收集提供可操作的采样策略，显著提升样本效率。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：比较选择对DPO后训练性能的影响可以归结为一个依赖于设计的信息矩阵 `Σ_D(θ)` 与策略Fisher信息矩阵 `I(θ)` 的迹 `tr(I(θ) Σ_D(θ)^†)`。该迹同时控制着DPO最优性差距的上界和下界，因此作为比较选择的优化目标。
- **关键技术细节**：
  - **建模**：对于每个提示，首先生成候选补全集；然后定义采样设计 `D`，即所有候选比较对上的概率分布；预算 `n` 个比较对从 `D` 中i.i.d.采样并标注。
  - **分析框架**：证明RLHF最优性差距 `J(π*) - J(π_θ)` 与参数误差 `(θ-θ*)ᵀI(θ*)(θ-θ*)` 局部二次等价（Proposition 1）。
  - **DPO上界**：利用DPO损失Hessian的下界（依赖于设计协方差矩阵）和score的协方差控制，得到 `J(π*) - J(ˆπ_n) ≤ (C/n) tr(I(θ*) Σ_D(θ*)^†)`（Theorem 3）。
  - **信息论下界**：应用Van Trees不等式，得到任意估计器下的贝叶斯风险下界 `≥ (C'/n) E[tr(I(θ*) Σ_D(θ*)^†)]`（Theorem 4）。
  - **实用设计**：由于 `θ*` 未知，使用参考策略参数 `θ0` 作为代理，求解 `min_D tr(I(θ0) Σ_D(θ0)^†)`，并证明其性能与Oracle设计至多差常数因子（Theorem 2）。
- **算法流程**（文字描述）：
  1. 对每个提示生成候选补全集。
  2. 计算参考策略下每个候选比较对的灵敏度向量 `g(e;θ0)` 和近似的Fisher矩阵 `I(θ0)`。
  3. 求解迹最小化问题：`min_D tr(I(θ0) Σ_D(θ0)^†)`，得到最优设计分布（Frank-Wolfe等算法）。
  4. 按该分布无放回抽取 `n` 个比较对，收集人工偏好标签。
  5. 在标注数据集上执行标准DPO训练，获得最终策略。

## 3. 实验设计：数据集/场景、基准、对比方法
- **合成实验**：
  - 场景1：**Tabular设置**。每个补全为独立项目，参考策略非均匀，真实奖励低信噪比。对比方法：Oracle设计、Plug-in设计、均匀采样、π0加权启发式。
  - 场景2：**线性上下文设置**。使用软最大化策略，嵌入包括提示、动作及其交互。对比方法同上。
  - 评估指标：RLHF最优性差距 `J(π*)-J(ˆπ_n)`。
- **IMDb实验**（真实LLM后训练）：
  - 使用GPT2-Large，SFT后作为参考策略。代理奖励为情感分类器。
  - **Prompt选择任务**：从1000个提示池中选择175个比较（预算小）。
  - **Response选择任务**：175个提示，每个提示生成8个候选，选一个比较对。
  - 对比方法：D*设计 vs. 基准（首次N个比较；固定对）。
  - 评估指标：Reward-KL前沿（多个β下的最终检查点）。
- **Anthropic HH实验**：
  - 使用Pythia-2.8B，在HH-RLHF数据集上SFT后做DPO。候选池包含160,800个现成比较对。
  - 预算为50%和60%池子（N=80,400和96,480）。
  - 对比方法：D*设计（按优化权重无放回采样） vs. 基准（前N个比较）。
  - 评估：使用GPT-4.1作为自动裁判，比较生成响应与HH chosen响应，报告胜率（在0.25、0.7、1.0温度下）。

## 4. 资源与算力
- **IMDb实验**：使用H100 GPU，每个DPO运行数分钟至约1小时（取决于设置）；D*构造和评估轻量。未说明总GPU数或总时长。
- **Anthropic HH实验**：D*构造和响应生成用1块H100；每个Pythia-2.8B DPO微调用4块H100，约1小时。GPT-4.1评估通过API，无额外GPU。
- **总体**：未详细报告总计算量，但实验规模适中，算力需求合理。

## 5. 实验数量与充分性
- **实验数量**：
  - 合成实验：重复Monte Carlo多次（图1显示均值与变异性，未明确次数）。
  - IMDb实验：Prompt selection 30次独立运行，Response selection 80次独立运行。误差棒显示标准误。
  - Anthropic HH实验：每个预算和温度条件一次运行（或少量重复？未明确）。提供两个预算、三个温度的胜率图。
- **充分性**：实验覆盖从简化模型到真实LLM场景，对比了必要基线（均匀、加权启发式），验证了理论预测。但仅使用GPT2-Large和Pythia-2.8B（中等规模），未在更大模型（如7B/13B）上验证。Anthropic HH未报告多次重复的误差。总体而言较充分但有一定扩展空间。

## 6. 论文的主要结论与发现
- 比较选择对DPO后训练的影响完全由一个设计依赖的信息矩阵 `Σ_D(θ)` 与Fisher矩阵 `I(θ)` 的迹刻画。
- DPO最优性差距的上下界匹配，表明 `tr(I(θ*) Σ_D(θ*)^†)` 是自然的设计准则。
- 使用参考策略参数 `θ0` 的Plug-in设计可以近似Oracle设计，且性能损失有界。
- 在合成和真实实验中，基于该准则的采样设计（D*）一致显著优于均匀采样和基于参考策略概率的启发式，样本效率更高。
- 在IMDb实验中，D*训练的策略在Reward-KL前沿上更优；在Anthropic HH实验中，D*训练的策略胜率更高（使用GPT-4.1评判）。

## 7. 优点：方法或实验设计上的亮点
- **理论完备**：提供了匹配的有限样本上界和信息论下界，揭示了比较选择影响RLHF性能的机制，而非仅凭直觉。
- **准则简洁可解**：优化目标 `tr(I(θ0) Σ_D(θ0)^†)` 可计算，并适合用Frank-Wolfe等算法求解连续设计问题。
- **实用有效**：仅需参考策略参数（已有），无需标注数据或自适应查询，适合离线批标注流程。
- **实验设计覆盖多样**：合成实验验证理论机制，IMDb实验用代理奖励评估Reward-KL权衡，Anthropic HH用GPT-4评判避免奖励偏差，层层递进。
- **控制变量公平**：D*与基准使用相同标注预算，仅比较选择策略不同。

## 8. 不足与局限
- **理论假设较强**：依赖可识别性、实值性、覆盖性、光滑性等假设，在大型神经网络中可能仅近似成立。
- **仅考虑离线随机设计**：设计在生成候选补全后固定，不根据已收集标签自适应调整（与在线主动学习对比）。
- **实验规模有限**：模型仅到2.8B（Pythia），未在7B/13B/70B等更大模型上验证；Anthropic HH实验缺少多次重复的误差条，统计可靠性待加强。
- **评估依赖代理**：IMDb使用情感分类器作为奖励，可能与真实人类偏好有偏差；Anthropic HH用GPT-4.1评判可能引入模型偏见。
- **未考虑标注噪声**：假设标签遵循Bradley–Terry模型，实际中标签可能有系统偏差或噪声。
- **计算设计成本**：求解迹最小化需要对每个候选比较对计算灵敏度，在极大候选池中可能计算开销大（尤其高维特征时）。

（完）
