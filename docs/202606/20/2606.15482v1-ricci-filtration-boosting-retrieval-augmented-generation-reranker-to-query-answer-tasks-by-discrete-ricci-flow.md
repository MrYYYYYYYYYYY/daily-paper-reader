---
title: "Ricci-Filtration: Boosting Retrieval-Augmented Generation Reranker to Query-Answer Tasks by Discrete Ricci Flow"
title_zh: 里奇过滤：通过离散里奇流提升检索增强生成重排序器在问答任务中的表现
authors: "Tian Qin, Wei-Min Huang"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15482v1"
tags: ["query:lpt"]
score: 8.0
evidence: 使用离散Ricci流这一数学形式化方法增强RAG重排序器
tldr: 针对检索增强生成中检索文档存在噪声的问题，受离散里奇流可分离簇结构的启发，提出几何驱动的重排增强方法Ricci-Filtration。将查询与初始检索块建模为加权图，利用离散曲率和里奇流评估每个块的结构重要性，过滤具有大权重和负曲率的噪声块。理论证明归一化离散里奇流可检测社区结构。实验在多项指标上超越基线重排方法，消融研究显示其在不同架构下的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15482v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1784, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1787, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1757, \"height\": 252, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15482v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1762, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1773, \"height\": 648, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1761, \"height\": 614, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1767, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1125, \"height\": 206, \"label\": \"Table\"}]"
motivation: 检索增强生成中，初始检索文档常含噪声，影响生成质量，需有效过滤不相关或误导性文档。
method: 将查询和初始检索块构建为加权图，计算离散里奇曲率并通过里奇流调整边权重，依据曲率选择性过滤噪声块，再由重排器处理剩余块。
result: 在准确率、精确率、召回率和F1分数上优于多种基线重排方法，消融实验验证了不同设置下的鲁棒性。
conclusion: 基于几何曲率与里奇流过滤噪声可有效提升检索增强生成的重排效果，且框架泛化性强。
---

## 摘要
里奇流是一种曲率引导的扩散过程，通过收缩高正曲率区域并扩展负曲率区域来变形空间。类似地，加权图上的离散里奇流通过收缩正里奇曲率的边和拉伸负里奇曲率的边来修改边权重，有效增加了簇间的分离度。受这两项奠基性工作的启发，我们提出了一种基于几何的RAG重排序器增强方法，称为里奇过滤。通过将输入查询和初始检索到的块建模为一个网络，其中输入查询和块作为节点，基于嵌入的成对关系定义初始图，里奇过滤利用离散曲率和里奇流来评估每个块相对于用户查询的结构重要性。该系统首先根据块相对于查询的几何曲率过滤初始块；然后，重排序器处理剩余的块以增强生成性能。我们从理论上证明，归一化的离散里奇流可以通过识别边权重的不同渐近行为来检测社区结构。这支持移除相对于查询节点具有大权重和负里奇曲率的“噪声”文档块。大量实验证实，里奇过滤在准确率、精确率、召回率和F1分数上优于几种基线重排序方法。此外，消融研究表明，里奇过滤在各种设置下通常优于基线，突出了该框架在不同架构下的鲁棒性。

## Abstract
Ricci flow is a curvature-guided diffusion process that deforms space by shrinking regions of high positive curvature and expanding those with negative curvature. Similarly, discrete Ricci flow on weighted graphs modifies edge weights by shrinking edges with positive Ricci curvature and stretching those with negative Ricci curvature, effectively increasing the separation between clusters. Inspired by these two cornerstone works, we propose a geometry-based RAG reranker enhancement procedure called Ricci-Filtration. By modeling the input query and initial retrieved chunks as a network, where the input query and chunks serve as nodes and embedding-based pairwise relations define an initial graph, Ricci-Filtration leverages discrete curvature and Ricci flow to evaluate the structural importance of each chunk with respect to the user query. The system first filters the initial chunks based on their geometric curvature relative to the query; then, a reranker processes the remaining chunks to enhance generative performance. We theoretically prove that normalized discrete Ricci flow can detect community structures by identifying distinct asymptotic behaviors in edge weights. This supports the removal of ``noisy'' document chunks characterized by large weights and negative Ricci curvature relative to the query node. Extensive experiments confirm that Ricci-Filtration outperforms several baseline reranking methods in accuracy, precision, recall, and F1 scores. Furthermore, ablation studies demonstrate that the Ricci-Filtration generally outperforms the baseline under various settings, highlighting the framework's robustness across different architectures.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：检索增强生成（RAG）系统中，初始检索常包含大量无关文本块，尤其在问答任务中，这些噪声会分散大语言模型（LLM）的注意力，导致幻觉和生成质量下降。传统重排序器（如交叉编码器、LLM重排序）虽可部分缓解，但存在计算成本高、需固定参数 k 等局限。
- **整体含义**：论文提出一种基于离散里奇流（Discrete Ricci Flow）的几何增强重排序方法——Ricci-Filtration，通过建模查询与文本块之间的结构关系，利用曲率引导的边权重演化自动过滤噪声块，从而提升RAG重排序的准确性和鲁棒性。

---

### 2. 论文提出的方法论

- **核心思想**：将输入查询和初始检索到的文本块视为一个加权图的节点，基于嵌入的余弦不相似度构建初始图拓扑，并强制所有查询-块边存在。利用离散奥利维尔-里奇曲率（Ollivier-Ricci curvature）评估每条边的“几何重要性”，然后通过迭代的归一化离散里奇流演化边权重。正曲率边（社区内）收缩，负曲率边（社区间）拉伸。最后根据查询边的归一化权重是否高于阈值 η=1 进行“手术”，过滤掉权重大的负曲率边（即噪声块）。剩余块再送入标准的交叉编码器重排序器。
- **关键技术细节**：
  - 图构建：节点包含查询和 top-k 个候选块；边通过余弦不相似度 rij 的百分位数阈值 τ 决定是否连接（非查询-块边需 rij ≥ τ），所有查询-块边强制连接。初始边权重设为1。
  - 离散里奇曲率计算：依据公式 κxy = 1 − W(m_x^{α,p}, m_y^{α,p})/d(x,y)，其中 W 为 Wasserstein 距离，m_x^{α,p} 为概率质量分布（α=0.5, p=2）。
  - 迭代更新：w_{xy}^{(i+1)} = (1 − ε κ_{xy}^{(i)}) w_{xy}^{(i)}，ε 为步长。每次迭代后归一化边权重使其均值为1。迭代次数 M=20。
  - 过滤：对查询节点，保留归一化边权重 ≤1 的块，丢弃 >1 的块。
- **理论支持**：定理3.1证明了在特定对称图族 G(a,b) 上，归一化离散里奇流可使社区内边权重渐近衰减快于社区间边（α=0, p=0 情况）。命题3.2对实际参数 α=1/2, p=2 给出有限时间分离保证。

---

### 3. 实验设计

- **数据集**：单跳QA：SQuAD2.0（1000问）、TriviaQA（100问）；多跳QA：HotpotQA（1000问、“硬桥接”子集）、MultiHop-RAG（2556问）、MuSiQue（1000问，仅在消融中）。
- **基准方法**：
  - No Reranker：直接取top-5相似块送入生成器。
  - Cross-Encoder Reranker：使用 bge-reranker-base（主要）或 MS-MARCO-MiniLM 系列，对top-20候选重排序后取top-5。
  - LLM-based Reranker：使用 gpt-4o-mini 进行点式评分（0-10），取top-5。
- **生成模型**：Llama-3.1-8B-Instruct 和 gpt-4o-mini。
- **评价指标**：准确率（Acc）、精确率（P）、召回率（R）、F1分数（部分数据集仅报告准确率）。
- **对比方式**：Ricci-Filtration 从同一top-20候选池中先进行几何过滤，再使用与 Cross-Encoder 相同的重排序器和生成器，以保证下游条件一致。

---

### 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量、训练时长或具体硬件配置。只提到推理时使用了 OpenAI 的 text-embedding-3-small 嵌入模型和 Llama-3.1-8B-Instruct（本地或 API），以及 gpt-4o-mini 的 API 调用。Ricci-Filtration 的迭代计算在 CPU 上完成（使用 Python CVXPY 求解 Wasserstein 距离），其时间开销约为交叉编码器的 6-8 倍（附录 H 表6：单跳约8-9秒，多跳约12秒）。总体而言，论文侧重方法验证，未提供详细的训练成本。

---

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：在 SQuAD2.0、HotpotQA、MultiHop-RAG 三个数据集上，每个数据集报告了两种生成模型的指标（表1、表2），共约6组对比。
  - 消融实验（表3、表4）：在 TriviaQA 和 MuSiQue 上，分别探究了节点分布参数 p（1,2,5）、图构建阈值 τ（25%,50%,75%）、迭代次数 M（1,10,20）、嵌入模型（text-embedding-3-small vs text-embedding-ada-002）、重排序模型（bge-reranker-base vs MS-MARCO-MiniLM-*）的影响，每组约10-12种配置。
  - 附加实验：K-means 过滤比较（附录F）、时间效率（附录H）。
- **充分性与公平性**：
  - 较好的方面：覆盖了多种类型的QA任务（单跳、多跳、含不可回答问题），并评估了不同生成器下的鲁棒性；消融实验较全面，揭示了各超参数的作用；与 K-means 对比验证了图几何过滤的额外价值。
  - 局限性：HotpotQA 上的性能弱于基准，作者承认在多跳推理中当前方法可能保留过多噪声；MuSiQue 上也未超越 Cross-Encoder（表4）。此外，LLM-based Reranker 仅使用 gpt-4o-mini 一种模型，且点式评分方式的优势有限，可能低估了该基线。所有实验均基于同一随机种子或未明确说明重复次数，缺乏方差统计（除时间外）。

---

### 6. 论文的主要结论与发现

- 在大多数数据集上，Ricci-Filtration 优于 No Reranker、Cross-Encoder 和 LLM-based Reranker 基线，尤其在 SQuAD2.0（提升约5个百分点）和 MultiHop-RAG 的空查询上（从60.8%提升至83.39%）。
- 在 HotpotQA 上表现较弱，提示该方法在多跳推理中仍需改进。
- 消融表明：迭代次数 ≥10、阈值 τ=50%、嵌入模型质量对性能至关重要；该方法对重排序器选择不敏感，凸显了框架的鲁棒性。
- 理论分析（定理3.1、命题3.2）为边权重分离提供了支撑，尽管实际图结构可能偏离理想对称图。

---

### 7. 优点

- **创新性强**：首次将离散里奇流引入RAG重排序，从几何直观出发解决噪声过滤问题，思路新颖。
- **可解释性**：基于曲率的过滤具有明确的几何意义（社区内 vs 社区间分离），非黑箱。
- **灵活性高**：LLM-无关的过滤步骤，可即插即用到现有RAG管道；不需要额外训练或微调，仅需预计算嵌入。
- **鲁棒性**：在多个嵌入模型、重排序模型和生成器下均表现出稳定的改进（除多跳场景外）。
- **理论支撑**：提供了归一化离散里奇流检测社区结构的数学证明（尽管针对特定图族），增强了方法的可信度。

---

### 8. 不足与局限

- **多跳推理能力弱**：在 HotpotQA 和 MuSiQue 上表现不佳，可能因为当前过滤策略过于宽松，保留了过多非关键文本块，误导多步推理。
- **时间复杂度高**：每次查询需运行20次 Wasserstein 距离求解（可通过 CVXPY 优化但仍有瓶颈），实际延迟约8-12秒，远高于交叉编码器（1-4秒），不适合实时场景。
- **图构建依赖嵌入质量**：初始图拓扑由余弦不相似度阈值决定，若嵌入空间不精确，滤波效果会严重下降（表3中 text-embedding-ada-002 明显差于 text-embedding-3-small）。
- **超参数选择启发式**：迭代次数 M=20、手术阈值 η=1 为实验经验值，缺乏通用准则；不同数据集可能需要调参。
- **基线比较不完全公平**：LLM-based Reranker 仅用 gpt-4o-mini 且为点式评分，若使用更强模型或列表式评分，结果可能提升；Cross-Encoder 基线未针对不同 k 值调优（统一 k=5），而 Ricci-Filtration 的剩余块数动态变化（约10-17个），未完全对齐 k。
- **实验覆盖不全**：未在更大规模数据集（如 Natural Questions）或实际产业级管道上验证；未报告不同数据域（如代码、医疗）的表现。
- **伦理与环境考量**：论文指出方法可能放大语料偏见，并增加推理能耗，但未提供具体缓解策略或能效分析。

（完）
