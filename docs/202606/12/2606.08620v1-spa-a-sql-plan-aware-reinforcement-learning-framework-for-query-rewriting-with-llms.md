---
title: "SPA: A SQL-Plan-Aware Reinforcement Learning Framework for Query Rewriting with LLMs"
title_zh: SPA：一种面向大语言模型查询重写的SQL计划感知强化学习框架
authors: "Xinyi Huang, Zhengjie Miao"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08620v1"
tags: ["query:lpt"]
score: 8.0
evidence: 使用强化学习结合计划感知奖励进行基于LLM的SQL查询重写
tldr: SQL查询重写是提升数据库性能的关键技术，但现有方法受限于规则或LLM产生等价却无实际加速的重写。SPA提出基于物理执行反馈的强化学习框架，将重写建模为策略优化，扩展GRPO引入语义等价、计划差异和运行时加速等奖励，并设计概率门控自适应奖励塑形与策略自改进。在IID和OOD负载上，SPA显著降低端到端运行时，减少有害重写，改善尾延迟，证明了利用执行反馈训练LLM的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08620v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 1358}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1823, \"height\": 813}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 664, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 827, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1709, \"height\": 832}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08620v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 340}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 818, \"height\": 273}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1764, \"height\": 1195}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 555}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 811}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1837, \"height\": 462}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 888, \"height\": 173}]"
motivation: 现有查询重写方法（规则或LLM）无法有效生成真正加速的重写，需要结合物理执行反馈的智能框架。
method: SPA将重写作为策略优化，扩展GRPO融合语义等价、文本距离、计划差异和运行时加速奖励，并采用概率门控自适应奖励塑形与自改进机制。
result: "在IID和OOD负载上，SPA端到端运行时优于规则和LLM基线，有害慢速重写减少80%，尾延迟显著改善。"
conclusion: SPA证明利用物理执行反馈训练LLM可有效生成加速查询重写，为数据库自动调优提供了新范式。
---

## 摘要
SQL查询重写是一种无需更改模式或索引即可提升数据库性能的成熟技术，然而为现代分析型工作负载寻找有效的重写仍然困难：基于规则的方法局限于预定义的转换，而基于大语言模型的方法产生的重写虽然语义有效，但编译后的物理计划相同或导致运行时性能下降。我们提出SPA，一种SQL计划感知的强化学习框架，利用物理执行反馈训练大语言模型进行查询重写。SPA将重写形式化为策略优化问题，并将GRPO扩展为包含语义等价性、文本重写距离、物理计划差异和运行时加速的奖励。为了解决查询难度带来的奖励稀疏性，SPA引入了概率门控自适应奖励塑形，这是一种查询级课程，仅当一次滚动组达到足够掌握低级目标时才解锁高级奖励，并通过利用当前策略中的减速重写作为有针对性的训练信号，通过在线策略自我改进进一步提高样本效率。在独立同分布和非独立同分布工作负载上，SPA在端到端运行时优于基于规则和强大大语言模型基线，显著减少了有害的减速重写，并产生了显著的尾部延迟收益。

## Abstract
SQL query rewriting is a well-established technique for improving database performance without schema or index changes, yet finding effective rewrites for modern analytical workloads remains difficult: rule-based methods are limited to predefined transformations, while LLM-based approaches often produce rewrites that are semantically valid but compile to equivalent physical plans or degrade runtime performance. We present SPA, a SQL-Plan-Aware reinforcement learning framework that trains LLMs to rewrite queries using physical execution feedback. SPA formulates rewriting as a policy optimization problem and extends GRPO with rewards spanning semantic equivalence, textual rewrite distance, physical-plan divergence, and runtime speedup. To handle reward sparsity across query difficulty, SPA introduces Probability-Gated Adaptive Reward Shaping, a query-level curriculum that unlocks higher-level rewards only once a rollout group achieves sufficient mastery of lower-level objectives, and further improves sample efficiency through on-policy self-improvement by recycling slowdown rewrites from the current policy as targeted training signals. On both IID and OOD workloads, SPA outperforms rule-based and strong LLM baselines in end-to-end runtime, substantially reduces harmful slowdown rewrites, and yields strong tail-latency gains.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：SQL查询重写是提升数据库性能的重要手段，但现有方法效果有限。
  - **规则方法**（如Calcite规则、WeTune等）受限于预定义变换模板，无法覆盖复杂优化机会。
  - **LLM方法**（如直接提示GPT-4o/Gemini等）虽能生成语义等价的重写，但常导致**物理计划不变**或**运行时反而变慢**（例如例1中GPT-5.4重写仅产生0.85倍加速，而SPA正确重写达到28.1倍加速）。
- **关键洞察**：需要利用数据库执行反馈（语义验证、物理计划变化、实际延迟）作为学习信号，而不仅是静态示例或规则。
- **研究意义**：首次将物理执行反馈嵌入LLM重写策略优化，为数据库自动调优提供了“执行驱动”的新范式。

## 2. 方法论：核心思想、关键技术细节

### 2.1 整体框架
SPA基于GRPO（Group Relative Policy Optimization）训练LLM策略 $\pi_\theta$，对同一原始查询$x$采样一组候选重写$G=\{y_1,...,y_G\}$，通过数据库执行获取奖励，计算组内相对优势$A_i = (r_i - \mu_G)/(\sigma_G + \epsilon)$，用截断策略梯度更新。

### 2.2 关键技术细节
1. **并行奖励计算**：每个重写由独立数据库worker并行执行，计算四类信号：
   - 语义等价性（结果集比较）
   - 文本重写距离（归一化Levenshtein距离，阈值$\lambda=0.01$，防止作弊）
   - 物理计划差异（递归比较操作树，仅当计划改变才测量速度）
   - 运行时加速比映射为有界奖励：$R_s(S)=\tanh(\ln\frac{S}{1\pm\delta})$，$\delta=0.1$，小波动视为零奖励
2. **概率门控自适应奖励塑形（PGARS）**：将重写任务分解为四个层次（语法有效→语义等价→计划差异→运行时加速），每个层次的门控$\Gamma_k$只有当组内通过率超过阈值$\tau$（默认50%）时才开启，使训练从低难度向高难度渐进。
3. **在线策略自我改进**：挖掘自身产生的**减速重写**（语义等价但更慢），且在**同组至少存在一个加速重写**（证明该查询可优化）时才将其作为新输入，以1:1比例混入原始训练数据继续GRPO训练。该阶段使策略学会修复自身错误计划。

### 2.3 辅助组件
- **计划解析器**：从PostgreSQL EXPLAIN JSON中提取基数流、排序关键节点（嵌套循环、连接、过滤、聚合），为LLM提供查询瓶颈上下文。
- **SFT数据准备**：使用o3-mini/GPT-5.4生成候选重写，仅保留语义等价且速度提升>1.1倍的样本（经验发现此阈值对应物理计划变化）。

## 3. 实验设计

### 3.1 数据集与场景
- **IID（训练分布内）**：TPC-H (10G) 和 TPC-DS (10G)，使用官方工具链生成查询，并利用SQLStorm合成更多结构变体。
- **OOD（零样本评估）**：DSB（基于TPC-DS修改，数据倾斜+复杂模板）和 StackOverflow（真实查询，领域迁移显著）。
- 每个基准测试使用不同`stream_id`避免训练和评估查询重叠。

### 3.2 对比方法
- **规则基线**：LearnedRewrite（MCTS+学习成本估计）、R-Bot（LLM引导规则选择，用Gemini-2.5-Pro作骨干）。
- **LLM基线**：Qwen3-8B/32B（无训练）、GPT-4o、Gemini-2.5-Pro、GPT-5.4。所有LLM使用相同提示（包含计划解析器输出）和最多3次重试。
- **SPA变体**：SPA-8B（Qwen3-8B骨干）和SPA-32B（Qwen3-32B骨干），经过SFT初始化+RL训练。

### 3.3 评估指标
- 有效性：语法成功率、语义等价率（通过执行结果比较）。
- 延迟：均值、P50/P75/P90/P95（对失败/不等价重写填充为原始查询延迟）。
- 加速/减速比例：≥2×、≥4×、≥10×。

## 4. 资源与算力

- **训练平台**：通过API训练平台，使用LoRA（rank=32，lr=4e-5）。
- **耗时与成本**：
  - Qwen3-8B：SFT 0.13h，初始优化9.43h，自我改进10.88h，总计20.44h，$12.95。
  - Qwen3-32B：SFT 0.29h，初始优化11.93h，自我改进12.77h，总计24.99h，$52.08。
- **硬件**：并行奖励计算使用单台Ubuntu机器（128GB内存），PostgreSQL配置shared_buffers=32GB，work_mem=256MB。评估使用8 vCPU、30GB RAM的实例。
- **未明确说明**：GPU型号、数量（因通过API平台训练，底层硬件细节未公开）。

## 5. 实验数量与充分性

### 已进行实验组：
1. **主实验**（表3）：13种方法 × 4个数据集 = 52个延迟矩阵，覆盖有效性+多分位延迟。
2. **阈值分析**（表5）：加速/减速比例在2×/4×/10×下的对比。
3. **消融实验**（表4）：8种变体（仅SFT、无计划差异奖励、不同τ值、无自我改进、仅慢速自我改进、无计划信息），在TPC-H和DSB上评估。
4. **数据效率实验**（表6）：2k vs 5k SFT数据量对比，聚合4个数据集结果。
5. **案例分析**（图6）：对比GPT-5.4与SPA在TPC-DS查询上的具体重写结果。
6. **训练曲线**（图5）：奖励和策略熵的变化。

### 充分性与客观性：
- **公平性**：所有LLM基线使用相同提示和重试机制；规则方法与LLM方法独立比较；禁止未来信息泄露（OOD严格分离）。
- **丰富性**：覆盖IID和OOD场景，消融验证各组件贡献，数据效率降低了SFT成本疑虑。
- **潜在不足**：仅评估了PostgreSQL 16；未对比E3-Rewrite（因未公开）；未测试多数据库系统泛化；每个查询只执行3次取均值，随机波动可能影响。

## 6. 论文的主要结论与发现

1. **SPA显著优于基线**：
   - 在IID上，SPA-32B在TPC-H上均值32.73s（原始57.00s），P95从>300s降至289.93s；在TPC-DS上均值19.92s（原始30.92s），P95从>300s降至72.92s。
   - 在OOD上，DSB均值降至6.96s（原始21.23s），StackOverflow保持100%等价率且延迟接近最佳。
2. **有效减少有害减速**：聚合结果中SPA-32B的非平凡减速率仅7.78%，对比GPT-5.4的21.56%，且无≥10×减速。
3. **组件必不可少**：
   - RL远超纯SFT（SFT在DSB上等价率降至75%且失败率升高）。
   - 计划差异奖励防止产生相同计划的重写。
   - PGARS阈值τ=50%优于25%（过于激进）和75%（过于保守）。
   - 自我改进比等量额外训练步骤效果更好。
4. **数据效率高**：仅2k SFT样本即可接近GPT-5.4性能，5k进一步提升。

## 7. 优点

- **创新性**：将物理执行反馈（计划差异+实际延迟）直接嵌入LLM强化学习，而非仅依赖成本估计。
- **解决关键痛点**：针对性处理了LLM重写“表面合理但实际无效或有害”的问题。
- **工程实用性**：并行奖励计算使训练可行；概率门控自适应课程避免梯度噪声；自我改进利用上线失败样本。
- **全面消融**：8种变体验证了每个设计选择的必要性。
- **OOD泛化证明**：在完全不同的数据集（StackOverflow）上仍有效，说明学到了通用优化原理而非记忆。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试PostgreSQL 16；其他DBMS（MySQL、SQL Server、Snowflake等）的优化器行为不同，泛化性未验证。
  - 仅涉及分析型负载；OLTP场景（短查询、索引敏感）未覆盖。
- **评估可靠性**：
  - 仅3次执行取均值，未报告方差或置信区间；部分长尾查询可能因缓存或并发产生波动。
  - 失败/不等价重写采用“填充为原始延迟”可能掩盖实际风险（例如错误结果未被检测）。
- **训练成本**：
  - 需要数据库执行环境，大规模训练（如5k查询×16个候选×多轮）仍然昂贵；虽报告成本约$52，但未说明训练数据准备成本（生成SFT数据调用o3-mini/GPT-5.4）。
- **方法依赖**：
  - 依赖PostgreSQL的EXPLAIN格式；其他系统需要适配解析器。
  - 语义等价验证仅通过结果集比较，对于大结果集可能耗尽内存或超时（文中已处理超时情况，但未完全解决）。
- **潜在偏差**：
  - SFT数据仅包含加速>1.1的样本，可能遗漏某些有潜力但小幅度优化的重写；RL阶段可能因此偏向激进变换。
  - 自我改进仅选择“组内有加速”的慢速样本，忽略那些全局无法优化但局部可修复的查询。

（完）
