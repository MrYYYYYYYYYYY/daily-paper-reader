---
title: "SPA: A SQL-Plan-Aware Reinforcement Learning Framework for Query Rewriting with LLMs"
title_zh: SPA：一种面向LLM查询重写的SQL计划感知强化学习框架
authors: "Xinyi Huang, Zhengjie Miao"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08620v1"
tags: ["query:lpt"]
score: 8.0
evidence: 基于物理执行反馈的LLM查询重写强化学习
tldr: SQL查询重写旨在不修改模式或索引的情况下提升数据库性能，但现有规则方法局限于预定义变换，LLM方法常生成语义正确但物理计划等价甚至更慢的重写。SPA框架将重写建模为策略优化，利用物理执行反馈训练LLM，扩展GRPO引入语义等价、文本距离、物理计划差异和运行加速四类奖励，并设计概率门控自适应奖励塑形，根据查询难度动态解锁高等级奖励。通过回收当前策略产生的慢速重写作为训练信号实现策略自我改进，SPA在分布内和分布外负载上均优于规则和强LLM基线，端到端运行时间显著缩短，有害重写大幅减少，尾部延迟得到有效改善。该工作为查询重写提供了结合LLM与强化学习的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08620v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 1358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1823, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 664, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 827, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08620v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1709, \"height\": 832, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08620v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 818, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1764, \"height\": 1195, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 555, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 811, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1837, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08620v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 888, \"height\": 173, \"label\": \"Table\"}]"
motivation: 现有SQL重写方法或受限于规则模板，或LLM生成等价但低效的查询，亟需一种物理计划感知的优化方法。
method: SPA框架将重写建模为强化学习策略优化，扩展GRPO引入多重奖励，并采用概率门控自适应奖励塑形和策略自我改进提升样本效率。
result: 在IID和OOD负载上，SPA端到端运行时间优于规则和LLM基线，有害重写比例显著降低，尾部延迟改善明显。
conclusion: SPA证明了利用物理执行反馈训练LLM进行查询重写的有效性，为数据库性能优化提供了新路径。
---

## 摘要
SQL查询重写是一种用于在不改变模式或索引的情况下提高数据库性能的成熟技术，但为现代分析型工作负载找到有效的重写仍然困难：基于规则的方法局限于预定义转换，而基于LLM的方法通常产生语义有效但编译为等效物理计划或降低运行时性能的重写。我们提出了SPA，一种SQL计划感知的强化学习框架，它使用物理执行反馈来训练LLM重写查询。SPA将重写表述为策略优化问题，并扩展GRPO，引入涵盖语义等价性、文本重写距离、物理计划差异和运行时加速的奖励。为了处理查询难度上的奖励稀疏性，SPA引入了概率门控自适应奖励塑形，这是一种查询级别的课程，只有在推出组达到足够掌握较低级别目标时才解锁更高级别的奖励，并通过从当前策略中回收减速重写作为定向训练信号，进一步通过策略内自我改进提高样本效率。在IID和OOD工作负载上，SPA在端到端运行时上优于基于规则的和强LLM基线，显著减少了有害的减速重写，并产生了强大的尾部延迟增益。

## Abstract
SQL query rewriting is a well-established technique for improving database performance without schema or index changes, yet finding effective rewrites for modern analytical workloads remains difficult: rule-based methods are limited to predefined transformations, while LLM-based approaches often produce rewrites that are semantically valid but compile to equivalent physical plans or degrade runtime performance. We present SPA, a SQL-Plan-Aware reinforcement learning framework that trains LLMs to rewrite queries using physical execution feedback. SPA formulates rewriting as a policy optimization problem and extends GRPO with rewards spanning semantic equivalence, textual rewrite distance, physical-plan divergence, and runtime speedup. To handle reward sparsity across query difficulty, SPA introduces Probability-Gated Adaptive Reward Shaping, a query-level curriculum that unlocks higher-level rewards only once a rollout group achieves sufficient mastery of lower-level objectives, and further improves sample efficiency through on-policy self-improvement by recycling slowdown rewrites from the current policy as targeted training signals. On both IID and OOD workloads, SPA outperforms rule-based and strong LLM baselines in end-to-end runtime, substantially reduces harmful slowdown rewrites, and yields strong tail-latency gains.

---

## 论文详细总结（自动生成）

我将按照您提供的框架，对论文《SPA: A SQL-Plan-Aware Reinforcement Learning Framework for Query Rewriting with LLMs》进行结构化总结。

---

## 1. 核心问题与整体含义

- **研究动机**：SQL查询重写旨在不修改模式或索引的前提下提升数据库性能，是一种成熟且重要的优化手段。然而，现有方法面临两难困境：基于规则的方法（如Calcite、WeTune）只能应用预定义的变换规则，对于需要复杂组合变换的分析型查询往往失效；基于LLM的方法（如GPT-5.4直接生成）虽能产生语法正确的重写，但常出现“语义等价但物理计划不变”或“语义等价但运行时反而更慢”的情况。
- **核心问题**：如何让LLM生成的重写不仅语义正确，而且能真正触发更优的物理执行计划、缩短实际运行时间？即需要让LLM感知并优化物理执行层面的反馈。
- **整体含义**：论文提出了一个**SQL计划感知的强化学习框架(SPA)**，将查询重写建模为策略优化问题，利用数据库执行反馈（计划差异、运行加速）作为奖励信号，训练LLM产生有效的重写。这为LLM在数据库性能优化中的应用提供了新的范式。

---

## 2. 方法论

- **核心思想**：SPA基于Group Relative Policy Optimization (GRPO)框架，对同一原始查询采样多个候选重写，根据每组内的相对优势更新策略。奖励函数包含四个维度：
  - **语义等价性**：通过执行结果集对比验证。
  - **文本重写距离**：使用归一化Levenshtein距离（阈值λ=0.01）防止仅做表面修改。
  - **物理计划差异**：通过递归比较EXPLAIN操作树，判断是否产生不同的物理计划。
  - **运行时加速比**：对实际执行时间计算加速比，并通过tanh映射为有界奖励（仅当加速比>1.1或<0.9时赋值）。
- **关键技术细节**：
  - **概率门控自适应奖励塑形(PGARS)**：将学习过程分为四个里程碑（语法正确、语义等价、计划变化、运行时加速），每个里程碑只有在当前rollout组中成功率超过阈值（默认τ=50%）时才激活后续奖励。这避免了早期训练中梯度噪声。
  - **策略内自我改进(On-Policy Self-Improvement)**：在基础RL训练后，从历史中选择那些“语义等价但导致减速”的重写，且其同一组内存在其他重写获得显著加速，将这些减速查询作为新的训练输入，促使LLM学会从自身错误中恢复。
  - **并行奖励计算**：为加速训练，将同一rollout组中的多个重写分发到独立数据库worker并行执行，同时配置PostgreSQL共享缓冲区32GB、工作内存256MB以支持并发。
  - **计划解析器**：从EXPLAIN (FORMAT JSON)中提取关键执行信号（如基数变化、操作类型），以结构化提示形式提供给LLM，帮助其识别瓶颈。

---

## 3. 实验设计

- **数据与场景**：
  - **In-Distribution (IID)**：TPC-H (10GB) 和 TPC-DS (10GB) 标准决策支持工作负载。
  - **Out-of-Distribution (OOD)**：DSB（基于TPC-DS修改，高偏斜数据）和 StackOverflow（真实世界数据，域转移明显）。
  - 使用SQLStorm合成额外查询丰富训练集。
- **基准方法**：
  - 原始查询（无重写）。
  - 规则系统：LearnedRewrite（MCTS+成本估计）、R-Bot（证据引导+LLM）。
  - 通用LLM：Qwen3-8B, Qwen3-32B, GPT-4o, Gemini-2.5-Pro, GPT-5.4（均为零样本，相同提示）。
  - 消融变体：仅SFT、不含计划差异奖励、不同τ值、不含自我改进、仅使用减速样本、不含计划信息。
- **评价指标**：语法成功率、语义等价率、平均/各百分位运行时间（P50, P75, P90, P95）、不同加速/减速阈值比例（≥2×,≥4×,≥10×）。

---

## 4. 资源与算力

- **训练基础设施**：通过API平台进行LoRA微调（rank=32，学习率4e-5），数据库侧并行执行在单台Ubuntu机器（128GB内存，PostgreSQL共享缓冲区32GB，工作内存256MB）。
- **成本与时间**：
  - Qwen3-8B：总成本$12.95，总时间20.44小时（SFT 0.13h + 初始优化9.43h + 自我改进10.88h）。
  - Qwen3-32B：总成本$52.08，总时间24.99小时（SFT 0.29h + 初始优化11.93h + 自我改进12.77h）。
- **未明确说明GPU型号**：文献未提及具体GPU型号和数量，因为训练通过API完成，计算资源由平台提供。

---

## 5. 实验数量与充分性

- **主实验**（表3）：覆盖4个数据集，每个数据集比较10种方法，报告完整指标。
- **消融实验**（表4）：在TPC-H和DSB上测试8个变体，验证PGARS、自我改进、计划信息等贡献。
- **阈值分析**（表5）：对TPC-H和DSB统计不同加速/减速阈值下的比例。
- **数据效率分析**（表6）：2k vs 5k SFT数据对SPA-32B性能的影响。
- **成本分析**（表7）：分阶段列出时间和费用。
- **实验充分性评价**：实验设计较为全面，覆盖了IID和OOD场景，对比了规则方法和多种LLM基线，消融实验各组件明确。但未与同期的E3-Rewrite（因未公开且依赖专用LLM等价判定）进行对比，这是公开文献中可查的类似RL方法。总体而言，实验客观且较为充分。

---

## 6. 主要结论与发现

1. **SPA在所有基准上优于规则系统和强LLM基线**：在IID和OOD工作负载上，SPA-32B均获得最低或接近最低的平均运行时和百分位运行时，尤其在DSB上将平均运行时从21.23s降至6.96s。
2. **大幅减少有害减速重写**：SPA-32B的≥10×减速率为0，而GPT-5.4为1.80%，Gemini-2.5-Pro为1.20%；总有害重写比例显著低于其他LLM。
3. **消融证实各组件的必要性**：缺少RL、计划差异奖励、适当τ值、自我改进或计划信息都会导致性能下降。
4. **数据效率较高**：仅使用2k SFT重写数据，SPA-32B仍能与GPT-5.4竞争，并在某些指标（等价率、有害减速控制）上超越。
5. **案例研究揭示**：即使GPT-5.4生成了语义正确的重写，因未考虑物理执行细节（如过滤条件下推时机），导致实际效果反而变慢（0.85×）；而SPA通过执行反馈学到正确策略，获得28.1×加速。

---

## 7. 优点

- **方法创新性**：
  - 首次将物理计划差异作为强化学习奖励信号直接用于LLM训练。
  - PGARS提供了一种统一的课程学习机制，自动适应查询难度，避免早期不稳定。
  - 策略内自我改进巧妙地利用自身的失败案例进行训练，提升了模型对有害重写的鲁棒性。
- **实验设计亮点**：
  - 同时评估分布内和分布外性能，表明方法具有泛化能力。
  - 数据效率分析显示方法对标记数据需求较低，实用性强。
  - 并行奖励计算使基于执行反馈的RL训练在合理成本内可行。
- **理论与实践结合**：通过具体案例直观展示了为什么计划感知是关键，加深了理解。

---

## 8. 不足与局限

- **实验覆盖**：
  - 仅使用PostgreSQL 16作为目标DBMS，未验证在其他数据库（如MySQL、Oracle、SQL Server）上的可迁移性。
  - 工作负载规模限于10GB，未测试更大数据集（如100GB/1TB）下的表现。
  - 未与最新同类工作E3-Rewrite进行直接比较（因未公开，但可在讨论中提及）。
- **偏差风险**：
  - SFT数据由o3-mini和GPT-5.4生成，可能存在对特定LLM模式的过拟合。
  - RL训练中使用了相同的数据库实例，可能产生对特定硬件/配置的依赖。
- **应用限制**：
  - 训练过程需要大量执行反馈，在慢速数据库或资源受限环境下面临成本压力。
  - 阈值τ（50%）和加速阈值δ（0.1）可能不是最优，需针对不同数据集调优。
  - 对于无法在超时内完成的原始查询，SPA直接跳过该组，可能导致一些本来可优化的查询被忽略。
- **理论层面**：未提供收敛性分析或最坏情况保证，RL训练本身可能不稳定（文中图5显示熵值波动）。

（完）
