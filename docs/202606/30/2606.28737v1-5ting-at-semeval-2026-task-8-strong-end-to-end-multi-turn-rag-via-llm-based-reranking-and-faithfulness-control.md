---
title: "5ting at SemEval-2026 Task 8: Strong End-to-End Multi-Turn RAG via LLM-Based Reranking and Faithfulness Control"
title_zh: 5ting 参加 SemEval-2026 任务 8：基于 LLM 重排序和忠实性控制的强端到端多轮 RAG
authors: "Thien-Qua-T-Nguyen, Chi Hoang, Nguyen Tran, Tri Le, Khanh Truong, Chinh Trong Nguyen"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28737v1"
tags: ["query:lpt"]
score: 9.0
evidence: 直接处理多轮RAG中的幻觉风险，采用忠实性控制
tldr: "针对多轮检索增强生成中的上下文漂移、欠指定和幻觉风险，提出5ting系统，采用BGE-M3稠密检索与FAISS索引、双查询合并检索及基于大语言模型的重排序，结合角色分离生成并约束于检索证据。在SemEval-2026 Task 8的任务A中检索器nDCG@5达0.4719，端到端系统在任务C中调和得分0.5597、RL_F=0.7692，验证了方法在缓解多轮RAG问题上的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28737v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1491, \"height\": 635, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28737v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 810, \"height\": 365, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28737v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 757, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28737v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 756, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28737v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 791, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28737v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1613, \"height\": 414, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28737v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1227, \"height\": 562, \"label\": \"Table\"}]"
motivation: 多轮RAG面临上下文漂移、欠指定和幻觉风险，现有方法效果有限，亟需强健的端到端系统。
method: 结合BGE-M3稠密检索与FAISS索引，采用双查询合并检索和LLM重排序，再以角色分离生成约束于检索证据。
result: "任务A检索nDCG@5=0.4719，任务C端到端调和得分0.5597、RL_F=0.7692。"
conclusion: 5ting系统通过重排序和忠实度控制有效提升多轮RAG性能，为后续研究提供强基准。
---

## 摘要
我们介绍了 5ting，这是为 SemEval2026 任务 8（MTRAGEval）开发的系统，用于评估多轮检索增强生成（RAG）系统。多轮 RAG 涉及语境漂移、不明确性和幻觉风险。我们的系统结合了 BGE-M3 密集检索与 FAISS 索引、双查询合并检索以及基于 LLM 的重排序，随后采用角色分离生成，并约束于检索到的证据。检索器在任务 A 中实现了 nDCG@5 = 0.4719，而端到端系统在任务 C 中排名，调和得分为 0.5597，RL_F = 0.7692。

## Abstract
We introduce 5ting, our system for the SemEval2026 Task 8 (MTRAGEval), which evaluates multi-turn Retrieval Augmented Generation (RAG) systems. Multi turn RAG involves context drift, under specification, and hallucination risk. Our system combines BGE-M3 dense retrieval with FAISS indexing, dual-query merged retrieval, and LLM based reranking, followed by role separated generation constrained to retrieved evidence. The retriever achieved nDCG@5 = 0.4719 in Task A, while the end to end system ranked in Task C with a harmonic score of 0.5597 and RL_F = 0.7692.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：多轮检索增强生成（Multi-Turn RAG）系统面临三大挑战：**上下文漂移**（对话历史导致意图变化）、**欠指定**（最后一轮查询依赖前文，有歧义或省略）以及**幻觉风险**（生成内容超出检索证据）。
*   **研究动机**：现有基准（如 MTRAG-UN）显示，即使是当前最先进系统在多轮场景中仍表现不佳。SemEval-2026 任务 8（MTRAGEval）专门评估多轮 RAG，涵盖四个领域（ClapNQ, FiQA, Cloud, Govt）的 842 个多轮对话评估任务。
*   **整体含义**：作者旨在构建一个**模块化、强健的端到端系统**，通过改进检索质量（特别是重排序）和生成忠实性控制，缓解上述问题，为多轮 RAG 提供一个强力基线。

## 2. 论文提出的方法论：核心思想、关键技术细节
*   **核心思想**：采用“双查询合并检索 + LLM 重排序 + 角色分离生成”的流水线，将检索与生成的职责严格分离：检索阶段追求高召回和高精度排序，生成阶段通过提示工程强制约束于检索证据。
*   **关键技术细节**：
    *   **查询重写**：使用 GPT-4o-mini（温度 0.3），将最后一轮查询结合完整对话历史，重写为自包含、上下文消歧后的查询（`rewrite`）。
    *   **向量化与索引**：使用 BGE-M3 稠密编码器（1024 维，支持 8192 token），FAISS IndexFlatIP（内积检索）按领域建立索引。
    *   **双查询合并检索**：分别使用**最后一句原始查询**（`last-turn`）和**重写查询**（`rewrite`）独立检索 top-10，合并两集合（重复则保留最高分），得到约 10-20 个候选。目的：兼顾表层词汇匹配和深层语义匹配。
    *   **LLM 重排序**：使用 GPT-4o-mini（温度 0.3）对合并候选进行**列表式重排序**（listwise reranking）。输入重写查询和所有候选文本，输出 JSON 格式的排序。此步骤是性能提升最大组件。
    *   **角色分离生成**：提示（prompt）严格区分三个角色：
        *   **对话历史**：仅用于解析意图和消解指代，**不作为事实来源**。
        *   **检索段落**：作为**唯一事实基础**，禁止使用参数化知识。
        *   **最后一轮问题**：具体需回答的问题。
    *   **反幻觉指令**：要求“如果段落不足以回答，必须明确声明”、“不得猜测或引入段外信息”。使用低温度（T=0.1）和 top-p=0.9 采样，促进稳定、忠实输出。

## 3. 实验设计：数据集、基准、对比方法
*   **数据集**：MTRAG 数据集（110 个对话，平均 7.7 轮，842 个评估任务），四个领域（ClapNQ, FiQA, Cloud, Govt）。语料库：78,170 文档，366,479 段落（每段 512 token，重叠 100 token）。
*   **基准（Baseline）**：论文对比了：
    *   **检索基准**：BM25（词法）、BGE-base 1.5（稠密）、**ELSER**（任务特定学习型稀疏检索，官方最强基线）。
    *   **生成基准**：多种 LLM（Llama 3.1 8B/70B/405B、Mixtral 8x22B、Command-R+、Qwen 2.5 72B、GPT-4o-mini、GPT-4o）在完全 RAG 设置（使用官方 top-5 段落）下的性能。
*   **评估指标**：
    *   任务 A（检索）：主要指标 **nDCG@5**，辅助：nDCG@1/3/10，Recall@1/3/5/10。
    *   任务 C（端到端）：调和均值 = 调和均值( `RB_alg`, `RL_F`, `RB_llm` )，其中 `RL_F` 为 RAGAS 忠实性 LLM 打分。
*   **对比方法**：作者系统与上述所有基线公平对比，均使用官方评估框架。

## 4. 资源与算力
*   **未明确说明**：论文中**未披露使用的 GPU 型号、数量或训练时长**。所有 LLM 调用（重写、重排序、生成）均使用 **GPT-4o-mini API**，未进行任何微调。FAISS 索引和稠密编码器（BGE-M3）也仅用于推理。因此，训练算力开销为零，推理算力主要来自 API 调用，本地算力需求较低（仅需 CPU/单 GPU 进行向量检索）。

## 5. 实验数量与充分性
*   **实验数量**：论文进行了以下主要实验：
    *   官方测试结果（任务 A 和 C）对比基线。
    *   域内性能分析（表 2：四个领域各自 nDCG@5 和 Recall@5）。
    *   **消融实验**：表 3 对比了不同查询类型（`question`, `last-turn`, `rewrite`）有无重排序的 nDCG@5 提升（+6.1%, +16.6%, +17.7%）。
    *   **累积消融实验**（表 4）：逐步添加“BGE-M3 稠密”、“+查询重写”、“+LLM 重排序”，展示各组件贡献。
    *   **端到端生成对比**（表 5）：开发集和测试集上与其他 LLM 的 RAG 性能对比。
    *   **错误分析**（表 6）：手动检查 30 例召回失败，分类失败类型（指代未消解、主题转移等）。
*   **充分性与公平性**：
    *   **充分**：涵盖了检索和生成的主要评估维度，消融实验清晰分离组件贡献。域内分析展示了跨领域泛化差异。
    *   **存在局限**：错误分析仅基于 30 例手动样本，可能不具全面统计代表性。未进行超参数搜索（如重排序温度、top-p 变化）。未对比其他重排序方法（如交叉编码器）。然而，对于参赛报告而言，实验设计合理且可复现。

## 6. 论文的主要结论与发现
*   **检索阶段**：LLM 重排序是**最有效的单一组件**，在 nDCG@5 上带来 **+17.7% 的相对提升**（对 `rewrite` 查询而言）。双查询合并优于单独使用任一查询。
*   **端到端阶段**：该系统的 `RL_F`（忠实性得分）为 **0.7692**，超越所有官方基线（包括 GPT-4o 的 0.66），提升 +16.5%。证明了**提示工程对忠实性的贡献甚至大于模型规模**。
*   **检索与生成不对称性**：检索性能（nDCG@5=0.4719，约为 ELSER 的 98.4%）并未达到顶尖，但生成端通过忠实性控制实现了较高排名（任务 C 第 3/29），表明**生成侧严格的证据约束可以部分补偿检索不足**。
*   **领域差异**：ClapNQ 性能最好（0.5112），FiQA 最难（0.3872），可能与专业术语和查询歧义有关。
*   **稳定性和提升**：开发集到测试集 nDCG@5 从 0.439 提升至 0.4719，归因于数据域组成差异。

## 7. 优点：方法或实验设计上的亮点
*   **双查询策略**：同时使用原始查询（捕获确切实体）和重写查询（捕获语义上下文），合并后提高召回率和多样性，避免单一查询的片面性。
*   **角色分离提示设计**：将对话历史严格限定为“意图解析”角色，检索段落为“事实”角色，有效防止模型混淆来源，是一种简洁但强有力的反幻觉措施。
*   **低资源需求**：完全使用闭源 API（GPT-4o-mini）进行推理，无需微调，极大降低了实现门槛。
*   **高度透明**：在附录 A 中公开了完整的生成提示模板，利于复现和研究。
*   **公平基线比较**：与多个主流 LLM 在同一评估框架下比较，结果可信。

## 8. 不足与局限
*   **依赖商业 API**：使用 GPT-4o-mini（非开源），导致系统**完全不可完全复现**（尤其是 API 更新的潜在差异）。
*   **延迟问题**：LLM 重排序步骤每查询增加 1-2 秒，对于实时应用可能不够。
*   **错误分析样本小**：仅 30 例，分类可能不全面，尤其是指代消解的具体模式。
*   **未探索的技术**：
    *   未尝试混合检索（密集+稀疏如 BGE-M3 原生支持）。
    *   未使用开源重排序器（如 RankZephyr）。
    *   未对嵌入模型进行领域微调。
    *   未实验滑动窗口或分块重排序（对于长候选集）。
*   **生成性能瓶颈**：`RB_alg`（基于参考的相关性）得分仅 0.3867，表明检索召回仍不足，导致生成内容与参考答案对齐差。作者也承认需改善召回。

（完）
