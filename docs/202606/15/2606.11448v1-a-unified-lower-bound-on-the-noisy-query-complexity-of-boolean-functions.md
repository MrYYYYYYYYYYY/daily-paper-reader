---
title: A Unified Lower Bound on the Noisy Query Complexity of Boolean Functions
title_zh: 布尔函数噪声查询复杂度的统一下界
authors: "Yuzhou Gu, Xin Li, Yinzhan Xu"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11448v1"
tags: ["query:lpt"]
score: 9.0
evidence: 噪声查询复杂度的数学公式下界
tldr: 带噪查询模型中，算法可自适应查询输入比特但结果以固定概率翻转。本文提出基于布尔超立方体子图度统计的通用下界，统一了先前大多数下界（常数因子内），并证明N_p(f)=Ω(I(f) log I(f))，其中I(f)为总影响。该工作提供了统一分析框架，简化了证明，并给出若干新函数的紧界。
source: arxiv
selection_source: fresh_fetch
motivation: 超越平凡随机查询复杂度下界，为带噪查询复杂度建立通用下界。
method: 利用布尔超立方体子图度统计，构造基于函数结构的下界。
result: 恢复先前大部分下界并证明N_p(f)=Ω(I(f)log I(f))，解决开放问题。
conclusion: 提供统一下界框架，简化证明，适用于新函数。
---

## 摘要
我们研究由Feige, Raghavan, Peleg和Upfal [SICOMP 1994]引入的噪声查询模型中布尔函数$f: \{0, 1\}^n \rightarrow \{0, 1\}$的查询复杂度。在该模型中，算法可以自适应地查询输入向量的比特，但每次查询结果以常数概率$p \in (0, 1/2)$独立翻转；允许重复查询。函数$f$的噪声查询复杂度$\mathsf{N}_p(f)$定义为在最坏输入$x$下，以不超过$1/3$的错误概率计算$f(x)$所需的最小期望查询次数。我们基于布尔超立方体某些子图的度统计，证明了$\mathsf{N}_p(f)$的一个通用下界。这是除简单观察$\mathsf{N}_p(f)$受随机化查询复杂度下界约束之外的首个通用下界。我们表明该下界（在常数因子内）恢复了先前已知的大多数布尔函数噪声查询复杂度下界，为理解这些结果提供了统一框架，并在若干情况下简化了证明。此外，这肯定性地解决了Gu, Li和Xu [COLT 2025]的一个开放问题：$\mathsf{N}_p(f) = \Omega(\mathsf{I}(f) \log \mathsf{I}(f))$，其中$\mathsf{I}(f)$表示$f$的总影响。我们还应用通用下界获得了多个新函数的噪声查询复杂度的紧界。

## Abstract
We study the query complexity of Boolean functions $f: \{0, 1\}^n \rightarrow \{0, 1\}$ in the noisy query model introduced by Feige, Raghavan, Peleg and Upfal [SICOMP 1994]. In this model, an algorithm can adaptively query the bits of an input vector, but each query result is independently flipped with constant probability $p \in (0, 1/2)$; repeated queries are allowed. The noisy query complexity $\mathsf{N}_p(f)$ of a function $f$ is defined as the minimum expected number of queries needed to compute $f(x)$ with error probability at most $1/3$, for the worst case input $x$.   We prove a general lower bound on $\mathsf{N}_p(f)$ based on degree statistics of certain subgraphs of the Boolean hypercube. This is the first general lower bound beyond those implied by the simple observation that $\mathsf{N}_p(f)$ is lower bounded by the randomized query complexity. We show that this recovers (up to a constant factor) most previously known lower bounds on the noisy query complexity of Boolean functions, providing a unified framework for understanding these results and simplifying the proofs in several cases. Furthermore, this resolves in the affirmative an open problem of Gu, Li and Xu [COLT 2025] that $\mathsf{N}_p(f) = Ω(\mathsf{I}(f) \log \mathsf{I}(f))$, where $\mathsf{I}(f)$ denotes the total influence of $f$. We also apply our general lower bound to obtain tight bounds on the noisy query complexity for several new functions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：在噪声查询模型（noisy query model）中，算法可以自适应地查询布尔输入 \( x \in \{0,1\}^n \) 的各个比特，但每次查询结果以固定概率 \( p \in (0,1/2) \) 独立翻转。目标是计算布尔函数 \( f(x) \)，并最小化期望查询次数 \( \mathsf{N}_p(f) \)。该模型由 Feige 等人（1994）提出，用于模拟硬件故障、通信错误等现实场景。
- **动机**：此前已知 \( \mathsf{N}_p(f) = \Omega(\mathsf{R}(f)) \)（随机查询复杂度的下界），且 \( \mathsf{N}_p(f) = O(\mathsf{R}(f)\log \mathsf{R}(f)) \)。但二者间存在间隙：有些函数（如 PARITY）达到 \(\Theta(n\log n)\)，而有些（如 AND/OR）仅为 \(\Theta(n)\)。因此需要更精细的下界工具。Gu 等人（2025）猜想 \( \mathsf{N}_p(f) = \Omega(\mathsf{I}(f)\log \mathsf{I}(f)) \)，其中 \(\mathsf{I}(f)\) 是总影响（total influence），本文肯定性地解决了该猜想。
- **整体含义**：本文首次为任意布尔函数（除了平凡下界外）建立了通用的噪声查询复杂度下界，并且该下界基于函数的图论性质（子图度统计），统一了之前针对特定函数（PARITY、图连通性、随机函数等）的分散下界，极大简化了证明。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用布尔超立方体 \( \{0,1\}^n \) 的边诱导子图 \( H_f \)（仅保留使 \( f(x) \neq f(y) \) 的边），分析其度统计。主要技术定理（Theorem 2）声称：若存在 \( H_f \) 的一个诱导子图 \( G \)，其最小度 \( d_1 \)，且按度加权分布 \(\mu(x)=\frac{\deg_G(x)}{2|E(G)|}\) 下，至少 \(\gamma\) 比例的顶点度 \(\ge d_2\)，则 \( \mathsf{N}_p(f) \ge c\cdot d_2\log(1+d_1) \)。
- **关键技术细节**：
  - **三阶段框架（Three-phase framework）**：借鉴 Feige (1992) 和 Gu 等人 (2025) 的思路。第一阶段：对每比特进行 \( c_1\log d_1 \) 次非自适应噪声查询；第二阶段：免费揭示部分信息（通过重整化过程使后验分布不依赖于查询结果）；第三阶段：允许至多 \( c_2 d_2 \) 次自适应精确查询。
  - **平衡引理（Balance Lemma）**：利用图上的加权度和边权，证明若后验分布中某部分顶点的加权度有界，则分布中 \( f=0 \) 和 \( f=1 \) 的概率均被常数下界，从而任何算法无法以低错误率输出。
  - **浓度不等式**：对 \( |N_-(x) \cap X_z| \) 等随机变量使用 Chernoff 和 Azuma-Hoeffding 不等式，控制相变概率。
  - **退化度（Degeneracy）**：\( H_f \) 的退化度 \( \text{degen}(f) \) 被证明与 \( \mathsf{N}_p(f) \) 有直接关系：\( \mathsf{N}_p(f) \ge c\, \text{degen}(f)\log \text{degen}(f) \)，并利用图论事实 \( \mathsf{I}(f) \le 2\,\text{degen}(f) \) 导出 \( \mathsf{N}_p(f) = \Omega(\mathsf{I}(f)\log \mathsf{I}(f)) \)。

### 3. 实验设计

- **实验说明**：本文为纯数学理论论文，没有进行任何实际实验、数据集测试或非理论性 benchmark。
- **对比方法**：文中未对比算法，而是与现有已知下界（PARITY、图连通性、对称函数等）进行理论比较，证明新下界能恢复（常数因子内）并统一这些结果。
- **Benchmark**：不存在。

### 4. 资源与算力

- **明确说明**：本文未提及任何 GPU、训练时长或算力资源。论文完全基于数学证明，不涉及计算实验。

### 5. 实验数量与充分性

- **适用性**：不适用。论文没有进行实验验证，所有结论均为理论证明。因此不存在实验数量、消融实验或公平性讨论。

### 6. 论文的主要结论与发现

- **Theorem 1**：\( \mathsf{N}_p(f) = \Omega(\mathsf{I}(f)\log \mathsf{I}(f)) \)，解决了 Gu 等人（2025）的开放问题。该界是可能的最优（考虑 PARITY 等函数的紧例）。
- **Theorem 2**（主要技术定理）：基于子图 \( G \) 的度统计 \( d_1, d_2 \) 给出通用下界，是其余推论的基础。
- **Theorem 3**：退化度下界 \( \mathsf{N}_p(f) \ge c\, \text{degen}(f)\log \text{degen}(f) \)。
- **Theorem 4**：偏置随机函数（\( \Pr[f(x)=1]= \gamma n^{-\lambda} \)）的噪声查询复杂度以高概率为 \( \Omega(n\log n) \)。
- **Theorem 5**：固定模式子序列计数（如 \( t \in \{0,1\}^k \)，\( k=O(1) \)）的复杂度为 \( \Omega(n\log n) \)。
- **统一性**：使用 Theorem 2 重新证明并简化了 k-THRESHOLD、对称函数、图连通性、s-t 连通性等已知下界，且证明比原论文（如图连通性需 5 页分析）简洁。

### 7. 优点

- **通用性**：首次提供适用于任意布尔函数的非平凡统一下界，而不仅限于高影响函数或特定结构。
- **简洁性**：利用图论概念（子图度、退化度）将前人的复杂证明统一到一个框架下，大大简化了图连通性等结果的证明。
- **紧性**：对于总影响 \( \mathsf{I}(f) \)，下界 \( \Omega(\mathsf{I}(f)\log \mathsf{I}(f)) \) 是最优的（存在匹配上界的函数）。
- **新应用**：将下界推广到偏置随机函数和子序列计数问题，此前无结果。
- **理论深度**：结合了三阶段框架、平衡引理、浓度不等式等工具，技术新颖且严谨。

### 8. 不足与局限

- **非紧情况**：对于随机查询复杂度 \( \mathsf{R}(f) \) 远大于 \( s(f)\log s(f) \) 的函数（如某些具有大块敏感度的函数），本下界 \( \Omega(s(f)\log s(f)) \) 可能远弱于实际复杂度（因为 \( \mathsf{N}_p(f) \ge \Omega(\mathsf{R}(f)) \)）。论文承认了这一点。
- **常数依赖性**：下界常数 \( c = c(p,\gamma) \) 依赖于噪声概率 \( p \) 和参数 \( \gamma \)，未给出具体显式值；对于极小 \( d_1 \)（如 \( d_1 \le O(1) \)），需要依赖平凡下界过渡，可能不是最优。
- **仅限布尔函数**：虽然论文对子序列计数等非布尔函数通过阈值转化进行了处理，但主要定理针对布尔输出函数。对于多值函数可能需要额外假设。
- **无实验验证**：作为纯理论工作，无法通过实验验证实际性能，但理论证明的完备性已足够。

（完）
