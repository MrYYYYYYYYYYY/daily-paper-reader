---
title: Can Post-Training Turn LLMs into Good Medical Coders? An Empirical Study of Generative ICD Coding
title_zh: 后训练能否让大语言模型成为优秀的医学编码员？生成式ICD编码的实证研究
authors: "Ziqing Wang, Weihao Li, Shijie Chen, Yuan Luo, Kaize Ding"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13940v1"
tags: ["query:lpt"]
score: 8.0
evidence: 基于强化学习的后训练用于LLM ICD编码
tldr: 自动ICD编码是计费、流行病学等医疗核心任务；生成式LLM因推理时评估常被视为弱编码器，但任务特定后训练尚未深入探索。本文在统一协议下对比判别式基线与LLM编码器，覆盖提示、监督微调（SFT）和基于GRPO的强化学习，并设计PHI诊断课程处理漏码。结果发现：提示评估严重低估LLM潜力；SFT带来主要能力跃升；GRPO在SFT基础上进一步优化代码集预测；PHI在宏观指标上提供针对性增益。结论表明瓶颈在于全分类召回的后训练优化而非生成式公式本身，并公开代码、数据划分和检查点。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13940v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 805, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13940v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1636, \"height\": 672, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13940v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1645, \"height\": 1079, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13940v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1644, \"height\": 1075, \"label\": \"Table\"}]"
motivation: 探索后训练能否释放生成式LLM在ICD编码中的潜力，弥补此前仅关注推理设置而忽视任务特定优化的研究空白。
method: 在统一协议下对比判别式基线与LLM编码器，运用提示、SFT和基于GRPO的强化学习，并引入PHI诊断课程扩展GRPO以改善漏码案例。
result: 提示评估大幅低估LLM能力；SFT带来主要性能跃升，使LLM接近判别式基线；GRPO进一步改善代码集预测，PHI提升宏观F1指标。
conclusion: 生成式ICD编码的主要瓶颈在于模型对全分类召回的后训练优化，而非生成式公式本身；通过SFT和RL可有效释放LLM潜力。
---

## 摘要
自动国际疾病分类（ICD）编码是用于计费、流行病学和临床决策支持的核心医学编码任务。生成式大语言模型（LLMs）常被报道为薄弱的医学编码员，但这一发现主要来源于推理时的设置，如提示、检索、重排序或工具使用，而任务特定的后训练所起的作用尚未被充分探索。我们提出了一项关于生成式ICD编码后训练的控制实证研究，在统一的协议和指标集下，比较了判别式基线与LLM编码员在提示、监督微调和强化学习上的表现。据我们所知，这是首次在ICD编码中评估基于强化学习的后训练用于生成式LLM编码员的研究。我们进一步引入了PHI，一种诊断性课程，它扩展了GRPO以改进遗漏编码案例。我们的结果表明，仅使用提示评估会严重低估LLM在ICD编码上的潜力。监督微调提供了主要的能力跃升，GRPO在监督微调基础上进一步改进了编码集预测，而PHI在宏观性能上带来了针对性的提升。这些发现表明，主要瓶颈并非仅仅是生成式形式本身，而是模型如何针对全分类召回率进行调整和优化。我们在https://github.com/AlexandreWANG915/LLM4ICD 上公开发布代码、数据划分和检查点。

## Abstract
Automated International Classification of Diseases (ICD) coding is a core medical-coding task for billing, epidemiology, and clinical decision support. Generative large language models (LLMs) are often reported as weak medical coders, but this finding mainly comes from inference-time settings such as prompting, retrieval, reranking, or tool use, leaving the role of task-specific post-training underexplored. We present a controlled empirical study of post-training for generative ICD coding, comparing discriminative baselines with LLM coders across prompting, supervised fine-tuning, and reinforcement learning under a common protocol and metric set. To our knowledge, this is the first study to evaluate RL-based post-training for generative LLM coders in ICD coding. We further introduce PHI, a diagnostic curriculum that extends GRPO to refine missed-code cases. Our results show that prompting-only evaluation substantially underestimates the potential of LLMs for ICD coding. SFT provides the main capability jump, GRPO further improves code-set prediction beyond SFT, and PHI provides targeted gains on macro-level performance. These findings suggest that the main bottleneck is not the generative formulation alone, but how the model is adapted and optimized for full-taxonomy recall. We release our code, data splits, and checkpoints at https://github.com/AlexandreWANG915/LLM4ICD.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：自动ICD编码是医疗计费、流行病学和临床决策支持的核心任务，属于极端多标签分类问题（标签空间超过70,000个代码）。传统方法采用判别式模型（基于PLM编码器和分类头），而生成式LLM以往仅在推理时通过提示、检索、重排序或工具使用进行评估，结果常显示为弱编码器（生成无效代码、精确率低）。但任务特定后训练（特别是强化学习）对生成式LLM编码能力的提升尚未被深入探索。
- **整体含义**：本文试图回答：通过后训练（监督微调SFT和强化学习RL），能否将生成式LLM转化为有竞争力的ICD编码器？核心结论是：仅凭提示评估严重低估了LLM的潜力，后训练可以显著缩小与判别式基线的差距，表明瓶颈在于模型适应和优化，而非生成式形式本身。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用分阶段后训练流水线：先SFT（学习输出格式和代码先验），再GRPO（基于F1奖励的强化学习优化代码集），最后PHI（渐进式提示注入，针对漏码案例进行课程学习）。
- **关键技术细节**：
  - **SFT**：在指令-响应对上训练，使用遮掩下一个token目标，仅对助手响应部分的token计算损失。采用LoRA进行参数高效微调。
  - **GRPO**：起始于SFT策略，对每个提示采样G个响应，解析出预测代码集\(\hat{Y}\)，使用样本级F1作为奖励（\(r_{F1} = 2PR/(P+R)\)），然后组内标准化得到优势\(A_j\)，用带裁剪和KL散度惩罚的目标函数更新策略。无需训练价值模型。
  - **PHI（Progressive Hint Injection）**：基于GRPO的扩展。在每个回合，模型在无提示下评估，收集漏码（\(Y_i \setminus \hat{Y}_i\)）形成每个样本的提示池。在下一回合的GRPO训练中，以一定概率从池中采样少量漏码作为上下文提示注入到提示中，但推理时保持无提示。提示采样优先考虑低频和低召回率代码，并采用概率平滑。目标是让模型专注于纠正遗漏，而不改变推理流程。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集**：使用MIMIC-III（ICD-9-CM）和MIMIC-IV（ICD-10-CM）数据集，采用Edin等人（2023）的干净分层划分。每个数据集包含Top-50和Full标签两种设置。
- **Benchmark**：判别式基线（CNN、GRU、CAML、MultiResCNN、LAAT、PLM-ICD），以及LLM提示基线（零样本、少样本、零样本CoT、少样本CoT）和LLM后训练基线（SFT、GRPO、PHI）。所有方法在相同预处理数据上评估。
- **LLM骨干**：Qwen2.5-1.5B和Qwen3-4B。
- **评估指标**：Micro和Macro级别的Precision、Recall、F1（百分数）。判别式模型在验证集上调阈值最大化micro-F1；生成式模型直接解析输出，无阈值。

### 4. 资源与算力

- 文中明确说明：
  - **判别式基线**：在单个A100 80GB GPU上训练。
  - **生成式RL实验**：在4× H100 80GB GPU上运行。
  - **SFT阶段**：约需24小时。
  - **Top-50 GRPO**：约120小时；Full GRPO：约160小时。
  - **每个PHI回合**增加一个GRPO epoch（从上回合检查点初始化）。

### 5. 实验数量与充分性

- **实验数量**：
  - 覆盖2个数据集（MIMIC-III, MIMIC-IV）、2个标签设置（Top-50, Full）、2个骨干模型（1.5B, 4B），以及多种方法（6种判别式基线 + 6种LLM提示 + 4种LLM后训练 = 16种方法组合）。主要结果表格（表1和表2）展示了所有组合的Micro/Macro Recall、Precision、F1。此外，还进行了消融实验（如PHI的分阶段效果，文中未全部列出但提及了对比）。
- **充分性与公平性**：
  - 所有生成式方法使用相同解析器、输出格式和指标集，确保公平。
  - 判别式基线采用已调优的超参数，并在相同预处理数据上重新训练。
  - 实验覆盖了从弱提示性能到后训练性能的全谱系，验证了各阶段贡献。但局限是只有两个相对较小的开源LLM骨干，未涵盖更大模型或闭源模型；另外数据仅来自单一医疗系统（MIMIC）。

### 6. 论文的主要结论与发现

- **提示评估严重低估LLM潜力**：零样本/少样本提示下，LLM性能远低于判别式基线（例如MIMIC-III Full micro-F1仅0.2-5.7%），而判别式基线可达28.3-59.7%。
- **SFT提供主要能力跃升**：SFT后LLM性能大幅提升，例如Qwen3-4B在MIMIC-III Top-50上micro-F1从18.2%提升至72.6%，超越PLM-ICD（68.1%）。
- **GRPO进一步优化代码集预测**：在Full标签设置下，GRPO均优于SFT，尤其在MIMIC-III上提升显著（SFT 45.8% → GRPO 56.5% micro-F1）。在Top-50设置下提升有限，主要是调整精度-召回权衡。
- **PHI提供针对性宏观提升**：PHI在保留GRPO性能的同时，针对漏码案例进行聚焦训练，在Full设置和Macro-F1上获得增益（例如MIMIC-III Full Macro-F1: GRPO 19.4% → PHI 20.0%）。
- **后训练LLM缩小与PLM-ICD的差距**：在Full设置中，GRPO和PHI接近但未完全超越PLM-ICD（PLM-ICD在MIMIC-III Full micro-F1为59.7%，GRPO为56.5%），但已大幅缩小差距。Top-50设置下后训练LLM已持平或超越。
- **核心瓶颈是后训练优化，而非生成式形式**。

### 7. 优点：方法或实验设计上的亮点

- **首次将RL后训练（GRPO）应用于生成式ICD编码**：填补了该领域空白。
- **引入PHI诊断性课程**：创新性地利用训练时漏码提示，在不改变推理接口的情况下提升召回，尤其对低频代码有效。
- **实验设计严谨**：统一协议下对比提示、SFT、GRPO、PHI，控制变量（相同骨干、解析器、指标），且包含Top-50和Full两种难度设置，全面评估各阶段贡献。
- **公开代码、数据划分和检查点**：促进可重复性。
- **针对判别式基线采用已复现的最优设置**（遵循Edin等人2023），确保公平对比。

### 8. 不足与局限

- **数据集局限**：仅使用MIMIC-III和MIMIC-IV，来自单一医疗系统，文档风格和标签分布可能不具代表性。
- **骨干模型限制**：仅评估了Qwen2.5-1.5B和Qwen3-4B两个相对较小的开源模型，未探索更大模型（如7B/13B）、其他模型族或闭源模型（如GPT-4）的表现，因此结论的泛化性有限。
- **Full标签设置仍具挑战**：后训练LLM在Full设置中未完全超越PLM-ICD，尤其是低频代码召回仍有差距。
- **PHI的增益主要体现在宏观指标，微观提升有限**：这表明漏码恢复尚未转化为微观性能的大幅提升，可能仍有改进空间。
- **计算资源要求高**：GRPO和PHI需要多GPU长时间训练，对于低资源场景可能不切实际。
- **未深入分析提示注入对模型训练稳定性的影响**（如过拟合提示格式的风险）。

（完）
