---
title: "Modularized Reinforcement Learning on LLMs: From MDP Creation to Exploration and Learning"
title_zh: 大语言模型上的模块化强化学习：从MDP构建到探索与学习
authors: "Zhao Yang, Yuxuan Jiang, Ting-Chih Chen, Lincen Yang, Annie Wong, Chao Gao, Jacob E. Kooi, Zhong Li, Jiayang Shi, Kevin Qiu, Qi Huang, Xinrui Zu, Shiping Yang, Hengyuan Zhang, Ngai Wong, Filip Ilievski, Shujian Yu, Aske Plaat, Zhaochun Ren, Mark Hoogendoorn, Vincent François-Lavet"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21943v1"
tags: ["query:lpt"]
score: 10.0
evidence: 关于LLM后训练中强化学习的全面综述
tldr: 强化学习在LLM后训练中至关重要，但当前主流方法PPO和GRPO仅代表冰山一角。该综述围绕MDP创建、探索和学习三大阶段构建分类学，系统梳理了奖励函数、状态空间、温度采样、信用分配等设计决策。将现有LLM研究映射到该框架后发现，无批评者策略梯度和蒙特卡洛信用分配高度密集，而基于值的方法、离策略演员-评论家及自举信用分配几乎空白。这些空白为迁移经典RL成熟技术提供了明确机会，为推动LLM训练方法创新奠定了共享基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21943v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1398, \"height\": 633, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21943v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1401, \"height\": 225, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21943v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 947, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21943v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1549, \"height\": 868, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21943v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 783, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1315, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1565, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1312, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 967, \"height\": 414, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1057, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1277, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21943v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1617, \"height\": 1535, \"label\": \"Table\"}]"
motivation: 当前LLM后训练过度依赖PPO和GRPO，需系统审视RL算法设计决策以探索更广泛可能性。
method: 围绕MDP创建（奖励、状态、动作、终止、折扣）、探索（温度采样、熵正则、树搜索等）和学习（四个RL维度）构建分类学，并映射LLM文献分布。
result: 发现批评者free策略梯度和蒙特卡洛信用分配密集，但基于值、离策略演员-评论家和自举信用分配在LLM中几乎未被探索。
conclusion: 明确的研究空白为将经典RL成熟方法迁移至LLM训练提供了具体路径，为研究者提供了统一框架。
---

## 摘要
强化学习已成为大语言模型后训练的核心方法，但当前主流流程中占主导地位的PPO和GRPO仅代表了强化学习所能提供的很小一部分。理解这些方法为何盛行以及存在哪些替代方案，需要对任何强化学习算法背后的设计决策进行原则性的审视。本综述围绕算法构建的三个阶段组织这一审视。我们首先从MDP构建开始：如何（或可能如何）为大语言模型训练定义奖励函数、状态空间、动作空间、终止条件和折扣因子。然后转向探索，涵盖温度采样、熵正则化、内在动机、树搜索和课程学习。最后，我们沿着四个经典强化学习维度讨论学习：无模型与基于模型、基于价值与基于策略与演员-评论家、同策略与异策略，以及信用分配，包括依赖完整回报估计的蒙特卡洛方法和使用其他学习预测进行更新的自助法方法。将大语言模型文献映射到此分类法上，显示出研究工作的分布非常不均匀。无评论家策略梯度和蒙特卡洛信用分配研究密集，而基于价值的方法、异策略演员-评论家训练和基于自助法的信用分配在很大程度上尚未被探索，尽管在经典强化学习中有成熟的对应方法。这些差距代表了将经过验证的强化学习技术迁移到大语言模型训练中的具体机会。通过明确这些差距以及已被证明有效的方法，本综述为强化学习和大语言模型领域的研究人员提供了一个共享框架，用于理解当前实践并识别未来工作的有希望方向。

## Abstract
Reinforcement learning (RL) has become central to LLM post-training, yet the methods that dominate current pipelines, PPO and GRPO, represent only a narrow slice of what RL offers. Understanding why these methods prevail, and what alternatives exist, requires a principled examination of the design decisions that underlie any RL algorithm.   This survey organizes that examination around three stages of algorithm construction. We begin with MDP creation: how the reward function, state space, action space, termination condition, and discount factor are, or could be, defined for LLM training. We then turn to exploration, covering temperature sampling, entropy regularization, intrinsic motivation, tree search, and curriculum learning. Finally, we address learning along four classical RL dimensions: model-free versus model-based, value-based versus policy-based versus actor-critic, on-policy versus off-policy, and credit assignment, including both Monte Carlo methods, which rely on full return estimates, and bootstrapping methods, which update estimates using other learned predictions.   Mapping the LLM literature onto this taxonomy reveals a strikingly non-uniform distribution of research effort. Critic-free policy gradients and Monte Carlo credit assignment are densely populated, while value-based methods, off-policy actor-critic training, and bootstrapping-based credit assignment remain largely unexplored despite well-established counterparts in classical RL. These gaps represent concrete opportunities for transferring proven RL techniques to LLM training.   By making these gaps explicit alongside the methods that have proven effective, this survey offers researchers in both RL and LLMs a shared framework for understanding current practice and identifying promising directions for future work.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景与动机**：尽管强化学习（RL）已成为大语言模型（LLM）后训练（post‑training）的核心手段（例如 InstructGPT 使用 PPO，DeepSeek‑R1 使用 GRPO），但当前主流方法仅涵盖了 RL 丰富工具箱的很小一部分。大量论文只修改单一组件（如优势估计、裁剪策略、课程调度），却未将其置于统一的 RL 设计空间中进行比较。这种碎片化使得研究者难以判断不同方法之间的实质差异，也难以识别哪些设计选择尚未被探索。
- **关键问题**：如何系统性地梳理 LLM 训练中 RL 算法的所有设计决策，并揭示当前研究投入分布的不均衡性，从而为迁移经典 RL 已有成果提供清晰路径。
- **整体含义**：本文通过建立从 RL 第一性原理出发的分类学（taxonomy），将 LLM 文献映射到三大算法构建阶段（MDP 创建、探索、学习），直观展示出“无评论家策略梯度”与“蒙特卡洛信用分配”等区域研究密集，而“基于价值的方法”“异策略演员‑评论家”“自举信用分配”等领域几乎空白。这为两个领域的研究者提供了共享的概念框架，并指出了结构性的未来研究方向。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将 RL 应用于 LLM 训练的过程解构为四个模块化组成部分，每一部分又可细分为多个子类别，构成完整分类体系。
- **关键技术细节**（按三大阶段依次说明）：
  1. **MDP 创建**（Section 3）：
     - **奖励函数**：分为三类来源——学习型模型（如 Bradley‑Terry 奖励模型、过程奖励模型 PRM）、基于规则的验证奖励（如数学答案正确性、代码通过单元测试）、LLM 作为裁判（如 RLAIF、自奖励循环）。奖励塑造（reward shaping）用于增加密集信号（如长度惩罚、格式遵守、步骤进展）。奖励破解（reward hacking）问题被详细讨论（如 sum‑form 信用分配导致推理过程操纵、KL 受限情况下性能退化）。
     - **状态空间**：通常为完整 token 序列，但可通过压缩（摘要、擦除中间步骤）或转向隐空间（如 COCONUT）来管理长上下文。
     - **动作空间**：默认全词汇表（128K tokens），可选约束解码（语法约束）或非 token 动作（隐动作空间如 BWArea/CoLA）。
     - **终止条件**：生成结束符或最大长度，可通过硬截断（ThinkPrune）或软惩罚（LEASH、DAPO）来调控。
     - **折扣因子**：几乎固定为 γ=1，但理论上 γ<1 可作为隐式长度正则器。
  2. **探索**（Section 4）：
     - **温度采样**：通过温度参数 τ 控制采样的随机性。
     - **熵正则化**：在训练目标中增加熵惩罚防止策略过早确定化，但研究显示均匀熵正则化可能低效，需按 token 位置自适应调整（如 Cheng et al.、Cui et al.）。
     - **内在动机（Intrinsic Motivation）**：基于预测误差（RND、ICM）、伪计数（MERCI）或不确定性的辅助奖励，鼓励探索新颖轨迹。按粒度分为 token 级（CD‑RLHF）和轨迹级（i‑MENTOR）。
     - **树搜索（Tree Search）**：借鉴 AlphaZero 的 MCTS 用于结构化探索，如 TreeRL、TreePO、BFS‑PO 在训练中实现选择性分支。
     - **课程学习**：沿三个维度——数据采样（难易调度）、起始状态（反向课程如 R3）、奖励信号（从稠密到稀疏如 DELTA‑Code）。
  3. **学习**（Section 5）：
     - **数据表示**：几乎全部使用无模型方法（model‑free），但预训练模型可视为隐式语言世界模型。
     - **解表示**：分为三类——
       - 基于价值的方法（Q‑SFT、ShiQ、Q♯）重新利用 LLM 或附加值网络。
       - 基于策略的方法（无评论家）：ReMax、RLOO、GRPO 及其大量变体（GSPO、GFPO、DPO 等）。这类方法因可重置环境、批量采样效率高、节省内存而成为默认选择。
       - 演员‑评论家方法：PPO 及其改进（AsyPPO、VC‑PPO、VAPO）用于长 CoT 任务中值估计偏差和稀疏奖励问题；SAC 风格的直接通过评论家梯度更新演员的方法在 LLM 中尚未实现。
     - **数据来源**：绝大多数为同策略（on‑policy），少数异策略方法（LUFFY、CHORD、BAPO、ReMix）通过经验回放或重要性采样重用旧数据，但仍受限于重要性采样的高方差。
     - **信用分配**：按两个维度分类——
       - 备份深度（backup depth）：蒙特卡洛（完整回报） vs 一步 TD vs λ‑return（如 GAE、GRPO‑λ、TEMPO）。
       - 动作影响度量：GRPO 的组相对优势、VinePPO 的 token 级 MC 优势、Tempo 的树分支门控 TD 修正。大量 GRPO 变体专注于改进优势估计、重要性采样或裁剪机制（详见表 9）。
  4. **其他 RL 框架**（Section 6）：简要覆盖元 RL、多智能体 RL、离线 RL、分层 RL 在 LLM 中的应用。

### 3. 实验设计（非实验性综述的特殊说明）

- **本文自身不包含新实验**：作为综述，它没有设计新数据集或 benchmark，也未报告任何实验对比。
- **它映射的现有实验设置**：文中大量引用了具体论文中的实验场景，例如：
  - 奖励模型：HH‑RLHF 数据集、PRM800K（数学推理）、CodeRL（代码生成）。
  - 探索方法：数学推理（DeepSeek‑Math）、代码生成、GUI 智能体。
  - 学习算法对比：PPO vs GRPO vs DPO，在总结任务（TL;DR）、数学推理、安全对齐等 benchmark 上。
- **Benchmark** 常见参考：MMLU、GSM8K、MATH、HumanEval、Codeforces、DeepSeek‑R1 测试集等。
- **对比方法**：通过表 9 等系统整理了数十种 GRPO 变体（VinePPO、GFPO、GTPO、DPO 等）的动机和解决方案。

### 4. 资源与算力

- **文中明确提及的算力信息很少**：只有少数地方提到如 ReMax 相比 PPO 可“将 GPU 内存减半、吞吐量加倍”；VAPO 等大型训练使用了多个 GPU 但不给出具体型号或时长。
- **整篇综述未统一汇总计算资源**（例如总 GPU 小时数、集群规模、模型规模范围），因为它是文献梳理而非实验报告。
- **建议**：对于试图复现具体方法的读者，需参考原文附录。

### 5. 实验数量与充分性

- **覆盖范围**：参考文献约 280 篇，涵盖 2020‑2026 年间的关键工作，分类表格（表 1‑9）系统总结了不同设计维度的现有方法。
- **实验设计的充分性**：本文不进行自身实验，因此无法评价其消融或对比实验的统计学充分性。然而，它作为综述，对现有实验密度进行了定性分析，清晰指出了哪些区域研究充分、哪些区域稀疏——这种映射本身是充分的，因为它基于对大量原始论文实验结果的归纳。
- **客观性与公平性**：分类标准基于经典 RL 理论，具有较强的客观性；对主流方法（如 GRPO）的密集区域和替代方法（如基于价值、异策略演员‑评论家）的空白区域都进行了同等尺度的讨论，没有明显偏袒某一派。

### 6. 论文的主要结论与发现

1. **研究分布极端不均**：无评论家策略梯度（GRPO 及其变体）和蒙特卡洛信用分配几乎占据了所有努力，而基于价值的方法、异策略演员‑评论家训练以及自举信用分配在 LLM 训练中几乎空白。
2. **现有默认选择有其合理原因**：例如无评论家方法因可重置环境、批量采样、内存节省而成为实际默认；蒙特卡洛信用分配因稀疏终末奖励简单有效。
3. **结构性的未探索领域是经典 RL 技术迁移的明确机会**：如 Q‑学习、SAC、GAE 的变体、自举 TD 学习、离线 RL 与在线 RL 混合等。
4. **探索策略仍以局部 token 级为主，缺乏对策略级/轨迹级多样性的显式激励**。
5. **信用分配改进的空间**：大多数 GRPO 变体只改进单个组件（优势、重要性采样、裁剪），而底层蒙特卡洛备份和轨迹级信用不变，更根本的变革（如自举、过程奖励模型、价值学习）可能带来更大收益。

### 7. 优点

- **原则性分类框架**：从经典 RL 基础出发，将 LLM 训练解构为可独立分析的设计模块，使任何新方法都能快速定位其创新点。
- **全面且系统**：覆盖奖励设计、探索机制、学习算法四大维度以及额外框架，每个部分都附有开放问题和“Take‑away”总结框，便于读者抓住要点。
- **表格丰富**：表 9（GRPO 变体总结表）等极大地方便了研究者横向对比不同工作的问题定义和解决方案。
- **跨领域桥梁**：为 RL 和 LLM 两个社区提供了共同语言，有助于避免重复发明轮子，促进成熟技术的迁移。
- **明确地标识了空白**：不仅指出“什么已被做”，还系统性地指出“什么尚未被做”，为未来工作给出具体候选方向。

### 8. 不足与局限

- **无新实验结果**：本文是纯综述，没有通过实验验证其分类的有效性或检验空白方向的实际收益。因此它所声称的“机会”仍待实证验证。
- **分类粒度可能仍有争议**：例如某些方法同时涉及多个类别（如 GRPO 既是策略梯度又使用组相对优势），分类边界在少数情况下可能模糊。
- **时间覆盖**：截止于 2026 年 6 月，最新工作可能已补充部分空白，但整体趋势仍成立。
- **计算资源与可重复性信息缺失**：未汇总各论文的 GPU 消耗、数据量、超参数等，不利于大算力条件下的可行性评估。
- **对 LLM‑as‑Judge 等新兴奖项的局限性讨论**：已提及位置偏差、事实可靠性等问题，但未深入分析这些偏差如何与 RL 循环互相强化。
- **偏重主流模型（如 DeepSeek、Llama）**，对较小规模或非自回归架构的覆盖较少。

（完）
