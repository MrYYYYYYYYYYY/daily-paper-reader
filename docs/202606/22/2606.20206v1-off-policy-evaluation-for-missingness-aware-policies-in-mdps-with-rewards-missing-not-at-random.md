---
title: Off-Policy Evaluation for Missingness-Aware Policies in MDPs with Rewards Missing Not at Random
title_zh: 马尔可夫决策过程中针对非随机缺失奖励的缺失感知策略的离线策略评估
authors: "Ziheng Wei, Annie Qu, Rui Miao"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20206v1"
tags: ["query:lpt"]
score: 8.0
evidence: 强化学习中缺失奖励的离线策略评估
tldr: 离线强化学习中，奖励常因记录稀疏或审查而缺失，且为缺失非随机（MNAR）模式，导致选择偏差。本文在有限时域MDP中，利用未来状态作为影子变量，引入桥函数恢复条件均值奖励，并通过min-max估计避免双重采样。进一步提出Fitted-Q-Evaluation风格的估计器，允许目标策略依赖历史缺失指示。理论证明了估计量的一致性和有限样本误差界，在模拟数据和MIMIC-III败血症数据上，方法相较基线显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20206v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 711, \"height\": 262, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20206v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1680, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20206v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1248, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20206v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1732, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20206v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1717, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20206v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1723, \"height\": 493, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20206v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1722, \"height\": 376, \"label\": \"Table\"}]"
motivation: 奖励非随机缺失（MNAR）破坏可忽略性，导致选择偏差，现有OPE方法失效。
method: 使用未来状态作影子变量，桥函数恢复条件均值，min-max估计避免双重采样，FQE式估计器传播恢复奖励。
result: 估计量一致且有有限样本误差界，实验在模拟和MIMIC-III数据上优于现有方法。
conclusion: 提出了一种处理奖励MNAR的OPE方法，兼具理论保证与实证效果。
---

## 摘要
在离线强化学习中，历史批处理数据中的即时奖励常常因记录稀疏或不规则而未被观测到，或因超出某些奖励值而被截断。这一问题出现在包括医疗保健和市场营销在内的实际场景中。我们研究了当奖励非随机缺失（MNAR）时，有限水平马尔可夫决策过程中的离线策略评估（OPE）。MNAR破坏了可忽略性，即使在以状态和动作为条件后也会导致选择偏差。为了解决这个问题，我们形式化了一个依赖于奖励的倾向模型，并使用未来状态作为阴影变量来识别完整数据条件下的均值奖励。我们进一步引入了一个桥函数，可以在不显式建模MNAR机制的情况下恢复条件均值奖励，并通过最小-最大过程进行估计以避免双重采样。基于这些识别结果，我们提出了一种拟合Q评估风格的估计器，该估计器传播恢复的奖励，同时允许目标策略依赖于过去的缺失指示符。最后，我们建立了我们的OPE估计器的一致性和有限样本误差界，并通过实验展示了我们的方法在模拟数据和MIMIC-III脓毒症数据上相比现有方法的强大性能。

## Abstract
In offline Reinforcement Learning, immediate rewards in logged batch data are often unobserved due to sparse or irregular record-keeping, or censored beyond certain reward values. This issue arises in practical settings, including health care and marketing. We investigate off-policy evaluation (OPE) in finite-horizon Markov decision processes when rewards are missing not at random (MNAR), which breaks ignorability and induces selection bias even after conditioning on states and actions. To address this, we formalize a reward-dependent propensity model and use future states as shadow variables to identify the full-data conditional mean reward. We further introduce a bridge function that recovers the conditional mean reward without explicitly modeling the MNAR mechanism, and estimate it via a min-max procedure to avoid double sampling. Building upon these identification results, we propose an Fitted-Q-Evaluation-style estimator that propagates the recovered rewards while allowing target policies to depend on past missingness indicators. Finally, we establish consistency and finite-sample error bounds for our OPE estimator, and show through experiments the strong performance of our method compared to existing methods on simulated and MIMIC-III Sepsis data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在离线强化学习（Offline RL）中，离线数据集的即时奖励常因记录稀疏、审查或无法观测而出现缺失，尤其是当缺失机制为**非随机缺失（MNAR）** 时——即奖励的缺失概率与奖励本身的值相关（例如，高价值医疗结果更可能被漏报）。这种缺失破坏了标准的可忽略性假设，在条件于状态和动作后仍会产生选择偏差，导致传统的离线策略评估（OPE）方法（如Fitted Q-Evaluation、重要性采样）产生有偏估计。
- **研究动机**：医疗、数字广告、推荐系统等实际应用场景中，奖励MNAR现象普遍存在。现有OPE方法要么忽略缺失（导致偏差），要么依赖外部辅助变量或强参数假设。本文致力于在有限时域马尔可夫决策过程（MDP）中，实现**无需显式建模缺失机制**、且**允许目标策略依赖历史缺失指示符**的无偏策略值评估。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用MDP中固有的**未来状态**作为**影子变量**，识别完整数据条件下的条件均值奖励；引入**桥函数**恢复缺失奖励，避免IPW的高方差；结合FQE框架传播恢复后的奖励，并允许目标策略利用历史缺失信息。
- **关键技术细节**：
    1. **模型假设**：
        - 假设奖励缺失概率依赖于当前状态、动作和奖励本身（MNAR），但给定当前奖励后，未来状态与缺失指示符独立（排除未来依赖）。
        - 未来状态作为影子变量需满足：**排除限制**（条件于奖励、状态、动作后，未来状态与缺失独立）和**相关性**（在观测子集上，未来状态仍对奖励具有信息）。
        - 扩展状态 $\tilde{S}_t = (S_t, O_{t-1})$ 保持马尔可夫性质。
    2. **桥函数** $b_t(S_t, A_t, S_{t+1})$：满足条件矩条件 $E[b_t(S_t, A_t, S_{t+1}) | R_t, S_t, A_t] = R_t$，从而在给定当前状态和动作下，用 $E[b_t(S_t, A_t, S_{t+1}) | S_t, A_t]$ 代替 $E[R_t | S_t, A_t]$，实现无偏恢复。
    3. **估计方法**：采用**min-max估计**（基于Dikkala等2020）求解条件矩模型，避免双重采样问题。具体地，对每个时间步 $t$，在观测子集上优化：
        $$\min_{b_t \in \mathcal{B}^{(t)}} \sup_{g_t \in \mathcal{G}^{(t)}} \frac{1}{n_t} \sum_{i: O_{t,i}=1} \big[ (b_t(S_{t,i},A_{t,i},S_{t+1,i}) - R_{t,i}) g_t(R_{t,i},S_{t,i},A_{t,i}) \big] - \lambda (\|g_t\|^2_{\mathcal{G}^{(t)}} + \frac{U}{\delta^2}\|g_t\|_2^2) + \lambda \mu \|b_t\|^2_{\mathcal{B}^{(t)}}.$$
    4. **Proximal FQE 算法**（Algorithm 1）：
        - 从 $t=T$ 到 $1$，依次：① 在观测样本上训练桥函数 $\hat{b}_t$；② 构造插补奖励 $\tilde{R}_{t,i} = O_{t,i} R_{t,i} + (1-O_{t,i})\hat{b}_t(S_{t,i},A_{t,i},S_{t+1,i})$；③ 用惩罚非参数最小二乘拟合 $Q$ 函数 $Q_t$，其目标值 $y_{t,i}$ 为插补奖励加下一状态值（算法用 $bV_{t+1}$）。
        - 最终输出策略值估计 $\hat{V}(\pi) = \frac{1}{n}\sum_i \hat{V}_1^\pi(S_{1,i},0)$。
- **理论保证**：证明了桥函数的投影误差界、$L_2$ 误差界（结合病态系数），以及策略值估计误差在非参数设定下的一致性和有限样本误差界，误差以 $O(K \tau_{\max} T^2 \sqrt{\log T}\, n^{-\frac{\alpha_{\min}}{2\alpha_{\min}+1}} \log n)$ 衰减。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **模拟数据**：
    - 状态为二维连续向量，动作空间二进制 $\{-1,1\}$，奖励生成有两种机制：sigmoid 奖励和线性剪辑奖励。
    - 缺失率通过倾向得分控制，目标缺失率分别为约 20%、40%、80%。
    - 改变样本量（$n = 64,128,256,512,1024,2048$，固定 $T=8$）和地平线长度（$T=2,4,8,16,32$，固定 $n=512$）。
- **真实数据**：MIMIC-III 脓毒症数据集，预处理后包含 13,943 个病人，48 维状态，25 个离散动作（5 种血管升压药 ×5 种静脉输液量），地平线 $T=10$。由于原始奖励完全观测，人为引入 MNAR 缺失（20%~80%）。
- **基准（baseline）**：
    1. **Oracle FQE**：使用完全观测奖励的 FQE（仅作为参考标准，不参与比较）。
    2. **Naive FQE**：忽略缺失机制，直接对观测样本运行 FQE。
    3. **IPW-FQE**：基于 Wang et al. (2025) 的 IPW 方法，先拟合桥函数估计倾向得分，再在观测样本上加权回归。
    4. **Impute-FQE**：用观测样本拟合回归模型估计 $E[R_t|S_t,A_t]$，然后插补缺失奖励，再运行 FQE。
    5. **SCOPE**：基于奖励塑形的每步重要性采样方法。
- **评估指标**：均方误差（MSE）、绝对偏差、估计值及其标准误。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **文中未明确提及使用的 GPU 型号、数量或训练时长**。仅提及实验基于 Python 实现，使用了高斯核 RKHS（模拟数据）和神经网络函数逼近（MIMIC-III）。未涉及大规模分布式训练或高算力需求。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量**：
    - 模拟数据：覆盖 3 种缺失率（20%、40%、80%）、6 个样本量、5 个地平线长度、2 种奖励生成机制（sigmoid/linear），每种条件设置下使用 50 个随机种子，形成多组重复实验。
    - MIMIC-III：4 种缺失率（20%、40%、60%、80%），使用 60/40 拆分拟合/评估，报告估计值和标准误。
- **充分性与公平性**：
    - 模拟实验变量覆盖全面（样本量、地平线、缺失率、奖励生成），基线方法涵盖忽略缺失、IPW、插补、奖励塑形等主流策略，对比结果清晰。
    - 真实数据实验的设计合理，构造了符合实际临床背景的缺失机制和目标策略（依赖历史缺失的保守剂量调整）。
    - 未进行显式的超参数消融研究（如不同核函数、正则化强度的影响），但通过交叉验证选择超参数。结论主要基于 MSE 和偏差比较，统计量中包含了标准误，结果具有可重复性。

### 6. 论文的主要结论与发现

1. **方法有效性**：提出 ProxFQE 在所有缺失率、样本量和地平线长度下均显著优于基线方法，尤其在严重缺失（80%）和长地平线（$T=32$）时优势更大。
2. **理论支撑**：严格证明了在满足完备性和正则化条件下，桥函数和策略值估计量是一致且具有多项式率的有限样本误差界。
3. **经验验证**：模拟实验中 ProxFQE 的 MSE 随样本量增加稳定下降，而基线方法出现偏差平台或爆炸性增长（impute-FQE 在长地平线指数增长，SCOPE 在 MIMIC-III 上产生退化结果）。
4. **实际应用价值**：在 MIMIC-III 数据上，ProxFQE 的偏差始终低于所有基线，且在 80% 缺失率下仍保持相对合理的估计。

### 7. 优点：方法或实验设计上有哪些亮点

- **方法论亮点**：
    - 利用内生变量（下一个状态）作为影子变量，无需额外数据收集或领域知识选择外部工具变量。
    - 桥函数法避免了显式建模 MNAR 缺失机制（高方差风险），且通过 min-max 估计绕过双重采样问题。
    - 允许目标策略依赖历史缺失指示符，更符合实际决策场景（如医生根据是否获取上一轮结果调整治疗）。
- **理论亮点**：
    - 在非参数设定下提供了完整的识别性证明和有限样本误差界，且误差率接近最优非参数回归率。
    - 引入病态系数量化条件矩问题的逆不稳定性，并给出桥函数 L2 误差的控制。
- **实验亮点**：
    - 模拟实验系统性地变化样本量、地平线长度、缺失率和奖励生成机制，全面评估方法稳健性。
    - 真实数据实验采用了具有挑战性的 MIMIC-III 脓毒症数据，动作空间大（25 个），状态维度高（48 维），并引入实际医生行为启发式的缺失感知目标策略。
    - 与多种代表性基线对比，报告了标准误，结论可信。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **理论假设较强**：依赖于完备性、影子变量排除限制、桥函数可实现性等，当这些假设被违反时估计可能严重偏差。文中承认了这一局限，并提议未来引入敏感性分析。
- **实验覆盖**：
    - 模拟数据的状态/动作空间较简单（连续二维状态、二元动作），与实际应用（高维、离散/连续混合动作空间）存在差距。
    - 未比较更多先进的 OPE 方法，如双鲁棒估计（DR）、模型基方法等。
    - 未包含消融实验分析桥函数 min-max 估计中不同正则化参数（λ, μ, U, δ）的影响，也未对比不同核函数的选择。
- **计算复杂度**：min-max 估计需要求解双层优化，在大规模高维数据上可能计算成本较高，论文未报告具体运行时间或内存消耗。
- **应用限制**：
    - 要求未来状态可作为影子变量，该假设在部分任务（如奖励与状态转移高度独立）中可能不成立。
    - 需要满足正性假设（每个奖励值都有非零的观测和缺失概率），在极限奖惩下可能违背。
- **偏差风险**：即使方法正确，桥函数估计的误差仍会通过 Bellman 回传累积，长期望误差受 $T^2$ 因子影响，当 $T$ 极大时难以实用。

（完）
