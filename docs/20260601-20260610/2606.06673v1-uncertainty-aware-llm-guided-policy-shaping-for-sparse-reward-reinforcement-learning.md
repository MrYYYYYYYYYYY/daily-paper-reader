---
title: Uncertainty-Aware LLM-Guided Policy Shaping for Sparse-Reward Reinforcement Learning
title_zh: 不确定性感知的LLM引导策略塑形用于稀疏奖励强化学习
authors: "Ujjwal Bhatta, Utsabi Dangol, Sumaly Bajracharya, Rodrigue Rizk, KC Santosh"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06673v1"
tags: ["query:lpt"]
score: 9.0
evidence: 面向稀疏奖励强化学习的大模型引导策略塑形
tldr: "稀疏奖励和任务异构导致强化学习收敛慢、探索低效。提出ULPS框架，利用A*生成符号轨迹微调BERT模型，以蒙特卡洛丢包估计不确定性并调节LLM建议权重，再通过熵混合机制自适应平衡LLM引导与PPO策略。在MiniGridUnlockPickup上，微调后执行准确率提升9%，样本效率与累积奖励显著改善。该方法融合符号规划、语言先验与不确定性控制，为稀疏奖励多任务RL提供系统方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06673v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06673v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 897, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06673v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06673v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 727, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06673v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1477, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06673v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 543, \"height\": 362, \"label\": \"Table\"}]"
motivation: 稀疏奖励与任务异构导致强化学习收敛慢、泛化差，探索效率低下，需外部知识引导。
method: "用A*合成轨迹微调BERT生成动作建议，MC dropout估计认知不确定性调节建议权重，熵混合自适应平衡LLM与PPO策略。"
result: "在MiniGridUnlockPickup上，ULPS相比基线的成功率和奖励AUC提升，执行准确率提高9%，样本复杂度降低。"
conclusion: "结合符号A*轨迹、预训练语言先验和不确定性控制，为稀疏奖励多任务RL提供有效且可扩展的框架。"
---

## 摘要
稀疏奖励和异构任务序列仍然是强化学习（RL）中持续存在的挑战，通常导致收敛缓慢、泛化能力弱和探索效率低下。我们提出不确定性感知的LLM引导策略塑形（ULPS），这是一种新颖的框架，将校准的大语言模型（LLM）集成到RL训练循环中，以提供结构化的、不确定性调节的行为指导。ULPS采用基于A*的预言机来合成最优符号轨迹，用于微调基于BERT的语言模型。在训练过程中，该模型提供动作建议，其影响取决于通过蒙特卡洛（MC）dropout估计的认知不确定性。基于熵的混合机制自适应地平衡LLM指导和学到的策略（通过近端策略优化，PPO），使智能体能够优先考虑可靠的先验，同时保持适应性。我们在MiniGridUnlockPickup基准上评估ULPS，并观察到在成功率、奖励效率和样本复杂度方面，相比于无引导、未校准和标准RL基线的一致性改进。ULPS在微调后执行准确性提高了9%以上，需要更少的环境交互，并产生更高的奖励AUC。我们的结果表明，集成符号A*轨迹、预训练语言先验和不确定性感知控制为稀疏奖励领域的多任务强化学习提供了一种有原则且有效的方法，具有扩展到部分可观测和多智能体设置的潜力。

## Abstract
Sparse rewards and heterogeneous task sequences remain persistent challenges in Reinforcement Learning (RL), often resulting in slow convergence, weak generalization, and inefficient exploration. We propose Uncertainty-Aware LLM-Guided Policy Shaping (ULPS), a novel framework that integrates a calibrated Large Language Model (LLM) into the RL training loop to provide structured, uncertainty-modulated behavioral guidance. ULPS employs an A*-based oracle to synthesize optimal symbolic trajectories, which are used to fine-tune a BERT-based language model. During training, this model supplies action suggestions whose influence is conditioned on epistemic uncertainty estimated via Monte Carlo (MC) dropout. An entropy-based blending mechanism adaptively balances LLM guidance and the learned policy (via Proximal Policy Optimization, PPO), allowing the agent to prioritize reliable priors while preserving adaptability. We evaluate ULPS on the MiniGridUnlockPickup benchmark and observe consistent improvements in success rate, reward efficiency, and sample complexity over unguided, uncalibrated, and standard RL baselines. ULPS achieves more than 9% improvement in execution accuracy after fine-tuning, requires fewer environment interactions, and yields higher reward AUC. Our results demonstrate that integrating symbolic A* trajectories, pretrained language priors, and uncertainty-aware control offers a principled and effective approach to multi-task reinforcement learning in sparse-reward domains, with potential extensibility to partially observable and multi-agent settings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在稀疏奖励（sparse-reward）和任务序列异构（heterogeneous task sequences）的强化学习（RL）场景中，智能体面临探索效率低下、收敛缓慢、泛化能力弱等难题。
- **背景动机**：传统RL方法（如随机探索策略）需要数千个episode才能发现有效轨迹；人类反馈（RLHF）等外部知识引导方法受限于人工标注成本；直接引入大语言模型（LLM）则存在过度依赖、不稳定和幻觉风险。
- **整体目标**：提出一种不确定性感知的LLM引导策略塑形框架（ULPS），通过校准LLM的置信度，动态调节其建议权重，实现高效、可靠的探索与学习。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：结合符号规划（A*）、预训练语言模型（BERT）和不确定性估计（MC dropout），以熵为调节量，自适应融合LLM先验与PPO策略，在稀疏奖励环境中引导智能体高效探索。
- **关键技术细节**：
  - **A* 预言机**：在MiniGrid网格世界中，使用曼哈顿距离启发式函数，计算从起点到目标的最优符号轨迹（动作序列），用于生成微调数据。
  - **BERT 微调**：将状态转换为文本提示（如编码的网格布局），用A*生成的21,500个样本微调bert-base-uncased，使其学会预测最优动作（分类到5个离散动作）。
  - **MC dropout 不确定性估计**：在推理时对每个状态执行T=8次随机前向传播（dropout率p=0.1），得到动作概率分布\(P_{\text{LLM}}\)和熵\(H\)，并归一化得到\(H_{\text{norm}}\)。
  - **策略融合**：最终策略为凸组合：\(P_{\text{final}} = (1-H_{\text{norm}}) \cdot P_{\text{LLM}} + H_{\text{norm}} \cdot P_{\text{agent}}\)。熵高（模型不确定）时更依赖PPO学到的策略，熵低时更信任LLM建议。
  - **PPO 训练**：PPO智能体使用bert特征（768维）作为共享输入，通过actor-critic网络输出\(P_{\text{agent}}\)；使用GAE(λ=0.95)、clipping(ε=0.2)更新策略；经验缓存每50个episode更新一次。
- **算法流程**（对应Algorithm 1）：
  1. 每个episode初始化状态S。
  2. 状态转文本提示，通过T次MC dropout得到\(P_{\text{LLM}}\)和\(H_{\text{norm}}\)。
  3. 用PPO网络得到\(P_{\text{agent}}\)，并按公式融合为\(P_{\text{final}}\)。
  4. 采样动作A，执行并存储经验(S,A,R,S’)到buffer。
  5. 每50个episode，用GAE和裁剪损失更新PPO网络。

## 3. 实验设计
- **Benchmark / 环境**：MiniGrid-UnlockPickup（稀疏奖励，包含三个子任务：取钥匙、开门、到达目标）。环境尺寸包括4×4和8×8（LLM微调在8×4上进行）。
- **对比方法**：
  - 无引导RL（PPO）
  - 未校准LLM（同模型但不使用MC dropout和不确定性加权）
  - 线性RL（PPO）
  - Q-Learning（使用GRPO）
  - DQN（使用GRPO，含Double DQN、dueling网络、优先经验回放）
  - 基线：Shoaeinaeini & Harrison (2024) 在4×4和3×3上的结果
- **指标**：准确率（与A*预言机一致的比例）、Brier Score（BS）、期望校准误差（ECE）、判别分析（DA / AUC-ROC）、成功率、平均步数、平均奖励、奖励AUC。
- **评估细节**：训练1,000个episode，每episode最多50步；固定随机种子42；超参数（dropout率0.1、前向传递8次）经消融实验确定。

## 4. 资源与算力
- **文中未明确说明使用的GPU型号、数量以及训练时长**。
- 只提到使用了bert-base-uncased（约1.1亿参数），PPO网络小（768→256→5/1），训练1,000 episode（4×4环境平均约7.3步/episode，总步数7,286）；8×8环境平均15.4步/episode，总步数15,478。
- 消融实验探索了不同dropout率（0.05/0.1/0.2）和forward passes（4/8/12），未报告具体GPU资源或时间。

## 5. 实验数量与充分性
- **实验组数**：
  - 主要在4×4和8×8两种环境上进行。
  - 对比方法共6种（含多种传统RL） + 2种基线（未校准LLM、无引导RL）。
  - 消融实验：改变dropout率和前向传递次数共9种组合（表III）。
  - 校准指标对比：与先前工作[15]在4×4和3×3上的结果对比（表I）。
- **充分性评价**：
  - **充分**：覆盖了多种基线（传统RL、未校准LLM、不同尺寸环境），有消融实验验证超参数影响。
  - **客观公平**：所有实验在相同种子下运行，对比方法均基于相同环境设置；但缺少与更多最新LLM引导RL方法（如SayCan、CLIPort等）的对比。
  - **局限性**：仅在一个benchmark（MiniGrid-UnlockPickup）上评估，未推广到其他稀疏奖励环境（如Minecraft、机器人控制）。

## 6. 论文的主要结论与发现
- **核心结论**：ULPS在MiniGrid UnlockPickup上显著优于所有对比方法：
  - 4×4环境：成功率99.9%，平均步数7.24，奖励AUC 2055.08。
  - 8×8环境：成功率99.7%，平均步数15.37，奖励AUC 1886.80。
  - 微调后执行准确率99.17%，高于先前工作（90%），Brier Score降至0.06，DA达到1.0。
- **关键发现**：
  - 不确定性校准至关重要：未校准LLM成功率仅94%且步数翻倍（18.39 vs 7.24），而ULPS通过MC dropout和熵加权避免了过度自信和无效探索。
  - LLM+RL结合优于任意单独使用：无引导RL成功率仅5.9%，Q-Learning 82.4%，DQN 11.6%，均远低于ULPS。
  - 消融实验显示dropout率0.1和8次前向传递在计算效率和性能间取得最佳平衡（AUC 2055.08，接近最高2055.88）。

## 7. 优点
- **方法创新**：
  - 首次将A*符号规划与LLM微调、MC dropout不确定性估计和熵加权策略塑形有机结合。
  - 自动生成最优轨迹微调LLM，无需人工标注，可扩展。
  - 自适应融合机制使LLM仅在低不确定时提供强引导，避免了幻觉的负面影响。
- **实验设计**：
  - 全面的对比基线（传统RL、未校准LLM、无引导RL），验证了各组件的必要性。
  - 消融实验系统分析了dropout率、前向传递次数对性能的影响。
  - 多指标评估（成功率、步数、AUC、校准误差、Brier Score）全面反映模型能力。
- **性能提升显著**：在稀疏奖励场景下，成功率提升至99.9%，步数减少86%（相比无引导RL），实现了高样本效率。

## 8. 不足与局限
- **实验覆盖有限**：仅在一个环境（MiniGrid-UnlockPickup）上测试，未在更复杂的稀疏奖励场景（如机器人操作、多智能体、部分可观测环境）验证泛化性。
- **未与最新方法对比**：未与后续出现的LLM引导RL方法（如Eureka、Voyager、TAMP-based方法）比较，也未与基于模型的RL对比。
- **计算开销**：MC dropout需要8次前向传递，虽然步数大幅减少，但每步推理时间增加；文中未给出具体推理延迟对比。
- **超参数敏感性**：消融实验显示dropout率和前向传递次数对性能有影响（如0.05 dropout+4次仅AUC 599.46），但未讨论如何自适应调整这些超参数。
- **潜在偏差风险**：LLM可能学习A*轨迹中的特定偏差（如只走曼哈顿距离最优路径），在更具随机性的环境中可能泛化不足。
- **未报告算力消耗**：缺失GPU型号、训练时间等关键信息，影响可复现性评估。

（完）
